---
phase: 03-footprint-signal-engines-e1-e8-e9
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - tests/test_imbalance.py
  - tests/test_delta.py
  - tests/test_auction.py
autonomous: true
requirements: [IMB-01, IMB-02, IMB-03, IMB-04, IMB-05, IMB-06, IMB-07, IMB-08, IMB-09, DELT-01, DELT-02, DELT-03, DELT-04, DELT-05, DELT-06, DELT-07, DELT-08, DELT-09, DELT-10, DELT-11, AUCT-01, AUCT-02, AUCT-03, AUCT-04, AUCT-05, ENG-01, ENG-08, ENG-09]

must_haves:
  truths:
    - "Every imbalance variant has at least one test that constructs a FootprintBar triggering that variant"
    - "Every delta variant has at least one test proving it fires under correct conditions"
    - "Every auction variant has at least one test proving detection"
    - "Config objects are tested to confirm they override default thresholds"
    - "Auction persistence round-trip is tested"
  artifacts:
    - path: "tests/test_imbalance.py"
      provides: "Comprehensive imbalance test suite"
      min_lines: 150
    - path: "tests/test_delta.py"
      provides: "Comprehensive delta test suite"
      min_lines: 150
    - path: "tests/test_auction.py"
      provides: "Comprehensive auction test suite"
      min_lines: 100
  key_links:
    - from: "tests/test_imbalance.py"
      to: "deep6/engines/imbalance.py"
      via: "import detect_imbalances"
      pattern: "from deep6.engines.imbalance import"
    - from: "tests/test_delta.py"
      to: "deep6/engines/delta.py"
      via: "import DeltaEngine"
      pattern: "from deep6.engines.delta import"
    - from: "tests/test_auction.py"
      to: "deep6/engines/auction.py"
      via: "import AuctionEngine"
      pattern: "from deep6.engines.auction import"
---

<objective>
Write comprehensive test suites for all three Phase 3 engines (imbalance, delta, auction) covering every signal variant, config override behavior, edge cases (empty bars, single-level bars), and auction level persistence. Each test constructs specific FootprintBar scenarios that trigger the target signal.

Purpose: Ensure all 25 signals fire correctly and regressions are caught. This is the verification gate before Phase 4 builds on these engines.
Output: Three test files with full variant coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-footprint-signal-engines-e1-e8-e9/03-CONTEXT.md
@.planning/phases/03-footprint-signal-engines-e1-e8-e9/03-01-SUMMARY.md
@.planning/phases/03-footprint-signal-engines-e1-e8-e9/03-02-SUMMARY.md

<interfaces>
From deep6/state/footprint.py:
```python
TICK_SIZE = 0.25

def price_to_tick(price: float) -> int:
    return round(price / TICK_SIZE)

def tick_to_price(tick: int) -> float:
    return tick * TICK_SIZE

@dataclass
class FootprintLevel:
    bid_vol: int = 0
    ask_vol: int = 0

class FootprintBar:
    def __init__(self): ...
    levels: dict[int, FootprintLevel]
    total_vol: int
    bar_delta: int
    cvd: float
    open: float; close: float; high: float; low: float
    bar_range: float
    poc_price: float
    timestamp: int
    def add_trade(self, price: float, size: int, aggressor: int) -> None: ...
    def finalize(self, prior_cvd: float = 0) -> None: ...
```

From tests/conftest.py:
```python
@pytest.fixture
def config() -> Config: ...

@pytest.fixture
def fake_tick_factory():
    def factory(price: float = 21000.0, size: int = 1, aggressor: int = 1) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Imbalance engine test suite</name>
  <files>tests/test_imbalance.py</files>
  <read_first>
    - deep6/engines/imbalance.py (current implementation after Plan 01)
    - deep6/engines/signal_config.py (ImbalanceConfig fields)
    - deep6/state/footprint.py (FootprintBar, FootprintLevel, price_to_tick)
    - tests/test_absorption.py (test pattern reference)
  </read_first>
  <behavior>
    - test_empty_bar_returns_no_signals: FootprintBar with no levels -> empty list
    - test_single_level_bar_returns_no_signals: Only 1 tick level -> empty list (need 2+ for diagonal)
    - test_single_buy_imbalance_detected (IMB-01): bar with ask[P]/bid[P-1] >= 3.0 -> SINGLE signal
    - test_single_sell_imbalance_detected (IMB-01): bar with bid[P]/ask[P+1] >= 3.0 -> SINGLE signal
    - test_oversized_imbalance (IMB-06): ratio >= 10.0 -> OVERSIZED instead of SINGLE
    - test_multiple_imbalance (IMB-02): 3+ imbalances at same price -> MULTIPLE signal
    - test_stacked_t1 (IMB-03): 3 consecutive imbalance levels -> STACKED_T1
    - test_stacked_t2 (IMB-03): 5 consecutive -> STACKED_T2
    - test_stacked_t3 (IMB-03): 7 consecutive -> STACKED_T3
    - test_reverse_imbalance (IMB-04): buy + sell imbalances in same bar -> REVERSE
    - test_inverse_trap_bearish (IMB-05): buy imbalances in red bar -> INVERSE_TRAP
    - test_inverse_trap_bullish (IMB-05): sell imbalances in green bar -> INVERSE_TRAP
    - test_diagonal_algorithm (IMB-08): verify ask[P] vs bid[P-1] comparison specifically
    - test_consecutive_across_bars (IMB-07): same level imbalanced in two bars -> CONSECUTIVE
    - test_reversal_pattern (IMB-09): prior bar buy-dominant, current sell-dominant -> REVERSAL
    - test_config_overrides: ImbalanceConfig(ratio_threshold=5.0) suppresses 3x imbalances
  </behavior>
  <action>
Create `tests/test_imbalance.py` with helper function `make_bar(**levels_data)` that builds a FootprintBar from a dict of {price: (bid_vol, ask_vol)} pairs, calling add_trade for each and finalize(). Each test constructs a specific FootprintBar scenario and asserts the correct ImbalanceType(s) appear in the result.

Use `price_to_tick` from footprint module for tick math. NQ tick size is 0.25, so adjacent prices are 0.25 apart.

For IMB-07 (consecutive): pass both current bar and prior_bar to detect_imbalances, assert CONSECUTIVE signal fires.
For IMB-09 (reversal): create prior_bar with dominant buy imbalances, current bar with dominant sell, assert REVERSAL fires.
  </action>
  <verify>
    <automated>cd /Users/teaceo/DEEP6 && python -m pytest tests/test_imbalance.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "test_single_buy_imbalance" tests/test_imbalance.py
    - grep -q "test_stacked_t1" tests/test_imbalance.py
    - grep -q "test_inverse_trap" tests/test_imbalance.py
    - grep -q "test_diagonal_algorithm" tests/test_imbalance.py
    - grep -q "test_consecutive_across_bars" tests/test_imbalance.py
    - grep -q "test_reversal_pattern" tests/test_imbalance.py
    - grep -q "test_config_overrides" tests/test_imbalance.py
    - python -m pytest tests/test_imbalance.py passes with 0 failures
  </acceptance_criteria>
  <done>
    - 15+ test functions covering all 9 IMB variants + edge cases + config override
    - All tests pass
    - Each test constructs specific FootprintBar data (not random)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delta engine test suite</name>
  <files>tests/test_delta.py</files>
  <read_first>
    - deep6/engines/delta.py (current implementation after Plan 01)
    - deep6/engines/signal_config.py (DeltaConfig fields)
    - deep6/state/footprint.py (FootprintBar)
    - tests/test_imbalance.py (bar-building helper from Task 1)
  </read_first>
  <behavior>
    - test_empty_bar_returns_no_signals: total_vol == 0 -> empty
    - test_rise_signal (DELT-01): positive delta -> RISE
    - test_drop_signal (DELT-01): negative delta -> DROP
    - test_tail_signal (DELT-02): |delta|/total_vol >= 0.95 -> TAIL
    - test_reversal_signal (DELT-03): delta sign opposite to bar direction -> REVERSAL
    - test_divergence_bearish (DELT-04): price at 5-bar high but CVD not -> DIVERGENCE direction=-1
    - test_divergence_bullish (DELT-04): price at 5-bar low but CVD not -> DIVERGENCE direction=+1
    - test_flip_to_negative (DELT-05): CVD crosses zero downward -> FLIP direction=-1
    - test_flip_to_positive (DELT-05): CVD crosses zero upward -> FLIP direction=+1
    - test_trap_bullish (DELT-06): strong buying delta then price drops -> TRAP
    - test_sweep_signal (DELT-07): many levels with increasing volume -> SWEEP
    - test_slingshot_signal (DELT-08): 3 quiet bars then explosive -> SLINGSHOT
    - test_at_max (DELT-09): CVD at session max -> AT_MAX
    - test_at_min (DELT-09): CVD at session min -> AT_MIN
    - test_cvd_multi_bar_divergence (DELT-10): 10+ bars with opposite price/CVD slopes -> CVD_DIVERGENCE
    - test_velocity_signal (DELT-11): large CVD acceleration -> VELOCITY
    - test_config_overrides: DeltaConfig(tail_threshold=0.99) suppresses 0.96 tails
    - test_engine_reset: reset() clears histories and session extremes
  </behavior>
  <action>
Create `tests/test_delta.py`. Use the same `make_bar()` helper pattern. For stateful tests (divergence, slingshot, CVD regression), feed the DeltaEngine a sequence of bars via repeated `process()` calls, constructing each bar to set up the desired condition.

Key: for DELT-10 (CVD divergence), create 10+ bars where price slopes up but CVD slopes down, using np.polyfit-compatible data.

For DELT-08 (slingshot), create 3 bars with very small delta relative to volume, then 1 bar with large delta.
  </action>
  <verify>
    <automated>cd /Users/teaceo/DEEP6 && python -m pytest tests/test_delta.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "test_rise_signal" tests/test_delta.py
    - grep -q "test_tail_signal" tests/test_delta.py
    - grep -q "test_divergence" tests/test_delta.py
    - grep -q "test_slingshot" tests/test_delta.py
    - grep -q "test_cvd_multi_bar" tests/test_delta.py
    - grep -q "test_sweep_signal" tests/test_delta.py
    - grep -q "test_velocity_signal" tests/test_delta.py
    - grep -q "test_config_overrides" tests/test_delta.py
    - python -m pytest tests/test_delta.py passes with 0 failures
  </acceptance_criteria>
  <done>
    - 17+ test functions covering all 11 DELT variants + engine reset + config override
    - All tests pass
    - Stateful tests feed multi-bar sequences to DeltaEngine
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Auction engine + persistence test suite</name>
  <files>tests/test_auction.py</files>
  <read_first>
    - deep6/engines/auction.py (current implementation after Plan 02)
    - deep6/engines/signal_config.py (AuctionConfig fields)
    - deep6/state/persistence.py (auction_levels methods after Plan 02)
    - deep6/state/footprint.py (FootprintBar)
  </read_first>
  <behavior>
    - test_empty_bar_returns_no_signals: no levels -> empty
    - test_unfinished_business_high (AUCT-01): bid_vol > 0 at bar high -> UNFINISHED_BUSINESS +1
    - test_unfinished_business_low (AUCT-01): ask_vol > 0 at bar low -> UNFINISHED_BUSINESS -1
    - test_finished_auction_high (AUCT-02): bid_vol == 0, ask_vol > 0 at high -> FINISHED_AUCTION -1
    - test_finished_auction_low (AUCT-02): ask_vol == 0, bid_vol > 0 at low -> FINISHED_AUCTION +1
    - test_poor_high (AUCT-03): vol at high < 30% avg -> POOR_HIGH
    - test_poor_low (AUCT-03): vol at low < 30% avg -> POOR_LOW
    - test_volume_void (AUCT-04): 3+ levels with < 5% of max vol -> VOLUME_VOID
    - test_market_sweep_up (AUCT-05): up bar with second half vol > 1.5x first half -> MARKET_SWEEP +1
    - test_market_sweep_down (AUCT-05): down bar equivalent -> MARKET_SWEEP -1
    - test_fsm_exploring_up (ENG-09): bar making new high -> EXPLORING_UP state
    - test_fsm_balanced (ENG-09): 3 bars inside range -> BALANCED state
    - test_fsm_breakout (ENG-09): large range expansion -> BREAKOUT state
    - test_unfinished_levels_tracking: process bar, check get_unfinished_levels returns level
    - test_load_unfinished_levels: load_unfinished_levels populates correctly
    - test_clear_finished_level: clear_finished_level removes price from tracking
    - test_config_overrides: AuctionConfig(poor_extreme_vol_ratio=0.1) changes poor high/low threshold
    - test_auction_persistence_roundtrip (async): persist -> restore -> resolve cycle
  </behavior>
  <action>
Create `tests/test_auction.py`. Use `make_bar()` helper. For FSM tests, feed multiple bars and check `engine.state` after each.

For the async persistence test, use `pytest.mark.asyncio` (or `asyncio.run()` in a sync test) with in-memory SQLite.

For unfinished level tracking tests, verify that after processing a bar with unfinished business at high, `get_unfinished_levels()` returns that price.
  </action>
  <verify>
    <automated>cd /Users/teaceo/DEEP6 && python -m pytest tests/test_auction.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "test_unfinished_business" tests/test_auction.py
    - grep -q "test_finished_auction" tests/test_auction.py
    - grep -q "test_poor_high" tests/test_auction.py
    - grep -q "test_volume_void" tests/test_auction.py
    - grep -q "test_market_sweep" tests/test_auction.py
    - grep -q "test_fsm" tests/test_auction.py
    - grep -q "test_auction_persistence" tests/test_auction.py
    - grep -q "test_config_overrides" tests/test_auction.py
    - python -m pytest tests/test_auction.py passes with 0 failures
  </acceptance_criteria>
  <done>
    - 17+ test functions covering all 5 AUCT variants + FSM states + persistence + config
    - All tests pass
    - Async persistence round-trip verified with in-memory SQLite
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test-only plan, no production trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | Tests only | accept | No security implications in test code |
</threat_model>

<verification>
- `python -m pytest tests/test_imbalance.py tests/test_delta.py tests/test_auction.py -v` all pass
- Each test file has 15+ test functions covering all variants
</verification>

<success_criteria>
All 25 signal variants have at least one dedicated test. Config override tests prove thresholds are parameterizable. Auction persistence round-trip is verified. Zero test failures.
</success_criteria>

<output>
After completion, create `.planning/phases/03-footprint-signal-engines-e1-e8-e9/03-03-SUMMARY.md`
</output>
