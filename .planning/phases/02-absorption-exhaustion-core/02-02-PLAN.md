---
phase: 02-absorption-exhaustion-core
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - deep6/engines/absorption.py
  - deep6/engines/narrative.py
  - deep6/engines/signal_config.py
autonomous: true
requirements: [ABS-06, ABS-07]

must_haves:
  truths:
    - "Absorption at VAH/VAL within 2 ticks produces a conviction bonus flag on the signal"
    - "Absorption confirmation logic upgrades zone score by +2 when defense occurs within 3 bars"
    - "Conviction bonus is visible in AbsorptionSignal fields and narrative labels"
  artifacts:
    - path: "deep6/engines/absorption.py"
      provides: "VA extremes bonus flag on AbsorptionSignal"
      contains: "at_va_extreme"
    - path: "deep6/engines/narrative.py"
      provides: "Confirmation tracker that upgrades absorption zone scores"
      contains: "AbsorptionConfirmation"
    - path: "deep6/engines/signal_config.py"
      provides: "Confirmation and VA extremes config params"
      contains: "va_extreme_ticks"
  key_links:
    - from: "deep6/engines/narrative.py"
      to: "deep6/engines/absorption.py"
      via: "reads at_va_extreme flag from AbsorptionSignal"
      pattern: "at_va_extreme"
    - from: "deep6/engines/narrative.py"
      to: "deep6/engines/signal_config.py"
      via: "uses confirmation_window_bars and confirmation_score_bonus"
      pattern: "confirmation_window_bars"
---

<objective>
Implement ABS-07 (VA extremes conviction bonus) and ABS-06 (absorption confirmation logic with defense window) — the two missing absorption requirements.

Purpose: Absorption at value area extremes is the highest-conviction reversal setup in the system. Confirmation logic validates that the absorption zone holds, upgrading its score for the Phase 7 scorer.
Output: AbsorptionSignal with at_va_extreme flag, confirmation tracker in narrative.py, config params for both features.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-absorption-exhaustion-core/02-01-SUMMARY.md
@deep6/engines/absorption.py
@deep6/engines/narrative.py
@deep6/engines/signal_config.py
@deep6/engines/poc.py
@deep6/engines/volume_profile.py
@deep6/state/footprint.py

<interfaces>
<!-- From Plan 01 output -->

From deep6/engines/signal_config.py (created in Plan 01):
```python
@dataclass(frozen=True)
class AbsorptionConfig:
    absorb_wick_min: float = 30.0
    absorb_delta_max: float = 0.12
    passive_extreme_pct: float = 0.20
    passive_vol_pct: float = 0.60
    stop_vol_mult: float = 2.0
    evr_vol_mult: float = 1.5
    evr_range_cap: float = 0.30
```

From deep6/engines/absorption.py:
```python
@dataclass
class AbsorptionSignal:
    bar_type: AbsorptionType
    direction: int
    price: float
    wick: str
    strength: float
    wick_pct: float
    delta_ratio: float
    detail: str

def detect_absorption(bar, atr=15.0, vol_ema=500.0, config=None) -> list[AbsorptionSignal]
```

From deep6/engines/poc.py:
```python
class POCEngine:
    def _compute_va(self, bar) -> tuple[float, float]:  # returns (vah, val)
```

From deep6/engines/narrative.py:
```python
def classify_bar(bar, prior_bar=None, bar_index=0, atr=15.0, vol_ema=500.0,
                  vwap=None, vah=None, val=None,
                  abs_config=None, exh_config=None) -> NarrativeResult
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VA extremes conviction bonus to AbsorptionSignal (ABS-07, D-05)</name>
  <files>deep6/engines/absorption.py, deep6/engines/signal_config.py, deep6/engines/narrative.py</files>
  <read_first>deep6/engines/absorption.py, deep6/engines/signal_config.py, deep6/engines/narrative.py, deep6/engines/poc.py</read_first>
  <action>
1. Add to `AbsorptionConfig` in signal_config.py:
   - `va_extreme_ticks: int = 2` — how close to VAH/VAL in ticks to qualify as "at VA extreme" (per D-05: within 2 ticks)
   - `va_extreme_strength_bonus: float = 0.15` — additive bonus to strength when at VA extreme

2. Add field to `AbsorptionSignal` dataclass in absorption.py:
   - `at_va_extreme: bool = False` — True when signal price is within va_extreme_ticks of VAH or VAL

3. Update `detect_absorption()` signature to accept `vah: float | None = None` and `val: float | None = None` parameters. After each signal is created, check if the signal's price is within `config.va_extreme_ticks * 0.25` (tick_size) points of either vah or val. If so:
   - Set `signal.at_va_extreme = True`
   - Add `config.va_extreme_strength_bonus` to `signal.strength` (cap at 1.0)
   - Append " @VAH" or " @VAL" to `signal.detail`

4. Update `classify_bar()` in narrative.py to pass `vah=vah, val=val` to `detect_absorption()` call. The vah/val params already exist on classify_bar's signature.

5. Update `_absorption_label()` in narrative.py: when `sig.at_va_extreme` is True, ensure the label includes the VA location. The existing logic already checks proximity to vah/val for label generation — verify it produces labels like "SELLERS ABSORBED @VAL -- HIGH CONVICTION LONG ZONE". If the at_va_extreme flag is True but the label doesn't reflect it (possible if the 5.0 proximity threshold in _absorption_label differs from the 2-tick threshold), update _absorption_label to check sig.at_va_extreme directly instead of re-computing proximity.
  </action>
  <verify>
    <automated>cd /Users/teaceo/DEEP6 && python -c "
from deep6.engines.absorption import detect_absorption, AbsorptionSignal
from deep6.engines.signal_config import AbsorptionConfig
from deep6.state.footprint import FootprintBar, FootprintLevel, price_to_tick

# Build a bar with heavy lower wick volume (classic absorption)
bar = FootprintBar()
bar.open = 21002.0; bar.close = 21003.0; bar.high = 21004.0; bar.low = 21000.0
bar.bar_range = 4.0; bar.bar_delta = 0; bar.total_vol = 1000; bar.poc_price = 21001.0

# Add levels: heavy balanced volume in lower wick
for tick_price in [21000.0, 21000.25, 21000.5, 21000.75]:
    tick = price_to_tick(tick_price)
    bar.levels[tick] = FootprintLevel(bid_vol=80, ask_vol=75)
for tick_price in [21001.0, 21002.0, 21003.0, 21004.0]:
    tick = price_to_tick(tick_price)
    bar.levels[tick] = FootprintLevel(bid_vol=20, ask_vol=25)

# Test with VAL at 21000.25 (within 2 ticks of absorption price 21000.0)
sigs = detect_absorption(bar, atr=15.0, vol_ema=200.0, vah=21010.0, val=21000.25)
va_sigs = [s for s in sigs if s.at_va_extreme]
print(f'Signals: {len(sigs)}, VA extreme: {len(va_sigs)}')
if va_sigs:
    print(f'at_va_extreme={va_sigs[0].at_va_extreme}, detail={va_sigs[0].detail}')
    assert va_sigs[0].at_va_extreme == True
    print('VA extreme bonus OK')
else:
    print('No VA extreme signals (check threshold conditions)')

# Verify at_va_extreme field exists on all signals
assert hasattr(AbsorptionSignal, '__dataclass_fields__')
assert 'at_va_extreme' in AbsorptionSignal.__dataclass_fields__
print('at_va_extreme field exists OK')

# Verify config has new fields
config = AbsorptionConfig()
assert config.va_extreme_ticks == 2
assert config.va_extreme_strength_bonus == 0.15
print('Config fields OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `AbsorptionSignal` has field `at_va_extreme: bool = False`
    - `AbsorptionConfig` has fields `va_extreme_ticks: int = 2` and `va_extreme_strength_bonus: float = 0.15`
    - `detect_absorption` accepts `vah` and `val` optional parameters
    - Signals within 2 ticks (0.50 points) of VAH or VAL have `at_va_extreme=True`
    - Signal strength is boosted by 0.15 when at VA extreme (capped at 1.0)
    - Signal detail string includes "@VAH" or "@VAL" when at VA extreme
    - _absorption_label uses sig.at_va_extreme flag directly for label generation
  </acceptance_criteria>
  <done>Absorption at VA extremes produces conviction bonus flag with strength boost and label annotation per ABS-07 and D-05.</done>
</task>

<task type="auto">
  <name>Task 2: Implement absorption confirmation logic with defense window (ABS-06, D-06, D-07)</name>
  <files>deep6/engines/narrative.py, deep6/engines/signal_config.py</files>
  <read_first>deep6/engines/narrative.py, deep6/engines/signal_config.py, deep6/engines/absorption.py</read_first>
  <action>
Per D-06 and D-07: After an absorption signal fires, track whether the absorption zone is "defended" within the next 3 bars. Defense = price holds within absorption zone (doesn't break through by more than 2 ticks) AND at least one bar shows same-direction delta.

1. Add to `AbsorptionConfig` in signal_config.py:
   - `confirmation_window_bars: int = 3` — number of bars to watch for defense (D-06)
   - `confirmation_score_bonus: float = 2.0` — additive score upgrade on confirmation (D-07)
   - `confirmation_breach_ticks: int = 2` — max ticks price can breach zone before defense fails (D-06)

2. Create `AbsorptionConfirmation` dataclass in narrative.py:
   ```python
   @dataclass
   class AbsorptionConfirmation:
       signal: AbsorptionSignal
       bar_fired: int           # bar_index when absorption fired
       zone_price: float        # absorption price level
       direction: int           # +1 or -1
       confirmed: bool = False
       expired: bool = False
   ```

3. Add a module-level `_pending_confirmations: list[AbsorptionConfirmation] = []` in narrative.py.

4. Add `reset_confirmations() -> None` function to clear pending confirmations (session start).

5. In `classify_bar()`, AFTER detecting absorption signals, add confirmation tracking logic:
   a. For each new absorption signal: create AbsorptionConfirmation and add to _pending_confirmations
   b. For each pending confirmation (not yet confirmed or expired):
      - Check if `bar_index - conf.bar_fired > config.confirmation_window_bars` → mark expired
      - Check defense: if direction is +1 (bullish absorption at low), price should not drop more than `confirmation_breach_ticks * 0.25` below zone_price. If direction is -1, price should not rise more than `confirmation_breach_ticks * 0.25` above zone_price.
      - Check same-direction delta: for bullish absorption, bar.bar_delta > 0 in at least one subsequent bar. For bearish, bar.bar_delta < 0.
      - If defense holds AND same-direction delta seen → mark confirmed = True

6. Add `confirmed_absorptions: list[AbsorptionConfirmation]` field to `NarrativeResult` with default_factory=list. Populate with any confirmations that triggered this bar.

7. The +2 score bonus from D-07 is NOT applied here — it feeds into the Phase 7 scorer. The confirmation flag and data are the output of this task. Document in code comment that scorer.py should read `result.confirmed_absorptions` and add `confirmation_score_bonus` to zone score.
  </action>
  <verify>
    <automated>cd /Users/teaceo/DEEP6 && python -c "
from deep6.engines.narrative import classify_bar, NarrativeResult, AbsorptionConfirmation, reset_confirmations, _pending_confirmations
from deep6.engines.signal_config import AbsorptionConfig
from deep6.state.footprint import FootprintBar, FootprintLevel, price_to_tick

# Verify AbsorptionConfirmation exists
assert hasattr(AbsorptionConfirmation, '__dataclass_fields__')
print('AbsorptionConfirmation exists OK')

# Verify NarrativeResult has confirmed_absorptions field
assert 'confirmed_absorptions' in NarrativeResult.__dataclass_fields__
print('NarrativeResult.confirmed_absorptions field OK')

# Verify reset_confirmations clears state
reset_confirmations()
assert len(_pending_confirmations) == 0
print('reset_confirmations OK')

# Verify config has confirmation fields
config = AbsorptionConfig()
assert config.confirmation_window_bars == 3
assert config.confirmation_score_bonus == 2.0
assert config.confirmation_breach_ticks == 2
print('Confirmation config OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `AbsorptionConfirmation` dataclass exists in narrative.py with fields: signal, bar_fired, zone_price, direction, confirmed, expired
    - `_pending_confirmations` module-level list exists in narrative.py
    - `reset_confirmations()` function exists and clears the list
    - `NarrativeResult` has `confirmed_absorptions: list[AbsorptionConfirmation]` field
    - `AbsorptionConfig` has `confirmation_window_bars: int = 3`, `confirmation_score_bonus: float = 2.0`, `confirmation_breach_ticks: int = 2`
    - Confirmation checks defense (price holds within zone +/- breach tolerance) AND same-direction delta
    - Confirmations expire after confirmation_window_bars without defense
    - classify_bar populates confirmed_absorptions on the bar where confirmation occurs
  </acceptance_criteria>
  <done>Absorption confirmation logic tracks defense within 3-bar window and produces confirmation events with +2 score bonus data for Phase 7 scorer per ABS-06, D-06, D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VA levels (vah/val) | External input from POCEngine — incorrect values produce false VA extreme flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | at_va_extreme flag | accept | Flag is informational for scoring — no execution action taken directly from it |
| T-02-05 | Spoofing | _pending_confirmations | mitigate | Module-level state reset at session start via reset_confirmations(); no cross-session leakage |
</threat_model>

<verification>
1. AbsorptionSignal.at_va_extreme field exists and defaults to False
2. detect_absorption accepts vah/val params and sets at_va_extreme correctly
3. AbsorptionConfirmation dataclass exists with all required fields
4. NarrativeResult.confirmed_absorptions field exists
5. AbsorptionConfig has va_extreme_ticks, confirmation_window_bars, confirmation_score_bonus, confirmation_breach_ticks
</verification>

<success_criteria>
- Absorption at VA extremes produces conviction bonus (at_va_extreme=True, +0.15 strength, label with @VAH/@VAL)
- Confirmation tracker watches 3-bar window for zone defense + same-direction delta
- Confirmations propagate via NarrativeResult.confirmed_absorptions for scorer consumption
- All module state resettable for session boundaries
</success_criteria>

<output>
After completion, create `.planning/phases/02-absorption-exhaustion-core/02-02-SUMMARY.md`
</output>
