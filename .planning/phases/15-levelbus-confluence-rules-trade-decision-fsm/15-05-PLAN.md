---
phase: 15-levelbus-confluence-rules-trade-decision-fsm
plan: 05
type: execute
wave: 4
depends_on: [15-01, 15-02, 15-03, 15-04]
files_modified:
  - tests/integration/test_phase15_end_to_end.py
  - tests/integration/test_fsm_reachability.py
  - tests/integration/test_cr_goldens_full.py
  - tests/integration/fixtures/phase15_fixtures.py
  - tests/integration/fixtures/synthetic_sessions.py
  - tests/integration/conftest.py
autonomous: true
requirements: [ZONE-01, ZONE-02, ZONE-03, EXEC-01, EXEC-03, GEX-04]

must_haves:
  truths:
    - "Every CR-XX rule from RULES.md has a golden-file test exercising both trigger and no-trigger cases"
    - "Every transition T1-T11 in the FSM is reachable from at least one synthetic fixture (D-38)"
    - "End-to-end integration: synthetic NQ session → VPContextEngine → LevelBus populated → ConfluenceRules evaluated → TradeDecisionMachine emits orders → RiskManager validates → EventStore records"
    - "Performance: full pipeline handles 390 bars (one RTH session) in < 5 seconds; ConfluenceRules.evaluate < 1ms/bar with 80 levels (D-34)"
    - "Test fixtures are either from Phase 13 replay harness (preferred) OR synthetic (documented fallback per D-36)"
  artifacts:
    - path: "tests/integration/test_phase15_end_to_end.py"
      provides: "Full pipeline test: 5 synthetic sessions covering Normal / Trend / Double Distribution / Neutral / Non-Trend day types"
      min_lines: 300
    - path: "tests/integration/test_fsm_reachability.py"
      provides: "Reachability test for every transition T1-T11 using synthetic fixtures"
      min_lines: 200
    - path: "tests/integration/test_cr_goldens_full.py"
      provides: "Comprehensive golden-file suite — cross-checks plan 15-03 unit tests with integration-level triggers"
      min_lines: 250
    - path: "tests/integration/fixtures/synthetic_sessions.py"
      provides: "Day-type fixtures (Normal / Trend / DD / Neutral / Non-Trend) per D-36 fallback path"
      min_lines: 300
  key_links:
    - from: "tests/integration/test_phase15_end_to_end.py"
      to: "deep6/engines/vp_context_engine + confluence_rules + execution/trade_decision_machine"
      via: "import + drive full bar stream"
      pattern: "VPContextEngine|evaluate|TradeDecisionMachine"
    - from: "tests/integration/fixtures/phase15_fixtures.py"
      to: "Phase 13 replay harness OR synthetic sessions"
      via: "fixture selector with env-based fallback"
      pattern: "replay_harness|synthetic"
---

<objective>
Validate the entire Phase 15 delivery with integration + reachability + golden-file tests. Covers (a) every CR-XX rule from RULES.md end-to-end (input bar → ConfluenceAnnotations → score mutation → tier outcome), (b) every FSM transition T1-T11 reachable from synthetic fixtures (D-38), and (c) a 5-session end-to-end integration: synthetic NQ bars → VPContextEngine → LevelBus → ConfluenceRules → TradeDecisionMachine → RiskManager → EventStore persistence (D-36 with Phase-13 replay as preferred source and synthetic fixtures as documented fallback).

Purpose: Plans 15-01 through 15-04 each have unit tests. Plan 15-05 is the integration gate — proves the pieces fit together, proves reachability of every FSM transition from real-looking inputs, and pins a performance baseline. If Phase 13 backtest harness lands before this plan executes, use its replay fixtures; otherwise use synthetic (note the swap in the integration SUMMARY).
Output: Integration test module covering 5 day-types; FSM reachability module covering T1-T11; full-suite CR golden runner; synthetic session fixture library (for fallback path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-CONTEXT.md
@.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/RULES.md
@.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-01-SUMMARY.md
@.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-02-SUMMARY.md
@.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-03-SUMMARY.md
@.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-04-SUMMARY.md

@.planning/research/pine/deep/auction_theory.md

@deep6/engines/level.py
@deep6/engines/level_factory.py
@deep6/engines/zone_registry.py
@deep6/engines/confluence_rules.py
@deep6/engines/vp_context_engine.py
@deep6/execution/trade_state.py
@deep6/execution/trade_decision_machine.py
@deep6/execution/engine.py
@deep6/state/eventstore_schema.py

<interfaces>
# Phase 13 replay harness (optional — preferred if available):
# from deep6.backtest.replay import ReplayHarness  # only if phase 13 landed
# Otherwise synthetic fixture builder in tests/integration/fixtures/synthetic_sessions.py

# Day-type classification (auction_theory.md): Normal, Trend, Double Distribution, Neutral, Non-Trend
# Each day-type fixture produces ~390 FootprintBars (one RTH session)

# End-to-end pipeline (as of plans 15-01..15-04):
# FootprintBar →
#   VPContextEngine.process(bar, narrative_result) →
#     LevelBus populated (zones + narrative Levels + GEX point-Levels) →
#   ConfluenceRules.evaluate(levels, gex_signal, bar, scorer_result) →
#     ConfluenceAnnotations →
#   scorer.score_bar(... confluence_annotations=annotations) →
#     ScorerResult (with meta_flags) →
#   TradeDecisionMachine.on_bar(bar, level_bus, scorer_result, confluence_annotations) →
#     list[OrderIntent] + FSM state transitions →
#   risk_manager.validate(intent) →
#     broker submission (stubbed in tests) →
#   EventStore (fsm_transitions + existing signal_events/trade_events)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>T-15-05-01: Synthetic day-type session fixtures + Phase-13-harness fallback detector</name>
  <files>tests/integration/fixtures/synthetic_sessions.py, tests/integration/fixtures/phase15_fixtures.py, tests/integration/conftest.py</files>
  <behavior>
    - `synthetic_sessions.py` produces 5 deterministic day-type sessions, each ≈ 390 FootprintBars (RTH):
      * `build_normal_day()` — auction builds inside initial balance, closes near POC
      * `build_trend_day()` — one-directional auction, extends IB early, closes at extreme
      * `build_double_distribution_day()` — two distinct value areas, migration between them
      * `build_neutral_day()` — range extends both sides of IB, closes near open
      * `build_non_trend_day()` — sub-1.5×IB range, no extension
    - Each fixture yields tuples `(bar, narrative_result, gex_signal)` — narrative_result and gex_signal are synthetic to exercise ConfluenceRules (e.g., trend day seeds MOMENTUM Levels; neutral day seeds rejection + absorption at both extremes)
    - Fixtures are deterministic (seeded RNG) — same seed → same bar stream
    - `phase15_fixtures.py` provides a `session_source(day_type: str)` fixture that returns Phase 13 replay if `DEEP6_USE_REPLAY=1` AND replay harness module importable, else falls back to synthetic. Emits a log line at fixture resolution time declaring which source was used (captured into the test report).
    - `conftest.py` at tests/integration/ exports `session_source` + makes synthetic_sessions module importable from all phase 15 integration tests.

    Tests (smoke only in this task):
    - test_normal_day_yields_390_bars
    - test_all_day_types_seeded_deterministically: two invocations of the same fixture with same seed produce byte-identical bar sequences
    - test_fallback_log_records_source: pytest caplog contains "fixture_source=synthetic" or "fixture_source=replay_harness"
    - test_replay_path_skipped_when_harness_missing: force-miss import → no crash, fall back to synthetic
  </behavior>
  <action>
    Create `tests/integration/__init__.py` (empty) and `tests/integration/fixtures/__init__.py` (empty) if missing.

    Write `tests/integration/fixtures/synthetic_sessions.py` — builder functions per day-type. Use `numpy.random.default_rng(seed)` seeded from day-type string hash. Build FootprintBars with realistic NQ price ranges (e.g., 18000-19000), tick_size=0.25, typical total_vol=500-3000 per 1m bar. Seed narrative signals at specific bar indices per day-type profile (e.g., trend day: MOMENTUM at bar 30, absorption at bar 100; double distribution: rejection at bar 120, acceptance at bar 240).

    Write `tests/integration/fixtures/phase15_fixtures.py`:
    ```python
    def _try_replay_harness():
        if os.environ.get("DEEP6_USE_REPLAY") != "1":
            return None
        try:
            from deep6.backtest.replay import ReplayHarness  # phase 13
            return ReplayHarness
        except ImportError:
            return None

    @pytest.fixture
    def session_source(request, caplog):
        harness = _try_replay_harness()
        ...
    ```

    Write `tests/integration/conftest.py` exposing the fixture.

    Add smoke tests inside `tests/integration/fixtures/test_synthetic_smoke.py`.
  </action>
  <verify>
    <automated>pytest tests/integration/fixtures/ -x -q</automated>
  </verify>
  <done>5 day-type synthetic fixtures are deterministic and produce 390-bar sessions. Phase-13 replay fallback detector works silently when harness absent.</done>
</task>

<task type="auto" tdd="true">
  <name>T-15-05-02: FSM reachability test — every T1-T11 fires from at least one synthetic fixture (D-38)</name>
  <files>tests/integration/test_fsm_reachability.py</files>
  <behavior>
    - For each transition T1 through T11: construct a minimal synthetic scenario that forces the machine to execute that transition; assert transition emitted via EventStore fsm_transitions rows
    - Use an in-memory EventStore (SQLite :memory:) + collect all fsm_transitions rows
    - Tests are table-driven: `TRANSITION_SCENARIOS = {T1: build_T1_scenario, T2: build_T2_scenario, ...}`
    - Each scenario drives the TradeDecisionMachine via its public interface (on_bar / on_fill / on_reject), NOT by hand-setting internal state (D-38 explicit: "every transition reachable from synthetic fixtures. No hand-traced paths.")
    - Aggregate assertion: `set(eventstore_rows.transition_id) >= {T1..T11}`

    Individual scenarios:
    - T1_idle_to_watching: LevelBus seeded with Level(score=70) near price; one on_bar call
    - T2_watching_to_armed: after T1, inject high-confluence annotations (no PIN) and bump scorer_result to TYPE_B+ → one more on_bar
    - T3_armed_to_triggered: after T2, inject ET-01 trigger fixture (ABSORB above wick at VAH) → on_bar emits order
    - T4_triggered_to_in_position: after T3, call on_fill with synthetic fill
    - T5_triggered_to_armed: after T3, call on_reject → should go back to ARMED if within retry window
    - T6_triggered_to_idle: after T3, advance 3 bars without fill (timeout=2) → IDLE
    - T7_in_position_to_managing: after T4, advance to first target checkpoint → MANAGING
    - T8_managing_to_exiting: after T7, inject I9 fixture (price retraces from MFE 10R to 4R) → EXITING
    - T9_exiting_to_idle: after T8, call on_fill (exit fill) → IDLE
    - T10_armed_to_idle: after T2, inject confluence drop → IDLE via T10
    - T11_watching_to_idle: after T1, invalidate all watched Levels → IDLE via T11

    Additional tests:
    - test_reachability_aggregate: after running all scenarios in sequence against independent machine instances, assert all 11 transition IDs present in eventstore records
    - test_reachability_no_hand_state_mutation: assert tests only use public on_bar / on_fill / on_reject (grep test source for "._state =" and fail if found, excluding fixture code)
    - test_reachability_coverage_report: print per-transition fixture name + row count at end of suite (informational)
  </behavior>
  <action>
    Write `tests/integration/test_fsm_reachability.py` with the 11 scenario builders + aggregate tests. Each scenario is a standalone function that returns (fsm, eventstore_rows) after running.

    Scenarios use the fixtures from T-15-05-01 where applicable (e.g., T2 uses trend-day bar 30 setup).

    Use pytest.fixture(scope="function") for a fresh machine + eventstore per scenario.
  </action>
  <verify>
    <automated>pytest tests/integration/test_fsm_reachability.py -x -q -v</automated>
  </verify>
  <done>All 11 FSM transitions fire from public-API-only synthetic scenarios. EventStore records prove it.</done>
</task>

<task type="auto" tdd="true">
  <name>T-15-05-03: End-to-end integration + full CR goldens + performance baseline</name>
  <files>tests/integration/test_phase15_end_to_end.py, tests/integration/test_cr_goldens_full.py</files>
  <behavior>
    End-to-end (test_phase15_end_to_end.py):
    - test_normal_day_full_pipeline: drive full pipeline from synthetic normal-day bars; assert:
      * LevelBus has ≥ 20 active Levels at session midpoint
      * ConfluenceRules.evaluate is called for every bar; never raises; annotations.regime ∈ {"NEUTRAL","BALANCE","TREND","PIN"}
      * TradeDecisionMachine state machine visits at least {IDLE, WATCHING, ARMED, IDLE} over the session
      * EventStore fsm_transitions table has ≥ 5 rows (hit-rate sanity floor for a normal day)
    - test_trend_day_full_pipeline: as above but also asserts at least one IN_POSITION state entered and exited via I9-less path (target hit)
    - test_double_distribution_full_pipeline: asserts regime transitions observed in annotations over session (REGIME_CHANGE flag fires at least once)
    - test_neutral_day_full_pipeline: asserts rejection-driven entries at both extremes
    - test_non_trend_day_full_pipeline: asserts LOW activity — FSM mostly IDLE/WATCHING; zero or one position
    - test_risk_manager_gates_hit_in_session: at least one scenario triggers risk_manager veto (stop_too_wide D-05 or D-16/D-17 GEX gate) — assert no order broker-submitted in that case
    - test_eventstore_fsm_transitions_integrity: after session, every row has valid (from_state, to_state, transition_id) matching ALLOWED_TRANSITIONS

    Full CR goldens (test_cr_goldens_full.py):
    - Re-run each CR-XX from RULES.md with its fixture at the INTEGRATION level (not just the stateless unit-level from plan 15-03). Difference: integration feeds through VPContextEngine.process + ConfluenceRules.evaluate + scorer, not rule function in isolation.
    - For each CR-XX: one trigger bar + one no-trigger bar; assert (trigger emits rule_hit, no-trigger doesn't) AND (scorer output reflects mutation)
    - Parametrized test — one test function, one param per CR-XX — clear failure report

    Performance:
    - test_pipeline_perf_390_bars: full pipeline for one synthetic normal day in < 5 seconds on dev machine; log wall time
    - test_confluence_evaluate_perf_p95_lt_1ms: 80-level LevelBus + 100 evaluate calls; p95 latency < 1ms (D-34). Mark as xfail on CI (env `CI=true`) since CI runners are slower; hard-fail locally.

    Retrospective artifact:
    - After all tests pass, write a summary note into 15-05-SUMMARY.md listing:
      * Fixture source used (synthetic vs replay)
      * CR-XX counts validated / total
      * FSM transitions reached / 11
      * p95 ConfluenceRules latency
      * Observed day-type regime distribution
  </behavior>
  <action>
    Write `tests/integration/test_phase15_end_to_end.py` with the 7 pipeline tests. Each test constructs: VPContextEngine, ConfluenceRules config, TradeDecisionMachine with in-memory EventStore. Drives the full session via the session_source fixture from T-15-05-01. Uses a stub broker that records submit() calls but never sends.

    Write `tests/integration/test_cr_goldens_full.py` with a parametrized test. Reads CR-XX list from RULES.md at test-collection time (pytest_collection_modifyitems hook or module-level scan). Each param feeds its fixture through the integration stack.

    Wire performance tests. Use `time.perf_counter_ns`. Log results via structlog at INFO — the 15-05 SUMMARY captures them.

    Add SUMMARY writer at end of module (pytest_sessionfinish hook): dumps aggregate stats to `.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-05-INTEGRATION-REPORT.md` (informational artifact; not committed unless user wants it).
  </action>
  <verify>
    <automated>pytest tests/integration/ -x -q --tb=short && pytest tests/ -x -q</automated>
  </verify>
  <done>5 day-types pass end-to-end; every CR-XX validated at integration level; performance baseline met on dev machine; no regression in any unit test from prior phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic fixtures → production pipeline | Fixtures are in-repo + deterministic; no external data ingress during tests |
| Phase 13 replay harness (when used) | Preloaded Databento parquet; validated upstream |
| Stub broker | Records intent only, no network egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Tampering | A test passes because it hand-mutates FSM internal state | mitigate | test_reachability_no_hand_state_mutation scans test source for `._state =` assignments. |
| T-15-05-02 | DoS | Integration tests hang on session replay | mitigate | pytest-timeout per session test (60s cap); synthetic sessions have hard 390-bar limit. |
| T-15-05-03 | Information Disclosure | Replay harness leaks Databento proprietary data into test logs | accept | Tests run locally only; Databento subscription + data are per-user. Mask nothing — user owns both. |
| T-15-05-04 | Repudiation | A CR-XX "passes" because its fixture is vacuous | mitigate | Each CR-XX must have both trigger + no-trigger case; assertion failures in either case fails the rule; parametrized IDs reported in pytest output. |
</threat_model>

<verification>
- `pytest tests/integration/ -x -q --tb=short` green
- `pytest tests/ -x -q` full repo green (no regression anywhere)
- `grep -cE "test_T[0-9]+_" tests/integration/test_fsm_reachability.py` ≥ 11
- `grep -c "def test_" tests/integration/test_phase15_end_to_end.py` ≥ 7
- Performance: `pytest tests/integration/test_phase15_end_to_end.py -k perf -v` p95 ConfluenceRules < 1ms on dev; 390-bar session < 5s
- Reachability coverage: all T1..T11 IDs present in aggregated fsm_transitions rows (logged in test report)
- Fixture source declared in report: synthetic OR replay_harness
</verification>

<success_criteria>
1. 5 day-type synthetic sessions (Normal / Trend / Double Distribution / Neutral / Non-Trend) runnable deterministically; Phase-13 replay fallback detected when available
2. All 11 FSM transitions reachable from public-API-only synthetic scenarios (D-38); proven via EventStore rows
3. Every CR-XX rule from RULES.md validated at integration level (trigger + no-trigger cases); parametrized failure reporting
4. End-to-end pipeline (VPContextEngine → LevelBus → ConfluenceRules → TradeDecisionMachine → RiskManager → EventStore) runs clean across all 5 day-types
5. Performance baseline: ConfluenceRules.evaluate p95 < 1ms with 80 levels (D-34); full 390-bar session < 5s
6. No regression in any prior-phase unit test
7. Integration report artifact captures fixture source + coverage + timings for future retrospective
</success_criteria>

<output>
After completion, create `.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-05-SUMMARY.md`
</output>
