---
phase: 15-levelbus-confluence-rules-trade-decision-fsm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deep6/engines/level.py
  - deep6/engines/level_factory.py
  - deep6/engines/zone_registry.py
  - deep6/engines/gex.py
  - .planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/RULES.md
  - tests/engines/test_level.py
  - tests/engines/test_level_bus.py
  - tests/engines/test_level_factory.py
  - tests/engines/test_gex_largest_gamma.py
autonomous: true
requirements: [ZONE-01, ZONE-02, ZONE-03, ZONE-04, ZONE-05, GEX-02]

must_haves:
  truths:
    - "A single `Level` dataclass represents all 17 LevelKind variants (zones and point levels) with unified score + state FSM + stable `uid: int` (uuid4.int) for cross-copy identity keying"
    - "LevelBus (upgraded ZoneRegistry) accepts add_level() for every LevelKind and preserves merge/evict semantics"
    - "LevelFactory.from_{narrative,volume_zone,absorption,exhaustion,gex}() produce valid Level instances without side effects"
    - "GexLevels exposes largest_gamma_strike (new) and zero_gamma (alias to gamma_flip)"
    - "RULES.md lists the deduplicated ~35-40 canonical CR-XX rules, each with trigger, action, source citation, implementability tier"
    - "Existing VolumeZone APIs continue to work as thin wrappers for one release window (no behavior break)"
  artifacts:
    - path: "deep6/engines/level.py"
      provides: "Level dataclass (slots=True), LevelKind (17 variants), LevelState (5 states)"
      min_lines: 120
    - path: "deep6/engines/level_factory.py"
      provides: "Conversion functions from narrative / volume_zone / absorption / exhaustion / gex to Level"
      min_lines: 140
    - path: "deep6/engines/zone_registry.py"
      provides: "LevelBus class with add_level, query_near, query_by_kind, get_top_n, max_levels=80 eviction"
      contains: "class LevelBus"
    - path: ".planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/RULES.md"
      provides: "Deduplicated canonical rule table CR-01..CR-NN with source citations"
      min_lines: 200
    - path: "deep6/engines/gex.py"
      provides: "GexLevels with largest_gamma_strike field + zero_gamma property"
      contains: "largest_gamma_strike"
  key_links:
    - from: "deep6/engines/level_factory.py"
      to: "deep6/engines/level.py Level"
      via: "direct construction"
      pattern: "Level\\("
    - from: "deep6/engines/zone_registry.py LevelBus"
      to: "deep6/engines/level.py Level"
      via: "List[Level] storage + unified queries"
      pattern: "List\\[Level\\]"
---

<objective>
Establish the unified level abstraction for Phase 15. Introduce `Level` / `LevelKind` / `LevelState` (D-01/D-02/D-03/D-04/D-05), upgrade `ZoneRegistry` → `LevelBus` in place (D-09/D-10/D-11), build the stateless `LevelFactory` (D-12), extend `GexLevels` with `largest_gamma_strike` and `zero_gamma` alias (D-28/D-29), and produce the deduplicated `RULES.md` canonical rule table (D-15) that plans 03/05 will encode and test against.

Purpose: Every downstream plan in phase 15 (narrative persistence 15-02, ConfluenceRules 15-03, TradeDecisionMachine 15-04, test suite 15-05) consumes this plan's APIs. Producing the Level dataclass, LevelBus query surface, and deduplicated RULES.md catalog in Wave 1 unblocks parallel work on 15-02 and sequential work on 15-03+.
Output: 4 new Python modules, one modified engine, one canonical RULES.md markdown artifact, unit test suites for dataclass invariants, merge/evict semantics, factory correctness, and GEX field additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-CONTEXT.md

# Research artifacts — source of truth for RULES.md deduplication
@.planning/research/pine/DEEP6_INTEGRATION.md
@.planning/research/pine/industry.md
@.planning/research/pine/deep/microstructure.md
@.planning/research/pine/deep/auction_theory.md
@.planning/research/pine/deep/practitioners.md

# Code surfaces being extended
@deep6/engines/zone_registry.py
@deep6/engines/volume_profile.py
@deep6/engines/gex.py

<interfaces>
From deep6/engines/volume_profile.py:
```python
class ZoneState(Enum):
    ACTIVE, DEFENDED, BROKEN, FLIPPED, INVALIDATED  # reuse verbatim in LevelState (D-03)

@dataclass
class VolumeZone:
    zone_type: ZoneType     # LVN | HVN
    top_price: float
    bot_price: float
    score: float
    touches: int
    direction: int          # +1 / -1 / 0
    state: ZoneState
    origin_bar: int
```

From deep6/engines/zone_registry.py (existing ZoneRegistry — being upgraded in place, D-09):
```python
class ZoneRegistry:
    GEX_LEVELS = ("call_wall", "put_wall", "gamma_flip", "hvl")
    def add_zone(self, zone: VolumeZone) -> None: ...
    def add_gex_levels(self, levels: GexLevels) -> None: ...
    def get_near_price(self, price: float, ticks: int = 4) -> list[VolumeZone]: ...
    def get_all_active(self, min_score: float = 0.0) -> list[VolumeZone]: ...
    def get_confluence(self, price: float, ticks: int = 4) -> ConfluenceResult: ...
    def clear(self) -> None: ...
    def bulk_load(self, zones: list[VolumeZone]) -> None: ...
```

From deep6/engines/gex.py:36-46 (GexLevels — extend in place, D-28/D-29):
```python
@dataclass
class GexLevels:
    call_wall: float = 0.0
    put_wall: float = 0.0
    gamma_flip: float = 0.0
    hvl: float = 0.0
    regime: GexRegime = GexRegime.NEUTRAL
    net_gex_at_spot: float = 0.0
    timestamp: float = 0.0
    stale: bool = False
    strikes: dict[float, float] = field(default_factory=dict)
    # NEW (D-28): largest_gamma_strike: float = 0.0  — raw peak call gamma × OI BEFORE put netting
    # NEW (D-29): zero_gamma property → returns self.gamma_flip
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>T-15-01-01: Produce deduplicated RULES.md + scaffold Level dataclass + LevelKind/LevelState enums</name>
  <files>.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/RULES.md, deep6/engines/level.py, tests/engines/test_level.py</files>
  <behavior>
    RULES.md:
    - Header lists all 47 source rules from the 4 research streams (CR source: DEEP6_INTEGRATION §Confluence Rules = 8 rules; industry.md §Actionable = 12; microstructure.md MS-01..MS-12 = 12; auction_theory.md §Actionable = 15)
    - Dedup pass collapses overlapping rules (e.g. "absorption at VA extreme" appears in DEEP6_INTEGRATION rule 6 AND auction_theory §Entry patterns — merged to single CR-XX)
    - Output: 35-40 canonical CR-01..CR-NN rules, each row: {rule_id, name, trigger_summary, action_summary, source_citations[], tier ∈ {EASY, MEDIUM, CALIBRATION-GATED}}
    - Calibration-gated rules (research-stated LOW confidence thresholds) tagged tier=CALIBRATION-GATED with default=OFF (D-16). Examples: Rule 8 (Baltussen last-30-min), MS-06 (VPIN regime threshold)
    - Footer: dedup audit trail — "CR-XX ← merged from [DEEP6-06, AUCT-03]" lineage table

    Level dataclass tests:
    - test_level_slots: `Level.__slots__` defined; attribute assignment to unknown name raises AttributeError
    - test_point_level_geometry: GEX kind sets price_top == price_bot; single containment check `bot <= p <= top` succeeds
    - test_zone_level_geometry: ABSORB kind uses top > bot; minimum width = 1 tick enforced
    - test_level_kind_count: LevelKind has exactly 17 members matching D-02 list
    - test_level_state_matches_zone_state: every ZoneState name has identical LevelState member (D-03)
    - test_origin_ts_and_origin_bar_both_set: Level(... origin_ts=t, origin_bar=b) preserves both (D-04)
    - test_meta_is_sparse_dict: Level.meta defaults to {} and accepts arbitrary keys (D-05)
    - test_confidence_cache: Level.confidence == Level.score / 100.0 (derived, no separate storage needed)
    - test_level_uid_unique: 100 Level() instances produce 100 distinct uids; uid survives dataclasses.replace() if explicitly copied (document: replace() generates fresh uid, callers wanting stable identity must copy uid explicitly)
  </behavior>
  <action>
    Write `.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/RULES.md` using research artifacts as the single source. Do NOT invent rules — each canonical CR-XX must cite at least one research source by {file}:{section}. Tier classification per D-15/D-16. Target 35-40 rules after dedup; document the dedup in a "Lineage" appendix.

    Create `deep6/engines/level.py`:
    - `class LevelKind(Enum)` with 17 members per D-02: LVN, HVN, VPOC, VAH, VAL, ABSORB, EXHAUST, MOMENTUM, REJECTION, FLIPPED, CONFIRMED_ABSORB, CALL_WALL, PUT_WALL, GAMMA_FLIP, ZERO_GAMMA, HVL, LARGEST_GAMMA.
    - `class LevelState(Enum)`: ACTIVE, DEFENDED, BROKEN, FLIPPED, INVALIDATED. Match deep6/engines/volume_profile.py:ZoneState verbatim.
    - `@dataclass(slots=True) class Level` with fields per D-01: price_top, price_bot, kind, origin_ts, origin_bar, last_act_bar, score (0-100), touches (int), direction (+1/-1/0), inverted (bool), state (LevelState), meta (dict factory), `uid: int = field(default_factory=lambda: uuid.uuid4().int)` (stable identity for mutation keying across copies — required by 15-03 ConfluenceAnnotations.score_mutations per C5 fix; slots=True is compatible with default_factory).
    - `@property confidence` → `self.score / 100.0` (cache cheap; explicit property clarifies intent).
    - Import `uuid` at module level for uid default_factory. Module docstring citing D-01 through D-05 and the C5 uid-stability rationale.

    Write `tests/engines/test_level.py` (≈120 LOC) with the 8 behavior tests above. Use synthetic construction only — no VolumeZone dependency yet.
  </action>
  <verify>
    <automated>pytest tests/engines/test_level.py -x -q && test -f .planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/RULES.md && grep -cE '^\| CR-[0-9]+\s*\|' .planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/RULES.md | awk '{ exit ($1 >= 35 && $1 <= 40) ? 0 : 1 }'</automated>
  </verify>
  <done>RULES.md contains 35-40 canonical CR-XX rows with source citations. Level dataclass + LevelKind (17) + LevelState (5) pass all 8 tests.</done>
</task>

<task type="auto" tdd="true">
  <name>T-15-01-02: Upgrade ZoneRegistry → LevelBus in place + add LevelFactory</name>
  <files>deep6/engines/zone_registry.py, deep6/engines/level_factory.py, tests/engines/test_level_bus.py, tests/engines/test_level_factory.py</files>
  <behavior>
    LevelBus (in-place rename of ZoneRegistry at `deep6/engines/zone_registry.py`):
    - `class LevelBus` replaces `class ZoneRegistry` (module path unchanged to minimize import churn)
    - `ZoneRegistry = LevelBus` alias exported at bottom for one-release compat (D-09)
    - `add_level(level: Level) -> None` subsumes add_zone/add_gex_levels (D-10). Dispatches on LevelKind: zone kinds → overlap-merge; point kinds (CALL_WALL, PUT_WALL, GAMMA_FLIP, ZERO_GAMMA, HVL, LARGEST_GAMMA) → dedupe by (kind, price) and replace
    - `add_zone(zone: VolumeZone)` retained as thin wrapper: calls LevelFactory.from_volume_zone(zone) then add_level(...)
    - `add_gex_levels(levels: GexLevels)` retained: calls LevelFactory.from_gex(levels) → iterable of Level → add_level() each
    - `query_near(price, ticks=4) -> list[Level]`: single-predicate `bot_price - thr <= price <= top_price + thr`, state != INVALIDATED
    - `query_by_kind(kind: LevelKind) -> list[Level]`: filter by kind, state != INVALIDATED
    - `get_top_n(n=6) -> list[Level]`: sorted by score desc, excluding INVALIDATED (Pine's max_visible filter, D-10)
    - `max_levels = 80` cap: after add, if len > 80 evict lowest-score ACTIVE first, preserve DEFENDED/FLIPPED until score < evict candidate (D-11)
    - Preserve existing get_near_price / get_all_active / get_confluence / clear / bulk_load unchanged in semantics (forward to new storage). CRITICAL (C3/C5 fix): `get_all_active()` now returns `list[Level]` (not `list[VolumeZone]`). The existing VPContextResult.active_zones typing must be relaxed to `list[Level]` in 15-02/15-03; downstream scorer mutation keys will use `level.uid` (C5) not `id(level)`. Existing VolumeZone consumers of active_zones are migrated to read Level.price_top/price_bot/score/state in the scorer integration (15-03).

    LevelFactory at `deep6/engines/level_factory.py` (new, stateless, D-12):
    - `from_volume_zone(zone: VolumeZone) -> Level`: kind = LVN | HVN from zone.zone_type; state = LevelState[zone.state.name]; copies price/score/touches/direction/origin_bar; origin_ts = time.time() fallback if not provided
    - `from_narrative(result, *, strength_threshold: float = 0.4, bar_index: int, tick_size: float) -> list[Level]`: consumes NarrativeResult-like object; emits ABSORB / EXHAUST / MOMENTUM / REJECTION Levels per signal present with `strength >= strength_threshold` (default 0.4 per D-06; caller may override); `bar_index` sets `origin_bar` + `last_act_bar`; `tick_size` used to enforce 1-tick minimum width. This is the single-source-of-truth signature consumed by 15-02.
    - `from_absorption(signal, bar, *, bar_index: int, tick_size: float) -> Level`: ABSORB kind; geometry per D-07 (UW: top=bar.high, bot=body_top; LW: top=body_bot, bot=bar.low; min width = 1 tick from `tick_size`). `bar` is the FootprintBar carrying high/low/open/close; `bar_index` sets origin_bar.
    - `from_exhaustion(signal, bar, *, bar_index: int, tick_size: float) -> Level`: EXHAUST kind; same wick-based geometry; same kwargs as `from_absorption`.
    - `from_momentum(signal, bar, *, bar_index: int, tick_size: float) -> Level`: MOMENTUM kind; body geometry (top=max(open,close), bot=min(open,close)); 1-tick min width.
    - `from_rejection(signal, bar, *, bar_index: int, tick_size: float) -> Level`: REJECTION kind; body geometry as momentum.
    - `from_gex(levels: GexLevels) -> list[Level]`: emits up to 6 point-Levels for call_wall, put_wall, gamma_flip, hvl, largest_gamma_strike, zero_gamma (alias — produces distinct Level with LevelKind.ZERO_GAMMA so downstream rules can treat them separately if desired)

    Tests (test_level_bus.py ≈ 150 LOC):
    - test_add_level_zone_overlap_merges: two ACTIVE LVN zones with overlap + same direction → single stored Level with touches summed, score = max(a,b) + 5 clipped to 100
    - test_add_level_point_dedupes: two CALL_WALL at same price → stored once
    - test_query_near_includes_zone_and_point: zone containing price AND point within threshold both returned
    - test_query_by_kind_filters_invalidated: INVALIDATED state excluded
    - test_get_top_n_sorted_desc: N highest-score active levels returned in score-desc order
    - test_max_levels_eviction: add 85 zones → len(_levels) == 80 after; evicted are lowest-score ACTIVE
    - test_zoneregistry_alias_still_imports: `from deep6.engines.zone_registry import ZoneRegistry` resolves to LevelBus
    - test_add_zone_wrapper_preserves_behavior: existing VolumeZone path produces identical observable behavior to pre-upgrade registry
    - test_level_uid_stable_across_merge_or_create: add_level() with overlapping zone merges into existing Level; the merged Level retains its ORIGINAL uid (not the incoming one) so downstream mutation keys (score_mutations dict) remain valid. Document: mutation caller snapshots uids BEFORE calling evaluate()/score_bar().

    Tests (test_level_factory.py ≈ 100 LOC):
    - test_from_volume_zone_round_trip: VolumeZone → Level → same price/score/touches/state mapping
    - test_from_absorption_wick_geometry_upper: absorption at high → z_top == bar.high, z_bot == body_top
    - test_from_absorption_wick_geometry_lower: absorption at low → z_top == body_bot, z_bot == bar.low
    - test_from_absorption_min_tick_width: degenerate zero-wick bar → width enforced to 1 tick
    - test_from_gex_emits_six_point_levels: GexLevels with all fields populated → 6 Level instances (CALL_WALL, PUT_WALL, GAMMA_FLIP, HVL, LARGEST_GAMMA, ZERO_GAMMA)
    - test_from_gex_skips_zero_fields: GexLevels with call_wall=0.0 → no CALL_WALL Level emitted
  </behavior>
  <action>
    Refactor `deep6/engines/zone_registry.py` per D-09:
    - Rename class `ZoneRegistry` → `LevelBus`; replace internal `_zones: list[VolumeZone]` and `_gex: dict[str,float]` with unified `_levels: list[Level]`
    - Implement add_level with LevelKind dispatch; point-kind dedup by (kind, price_top); zone-kind merge uses existing _find_overlap/_merge_into logic rewritten for Level
    - Keep existing `add_zone` / `add_gex_levels` / `get_near_price` / `get_all_active` / `get_confluence` method signatures; rewrite bodies to delegate via LevelFactory + new queries. Preserve ZONE-01..05 semantics exactly.
    - Add `query_near`, `query_by_kind`, `get_top_n` (D-10)
    - Add `max_levels = 80` cap + eviction in add_level (D-11). DEFENDED/FLIPPED preserved unless their score falls below evict candidate.
    - Bottom of file: `ZoneRegistry = LevelBus  # D-09 compatibility alias` + `__all__ = ["LevelBus", "ZoneRegistry", "ConfluenceResult"]`

    Create `deep6/engines/level_factory.py` per D-12 spec above. Stateless; no I/O; all functions pure. Import Level, LevelKind, LevelState from deep6.engines.level.

    Write both test files using synthetic constructors. No real VPContextEngine or narrative engine touched here.
  </action>
  <verify>
    <automated>pytest tests/engines/test_level_bus.py tests/engines/test_level_factory.py -x -q && pytest tests/engines/ -x -q</automated>
  </verify>
  <done>LevelBus replaces ZoneRegistry internals; ZoneRegistry alias preserved. LevelFactory converts all 5 input types. All new + existing engine tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>T-15-01-03: Extend GexLevels with largest_gamma_strike + zero_gamma alias; factory consumes both</name>
  <files>deep6/engines/gex.py, deep6/engines/level_factory.py, tests/engines/test_gex_largest_gamma.py</files>
  <behavior>
    - `GexLevels` (at `deep6/engines/gex.py:36`) gains `largest_gamma_strike: float = 0.0` — the strike with peak raw call gamma × OI BEFORE put netting. Distinct from `hvl` (peak |net GEX|). Consumed by confluence Rule 4 (Pin Regime) per D-28
    - `GexLevels` gains `@property zero_gamma` → `return self.gamma_flip` — naming alias only, no new computation (D-29)
    - `GexEngine._compute_gex` at `deep6/engines/gex.py:204-286` populates `largest_gamma_strike`: during the existing loop that tracks `max_call_gex / max_call_strike` (lines 236-240), also track `max_call_raw_strike` using PRE-aggregation gamma × OI (before the put-side subtraction at strike_gex). This is the already-existing `max_call_strike` variable reused into the new field.
    - `LevelFactory.from_gex` emits LARGEST_GAMMA and ZERO_GAMMA kinds when the respective fields are nonzero. Six total point-Levels possible from one GexLevels.

    Tests:
    - test_gex_largest_gamma_populated: synthetic chain where call with 5000 OI × gamma=0.05 at strike 450 AND put with 8000 OI × gamma=0.05 at strike 450 → largest_gamma_strike == 450 (raw call peak), hvl == 450 (net peak also), gamma_flip computed separately
    - test_zero_gamma_alias_returns_gamma_flip: levels.zero_gamma is levels.gamma_flip (same value)
    - test_from_gex_emits_largest_gamma_level: LevelFactory.from_gex(levels with largest_gamma_strike=450.0) yields a Level with kind == LevelKind.LARGEST_GAMMA at price 450.0
    - test_from_gex_emits_zero_gamma_level: same test for ZERO_GAMMA kind
    - test_existing_gex_fields_unchanged: call_wall/put_wall/gamma_flip/hvl paths untouched — existing tests in tests/engines/test_gex.py (if present) still pass
  </behavior>
  <action>
    Edit `deep6/engines/gex.py`:
    - Line 36-46 (GexLevels dataclass): add `largest_gamma_strike: float = 0.0` after `hvl` field
    - Add `@property def zero_gamma(self) -> float: return self.gamma_flip` below existing methods
    - `_compute_gex` (line 204-286): at line ~239 where max_call_gex is tracked, store `max_call_raw_gex = 0.0; max_call_raw_strike = 0.0` updated on call branch BEFORE any put subtraction modifies strike_gex[strike]. At line 276-286 return, add `largest_gamma_strike=max_call_raw_strike`.

    Edit `deep6/engines/level_factory.py`:
    - `from_gex(levels)`: extend emission list. For each of (call_wall, CALL_WALL), (put_wall, PUT_WALL), (gamma_flip, GAMMA_FLIP), (hvl, HVL), (largest_gamma_strike, LARGEST_GAMMA), (zero_gamma, ZERO_GAMMA) produce a point-Level when price > 0.

    Write `tests/engines/test_gex_largest_gamma.py` (≈80 LOC) with 5 tests above. Mock `_fetch_options_chain` to avoid network.
  </action>
  <verify>
    <automated>pytest tests/engines/test_gex_largest_gamma.py tests/engines/test_level_factory.py -x -q && pytest tests/engines/ -x -q</automated>
  </verify>
  <done>GexLevels exposes both new members. LevelFactory emits 6 point-kinds. No regression in prior GEX tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VolumeZone → Level migration | Internal serialization compatibility preserved via alias + wrapper; no on-disk break (EventStore uses row schemas, not pickles, per D-09) |
| GexLevels external field addition | New field defaults to 0.0 — backward-compatible for any consumer reading via dataclasses.asdict |
| LevelBus query API | Internal; returns List[Level] — no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | add_level with malformed Level (top < bot) | mitigate | Add invariant check in Level.__post_init__; test_level_invariants enforces. |
| T-15-01-02 | Denial of Service | Uncapped level growth at 1000+ bars/session | mitigate | max_levels=80 eviction (D-11); stress test adds 200 zones, asserts steady state == 80. |
| T-15-01-03 | Repudiation | RULES.md claims a rule derives from a research source it doesn't | mitigate | Each CR-XX row cites {file}:{section}; grep audit in verify runs. |
</threat_model>

<verification>
- `pytest tests/engines/test_level.py tests/engines/test_level_bus.py tests/engines/test_level_factory.py tests/engines/test_gex_largest_gamma.py -x -q` green
- `pytest tests/engines/ -x -q` full engines suite green (no regression in VolumeZone/ZoneRegistry consumers)
- `grep -c "LevelKind\." deep6/engines/level.py` shows 17 kinds listed
- `grep -n "largest_gamma_strike" deep6/engines/gex.py` shows field in dataclass + assignment in _compute_gex
- `grep -cE '^\| CR-[0-9]+\s*\|' .planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/RULES.md` is between 35 and 40
- `grep -c "ZoneRegistry = LevelBus" deep6/engines/zone_registry.py` == 1 (alias present)
</verification>

<success_criteria>
1. `Level` dataclass + `LevelKind` (17 variants) + `LevelState` (5 states) exist and match D-01/D-02/D-03 exactly
2. `LevelBus` replaces `ZoneRegistry` in place; `ZoneRegistry` alias preserves import compat; zone-merge + GEX dedup + 80-level eviction + three new queries work
3. `LevelFactory` converts narrative / volume_zone / absorption / exhaustion / gex → Level(s) statelessly
4. `GexLevels.largest_gamma_strike` populated by engine; `zero_gamma` alias returns `gamma_flip`
5. `RULES.md` lists 35-40 canonical CR-XX rules with tier classification and source citations
6. No regression in existing VolumeZone / GEX consumers (existing engine tests pass)
</success_criteria>

<output>
After completion, create `.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-01-SUMMARY.md`
</output>
