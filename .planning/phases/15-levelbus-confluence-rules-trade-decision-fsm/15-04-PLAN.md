---
phase: 15-levelbus-confluence-rules-trade-decision-fsm
plan: 04
type: execute
wave: 3
depends_on: [15-01, 15-02, 15-03]
files_modified:
  - deep6/execution/trade_state.py
  - deep6/execution/trade_decision_machine.py
  - deep6/execution/engine.py
  - deep6/execution/position_manager.py
  - deep6/execution/risk_manager.py
  - deep6/state/eventstore_schema.py
  - tests/execution/test_trade_state.py
  - tests/execution/test_trade_decision_machine.py
  - tests/execution/test_engine_delegates_to_fsm.py
autonomous: true
requirements: [EXEC-01, EXEC-03, EXEC-04, EXEC-05]

must_haves:
  truths:
    - "TradeDecisionMachine holds a 7-state FSM with all 11 transitions reachable per trade_logic.md §2"
    - "17 entry triggers from trade_logic.md §3 are each mapped to exactly one of 4 types (IMMEDIATE_MARKET / CONFIRMATION_BAR_MARKET / STOP_AFTER_CONFIRMATION / LIMIT_AT_LEVEL) per D-21"
    - "Confirmation-bar triggers fire on NEXT bar close, not same bar (D-20)"
    - "Stop / target / invalidation / sizing policies encoded per D-23/D-24/D-25/D-26; invalidations I1-I9 verbatim from §6"
    - "PIN regime (ConfluenceAnnotations.regime == 'PIN') blocks WATCHING → ARMED transition (D-27)"
    - "deep6/execution/engine.py becomes a thin delegate — ExecutionEngine.evaluate() calls into TradeDecisionMachine; existing caller contract preserved for one release window (D-18)"
    - "Every state transition persisted to EventStore via new `fsm_transitions` table (D-19)"
    - "D-16/D-17 GEX gates in risk_manager.py preserved"
  artifacts:
    - path: "deep6/execution/trade_state.py"
      provides: "TradeState enum (7), TransitionId enum (T1-T11), EntryTriggerType enum (4), EntryTrigger enum (ET-01..ET-17)"
      min_lines: 140
    - path: "deep6/execution/trade_decision_machine.py"
      provides: "TradeDecisionMachine class with on_bar / on_fill / on_reject; stop/target/invalidation/sizing policies"
      min_lines: 500
    - path: "deep6/state/eventstore_schema.py"
      provides: "fsm_transitions table schema + insert helper"
      contains: "fsm_transitions"
    - path: "deep6/execution/engine.py"
      provides: "ExecutionEngine.evaluate() delegates to TradeDecisionMachine.on_bar; legacy output shape preserved"
      contains: "TradeDecisionMachine"
  key_links:
    - from: "deep6/execution/engine.py evaluate()"
      to: "deep6/execution/trade_decision_machine.py on_bar"
      via: "one-line delegation + adapter mapping existing args to FSM inputs"
      pattern: "TradeDecisionMachine|\\.on_bar\\("
    - from: "deep6/execution/trade_decision_machine.py transition handlers"
      to: "deep6/state/eventstore_schema.py fsm_transitions"
      via: "INSERT on every state change with (from_state, to_state, trigger, bar_ts, bar_index)"
      pattern: "fsm_transitions"
    - from: "deep6/execution/trade_decision_machine.py WATCHING→ARMED guard"
      to: "ConfluenceAnnotations.regime"
      via: "transition refused if regime == 'PIN' per D-27"
      pattern: "PIN|regime"
---

<objective>
Replace the bar-close-only execution flow at `deep6/execution/engine.py:24-206` with a 7-state TradeDecisionMachine (IDLE → WATCHING → ARMED → TRIGGERED → IN_POSITION → MANAGING → EXITING) per D-17/D-18. Implement all 11 transitions from `trade_logic.md §2`, 17 entry triggers from §3 (each mapped to one of 4 trigger types per D-21), and the stop/target/invalidation/sizing policies from §6 (D-23/D-24/D-25/D-26). Pin-regime queueing per D-27, confirmation-bar timing per D-20, confluence-score precedence for simultaneous triggers per D-22, and E10 gating as an optional guard per D-42.

Purpose: Today `engine.py:24-206` evaluates on bar close only, bypassing the confirmation-bar pattern every prop desk uses. This plan adds the FSM layer, threads ConfluenceAnnotations into transition guards, persists every transition to EventStore for post-hoc analysis (D-19), and leaves `engine.py` as a thin delegate so existing callers (PaperTrader / LiveTrader) see unchanged contracts for one release window.
Output: 2 new execution modules; modifications to engine.py / position_manager.py / risk_manager.py; EventStore schema extension with `fsm_transitions` table; tests covering all 11 transitions, confirmation-bar delay, pin-regime blocking, invalidations I1-I9, sizing formula, and legacy-path delegation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-CONTEXT.md
@.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-01-SUMMARY.md
@.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-02-SUMMARY.md
@.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-03-SUMMARY.md

@.planning/research/pine/deep/trade_logic.md
@.planning/research/pine/deep/practitioners.md

@deep6/execution/engine.py
@deep6/execution/position_manager.py
@deep6/execution/risk_manager.py
@deep6/execution/config.py

<interfaces>
From deep6/execution/engine.py:47-58 (existing evaluate signature — PRESERVE for delegation):
```python
def evaluate(
    self,
    result: ScorerResult,
    entry_price: float,
    bar_high: float,
    bar_low: float,
    atr: float,
    tick_size: float = 0.25,
    zone_target: float | None = None,
    e5_prob: float | None = None,
    gex_signal: GexSignal | None = None,
) -> ExecutionDecision:
```

From plan 15-03:
```python
@dataclass
class ConfluenceAnnotations:
    flags: set[str]
    regime: str  # "PIN" | "TREND" | "BALANCE" | "NEUTRAL"
    score_mutations: dict[int, float]
    vetoes: set[str]
    rule_hits: list[tuple[str, str]]
```

From deep6/execution/risk_manager.py (preserve):
- D-16 / D-17 GEX gates (research verbatim) must still apply after FSM emits order intent

From trade_logic.md §2 (transition table — cite by T-id):
T1: IDLE → WATCHING       | any level score ≥ 60 in range
T2: WATCHING → ARMED      | confluence_score ≥ threshold AND regime != PIN
T3: ARMED → TRIGGERED     | entry trigger fires (ET-01..ET-17)
T4: TRIGGERED → IN_POSITION | order filled
T5: TRIGGERED → ARMED     | order rejected, retry within window
T6: TRIGGERED → IDLE      | timeout without fill (cancel)
T7: IN_POSITION → MANAGING | position held past first-profit-target checkpoint
T8: MANAGING → EXITING    | invalidation fires (I1-I9) OR target hit
T9: EXITING → IDLE        | exit order filled
T10: ARMED → IDLE         | confluence drops below threshold OR timeout
T11: WATCHING → IDLE      | all tracked levels invalidated

From trade_logic.md §3 (17 entry triggers):
ET-01..ET-17 — each mapped to {IMMEDIATE_MARKET | CONFIRMATION_BAR_MARKET | STOP_AFTER_CONFIRMATION | LIMIT_AT_LEVEL} (D-21)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>T-15-04-01: TradeState enums + transition table + EntryTrigger taxonomy</name>
  <files>deep6/execution/trade_state.py, tests/execution/test_trade_state.py</files>
  <behavior>
    - `TradeState` enum: IDLE, WATCHING, ARMED, TRIGGERED, IN_POSITION, MANAGING, EXITING (D-17)
    - `TransitionId` enum: T1..T11 (D-17)
    - `EntryTriggerType` enum (D-21): IMMEDIATE_MARKET, CONFIRMATION_BAR_MARKET, STOP_AFTER_CONFIRMATION, LIMIT_AT_LEVEL
    - `EntryTrigger` enum ET-01..ET-17 with a `trigger_type` property returning one of the 4 types (mapping ported verbatim from trade_logic.md §3 — cite each)
    - `TRANSITION_TABLE`: dict[(TradeState, TradeState), TransitionId] — 11 entries matching §2 exactly
    - `ALLOWED_TRANSITIONS`: dict[TradeState, set[TradeState]] — derived
    - Guard functions (pure, side-effect-free): `guard_T2_ready(scorer_result, confluence, min_score) -> bool` (implements D-22 + D-27 pin block + D-42 e10 gate when enabled), `guard_T8_invalidated(position_state, bar, ...) -> tuple[bool, str]` returning (fired, invalidation_id ∈ I1..I9)

    Tests:
    - test_trade_state_members: 7 states, names/order match D-17
    - test_transition_table_completeness: exactly 11 transitions, matches T1..T11 from §2
    - test_entry_trigger_taxonomy: each ET-01..ET-17 has a trigger_type in the 4-member set (D-21); count == 17
    - test_entry_trigger_type_assignment_matches_research: for each ET-XX, assert the trigger_type matches the classification from trade_logic.md §3 (goldens embedded in test from research)
    - test_guard_T2_pin_regime_blocks: confluence.regime == "PIN" → guard_T2_ready returns False even with high score (D-27)
    - test_guard_T2_non_pin_allows: regime == "NEUTRAL" + score above threshold → True
    - test_guard_T2_e10_opposite_blocks_when_enabled: cfg.enable_e10_gating=True, e10_direction opposite signal_direction, e10_confidence=0.80 → False (D-42)
    - test_guard_T2_e10_default_off: cfg.enable_e10_gating=False → e10 ignored; returns True under same conditions
    - test_guard_T8_invalidation_I9_mfe_giveback: position with MFE=10R, current=4.9R → I9 fires (50% threshold D-25/D-41)
  </behavior>
  <action>
    Read `.planning/research/pine/deep/trade_logic.md` §2 (transition table), §3 (17 triggers), §6 (invalidations I1-I9). Port verbatim into `deep6/execution/trade_state.py`.

    Create the file with all enums + TRANSITION_TABLE + guard functions. Include in each guard docstring the D-XX references.

    Write `tests/execution/test_trade_state.py` with the 9 tests above. Fixtures are pure Python objects — no dependency on LevelBus or ScorerResult beyond minimal stubs.
  </action>
  <verify>
    <automated>pytest tests/execution/test_trade_state.py -x -q</automated>
  </verify>
  <done>7 states, 11 transitions, 17 triggers × 4 types, 9 invalidation rules — all encoded and tested. Guard D-27 pin-block + D-42 e10 gate + D-25 I9 MFE give-back covered.</done>
</task>

<task type="auto" tdd="true">
  <name>T-15-04-02: TradeDecisionMachine class + on_bar / on_fill / on_reject + confirmation-bar timing + EventStore persistence</name>
  <files>deep6/execution/trade_decision_machine.py, deep6/state/eventstore_schema.py, tests/execution/test_trade_decision_machine.py</files>
  <behavior>
    - `class TradeDecisionMachine`:
      * `__init__(config, freeze_guard, risk_manager, position_manager, eventstore)`
      * `self._state: TradeState = TradeState.IDLE`
      * `self._pending: list[PendingTrigger]` for CONFIRMATION_BAR_MARKET / STOP_AFTER_CONFIRMATION (one-bar delay per D-20)
      * `on_bar(bar, level_bus, scorer_result, confluence_annotations) -> list[OrderIntent]`
      * `on_fill(fill)` transitions TRIGGERED → IN_POSITION (T4); persists fill to EventStore
      * `on_reject(reject)` transitions TRIGGERED → ARMED (T5) or IDLE (T6 on timeout)

    - on_bar logic (per-state dispatch):
      1. Fire pending confirmation-bar triggers from prior bar: if pending.type == CONFIRMATION_BAR_MARKET → emit IMMEDIATE market order now; STOP_AFTER_CONFIRMATION → emit stop-entry at the level
      2. Evaluate state-specific logic via `STATE_HANDLERS[self._state](self, bar, level_bus, scorer_result, confluence_annotations)` — returns (next_state, order_intents, transition_id_or_None)
      3. If next_state != current: validate via ALLOWED_TRANSITIONS[current]; persist to EventStore; update state
      4. VETO check: if "SPOOF_DETECTED" in confluence_annotations.vetoes AND current state in {WATCHING, ARMED}, force T11/T10 → IDLE and log SPOOF_VETO

    - IDLE handler: query LevelBus.get_top_n(6); if any has score ≥ 60 AND price within 12 ticks of bar.close → transition T1 → WATCHING with watchlist = top-N levels
    - WATCHING handler: re-evaluate watchlist; if level invalidated, prune; if total_score across watched levels crosses confluence threshold AND guard_T2_ready → T2 → ARMED
    - ARMED handler: check each of ET-01..ET-17 in descending priority (D-22: ABSORB > EXHAUST > MOMENTUM > REJECTION). If any fires:
      * trigger_type == IMMEDIATE_MARKET → emit market OrderIntent NOW; transition T3 → TRIGGERED
      * trigger_type == CONFIRMATION_BAR_MARKET → append to self._pending (fires next bar per D-20); state stays ARMED this bar
      * trigger_type == STOP_AFTER_CONFIRMATION → append pending stop; state stays ARMED
      * trigger_type == LIMIT_AT_LEVEL → emit limit order at level price immediately; transition T3 → TRIGGERED
      * If confluence drops → T10 → IDLE
    - TRIGGERED: wait for fill/reject; bar timeout (config.trigger_timeout_bars) → T6 → IDLE
    - IN_POSITION handler: check T7 (price past first-target checkpoint) and T8 invalidations I1-I9
    - MANAGING handler: runner logic per D-24 (trail by OF-exhaustion); exit on opposing ABSORB/EXHAUST; check invalidations
    - EXITING handler: wait for exit fill → T9 → IDLE

    - Stop / target / sizing helpers:
      * `_compute_stop(level, side, atr, tick_size, account_balance)` per D-23: max(beyond_level + 2*tick_size, 2.0 * atr); cap at 1.5% of account (delegates to risk_manager.max_risk)
      * `_compute_target(entry, stop, level_bus, side)` per D-24: primary = nearest opposing VAH/VAL/LVN/GEX wall in profit direction; enforce R-floor = entry ± 1.5 × stop_distance; runner = None (exit logic handles trail)
      * `_compute_size(risk_budget, stop_distance, conviction_mult, regime_mult, recency_mult)` per D-26: floor(risk_budget / stop_distance × conviction_mult × regime_mult × recency_mult × 0.25)

    - EventStore persistence: new table `fsm_transitions` in `deep6/state/eventstore_schema.py`:
      ```sql
      CREATE TABLE fsm_transitions (
          id INTEGER PRIMARY KEY AUTOINCREMENT,
          bar_ts INTEGER NOT NULL,
          bar_index INTEGER NOT NULL,
          from_state TEXT NOT NULL,
          to_state TEXT NOT NULL,
          transition_id TEXT NOT NULL,      -- T1..T11
          trigger TEXT,                     -- ET-XX or invalidation I-XX
          regime TEXT,
          confluence_score REAL,
          payload_json TEXT                 -- level IDs, order intents
      );
      ```
    - Add `insert_fsm_transition(...)` helper to schema module.

    Tests (tests/execution/test_trade_decision_machine.py ≈ 400 LOC):
    - test_fsm_initial_state_IDLE
    - test_T1_idle_to_watching_on_strong_level: LevelBus with one Level(score=70) near price → state advances to WATCHING after on_bar
    - test_T2_watching_to_armed: high confluence + non-PIN regime → ARMED
    - test_T2_blocked_by_pin_regime: regime="PIN" → stays WATCHING (D-27)
    - test_T2_blocked_by_low_score: score < 70 + PIN → blocked (D-27 explicit: "Directional signals with score < 70 suppressed")
    - test_T3_armed_to_triggered_immediate: ET-XX with trigger_type=IMMEDIATE_MARKET → order emitted same bar, state=TRIGGERED
    - test_T3_confirmation_bar_delays_one_bar: ET with CONFIRMATION_BAR_MARKET on bar N → pending on bar N; order emitted on bar N+1 (D-20)
    - test_T3_precedence_absorb_beats_momentum: two ETs fire same bar (ABSORB + MOMENTUM) → ABSORB path taken (D-22)
    - test_T4_triggered_to_in_position_on_fill
    - test_T5_triggered_to_armed_on_reject
    - test_T6_triggered_to_idle_on_timeout: config.trigger_timeout_bars=2; no fill in 2 bars → T6
    - test_T7_in_position_to_managing_at_first_target
    - test_T8_managing_to_exiting_on_I9_mfe_giveback
    - test_T9_exiting_to_idle_on_exit_fill
    - test_T10_armed_to_idle_on_confluence_drop
    - test_T11_watching_to_idle_all_levels_invalidated
    - test_spoof_veto_forces_idle_from_armed
    - test_limit_at_level_cancelled_in_pin_regime_after_3_bars (D-27 explicit)
    - test_stop_computed_per_D23: stop distance = max(level_beyond + 2 ticks, 2.0*ATR); clamped to 1.5% account
    - test_target_computed_per_D24: target picks nearest opposing zone OR R-floor=1.5R whichever farther
    - test_size_computed_per_D26: formula produces expected integer floor across fixtures
    - test_every_transition_persisted: mock eventstore — after a synthetic session produces all 11 transition IDs, assert 11 rows inserted
    - test_risk_manager_d16_d17_gates_preserved: on_bar produces order intent → passes through risk_manager which still enforces D-16/D-17 gates (existing tests green)
  </behavior>
  <action>
    Create `deep6/execution/trade_decision_machine.py`:
    - Import TradeState, TransitionId, EntryTrigger, EntryTriggerType, guards from deep6.execution.trade_state
    - Import ConfluenceAnnotations from deep6.engines.confluence_rules
    - Import LevelBus, Level, LevelKind from deep6.engines.level / zone_registry
    - Class TradeDecisionMachine per spec above
    - Per-state handlers as private methods (_handle_idle, _handle_watching, etc.)
    - Entry-trigger detectors as private methods (_et_01 through _et_17); each inspects level_bus, bar, scorer_result, confluence and returns Optional[EntryTrigger]. Order of evaluation enforces D-22 precedence.
    - Helpers _compute_stop / _compute_target / _compute_size per D-23/D-24/D-26 specs

    Extend `deep6/state/eventstore_schema.py`:
    - Add fsm_transitions DDL to schema-creation sequence (idempotent `CREATE TABLE IF NOT EXISTS`)
    - Add insert helper function

    Update `deep6/execution/position_manager.py`:
    - Extend MANAGING state logic to expose a `current_mfe_R()` method used by guard_T8_invalidated I9 check
    - No other behavioral change

    Update `deep6/execution/risk_manager.py`:
    - Verify D-16/D-17 gates are applied to the OrderIntent produced by TradeDecisionMachine — wrap FSM order output through existing risk_manager.validate() if not already. Grep call sites to confirm.

    Write `tests/execution/test_trade_decision_machine.py` with the 22 tests above. Use a mock EventStore that records inserts to an in-memory list. Use synthetic LevelBus / ScorerResult / ConfluenceAnnotations builders.
  </action>
  <verify>
    <automated>pytest tests/execution/test_trade_decision_machine.py tests/execution/test_trade_state.py -x -q && pytest tests/execution/ -x -q</automated>
  </verify>
  <done>TradeDecisionMachine with all 11 transitions reachable, 17 triggers + 4 types wired, stop/target/size policies encoded, EventStore persistence on every transition. Risk manager gates preserved. 22 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>T-15-04-03: ExecutionEngine.evaluate() delegates to TradeDecisionMachine (D-18)</name>
  <files>deep6/execution/engine.py, tests/execution/test_engine_delegates_to_fsm.py</files>
  <behavior>
    - `ExecutionEngine.__init__` gains `trade_machine: TradeDecisionMachine` (constructed at top-level app boot; injected). Default-construct if None passed for one-release compat path.
    - `ExecutionEngine.evaluate(...)` at L47-206: REPLACE body with adapter that:
      1. Builds `bar` + `level_bus` + `confluence_annotations` from the args (caller already has these upstream; if not, synthesize minimal stubs with level_bus=empty LevelBus() and confluence_annotations=ConfluenceAnnotations()) — annotate this adapter as a one-release compat shim
      2. Calls `order_intents = self.trade_machine.on_bar(bar, level_bus, scorer_result=result, confluence_annotations=annotations)`
      3. Maps the order intents back to legacy `ExecutionDecision(action, side, entry_price, stop_price, target_price, ...)` format — preserves existing CALLER contract
      4. If FSM state in {TRIGGERED, IN_POSITION, MANAGING, EXITING}: return ExecutionDecision(action="ENTER" or "EXIT" with bracket params)
      5. If state in {IDLE, WATCHING, ARMED}: return ExecutionDecision(action="SKIP", reason="FSM not yet armed/triggered")
      6. FreezeGuard check at top preserved (existing L78-82)
      7. D-02 TYPE_B WAIT_CONFIRM gate preserved — if FSM would ENTER but result.tier == TYPE_B, return "WAIT_CONFIRM"

    - Deprecation: add `warnings.warn(DeprecationWarning, "ExecutionEngine.evaluate is a compat shim — callers should migrate to TradeDecisionMachine.on_bar directly by phase 16")` emitted ONCE per process at the first call (use module-level flag).

    Tests (tests/execution/test_engine_delegates_to_fsm.py ≈ 150 LOC):
    - test_evaluate_delegates_to_fsm: mock TradeDecisionMachine.on_bar → assert called with correct args
    - test_legacy_enter_decision_shape: FSM emits order intent → ExecutionDecision.action == "ENTER" with same stop/target/side fields as pre-plan behavior
    - test_legacy_skip_when_fsm_idle: FSM.state == IDLE → SKIP
    - test_type_b_wait_confirm_preserved: result.tier == TYPE_B AND FSM would ENTER → WAIT_CONFIRM (D-02)
    - test_freeze_guard_preserved: FreezeGuard.is_frozen → FROZEN short-circuit before FSM is called
    - test_deprecation_warning_emitted_once: call evaluate twice, assert warning captured exactly once via warnings.catch_warnings
    - test_existing_d05_stop_too_wide_still_applies: stop > 2x ATR → SKIP (D-05; now enforced inside FSM._compute_stop via risk_manager; verify end-to-end)
    - Existing phase-08 ExecutionEngine tests (`tests/execution/test_execution_engine.py` or similar — grep to find) remain green: re-run them to confirm delegation preserves observable behavior for all existing test fixtures
  </behavior>
  <action>
    Read `deep6/execution/engine.py:24-206` in full (already loaded in context). Replace body of `evaluate()` per the adapter spec. Preserve method signature exactly — existing callers (PaperTrader, LiveTrader) see NO change.

    Add the deprecation warning using a module-level `_warned = False` flag.

    Grep for all call sites of ExecutionEngine.evaluate in the repo to confirm no signature churn: `grep -rn "\.evaluate(" deep6/ tests/ | grep -i execution`

    Run the existing execution test suite first (pre-change) to capture baseline; then run after change to confirm delegation preserves behavior.
  </action>
  <verify>
    <automated>pytest tests/execution/ -x -q</automated>
  </verify>
  <done>ExecutionEngine.evaluate delegates to TradeDecisionMachine.on_bar. Legacy caller contract preserved. FreezeGuard + TYPE_B + D-05 gates still fire. Deprecation warning emitted once. Existing execution tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TradeDecisionMachine → Rithmic ORDER_PLANT | OrderIntent is internal; still flows through risk_manager before broker submission |
| EventStore fsm_transitions writes | Local SQLite (per prior phases); no external exposure |
| Legacy ExecutionEngine callers | Contract preserved via adapter; any new field additions are optional/defaulted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering | FSM transitions to invalid state (e.g., IDLE→TRIGGERED skipping WATCHING) | mitigate | ALLOWED_TRANSITIONS validation before every state change; test_fsm_invalid_transition_raises. |
| T-15-04-02 | DoS | Pending-trigger queue growth on stuck WATCHING/ARMED | mitigate | Trigger timeout config (trigger_timeout_bars) forces T6 → IDLE; pending queue cleared on IDLE transition. |
| T-15-04-03 | Repudiation | Trade entered without audit trail for why | mitigate | EventStore fsm_transitions row with full context (from/to/trigger/regime/confluence_score/payload) on every transition. |
| T-15-04-04 | Elevation of Privilege | FSM bypasses risk_manager D-16/D-17 GEX gates | mitigate | Order intents routed through risk_manager.validate() before broker submission; test_risk_manager_gates_preserved asserts. |
| T-15-04-05 | Tampering | Confirmation-bar pending trigger replays after state change | mitigate | Pending list cleared on any transition to IDLE; test_pending_cleared_on_idle. |
</threat_model>

<verification>
- `pytest tests/execution/ -x -q` full green
- `pytest tests/ -x -q` full green (no regression anywhere)
- `grep -n "class TradeDecisionMachine" deep6/execution/trade_decision_machine.py` == 1
- `grep -cE "^def _et_|EntryTrigger\\.ET_" deep6/execution/trade_decision_machine.py deep6/execution/trade_state.py` covers all 17 ET-XX
- `grep -n "fsm_transitions" deep6/state/eventstore_schema.py` shows DDL + insert helper
- `grep -n "TradeDecisionMachine\|on_bar" deep6/execution/engine.py` shows delegation present
- `python -c "from deep6.execution.trade_state import TradeState; assert len(TradeState) == 7"`
- `python -c "from deep6.execution.trade_state import TRANSITION_TABLE; assert len(TRANSITION_TABLE) == 11"`
</verification>

<success_criteria>
1. 7-state FSM with 11 transitions encoded + 17 entry triggers × 4 types mapped exactly per trade_logic.md §2/§3
2. Confirmation-bar timing (D-20), PIN regime blocking (D-27), precedence (D-22), E10 gating (D-42 behind flag) all enforced
3. Stop / target / size policies per D-23/D-24/D-26; invalidations I1-I9 including I9 MFE give-back
4. Every transition persisted to EventStore fsm_transitions
5. deep6/execution/engine.py delegates to FSM; legacy ExecutionDecision contract preserved; FreezeGuard + TYPE_B + D-05 paths retained
6. risk_manager D-16/D-17 gates preserved
7. Deprecation warning emitted once for legacy callers
</success_criteria>

<output>
After completion, create `.planning/phases/15-levelbus-confluence-rules-trade-decision-fsm/15-04-SUMMARY.md`
</output>
