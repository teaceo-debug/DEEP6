---
phase: "11.2"
plan: "04"
subsystem: "dashboard-ui"
tags: ["signal-feed", "tape-scroll", "replay-controls", "error-banner", "terminal-noir", "type-a-animation", "hover-expand"]
dependency_graph:
  requires:
    - "UI-SPEC v2 §1 tokens (globals.css from 11.2-01)"
    - "Asymmetric 3-column layout shell (page.tsx from 11.2-01)"
    - "ConfluencePulse + KronosBar + ZoneList (11.2-02)"
    - "FootprintRenderer rewrite (11.2-03)"
  provides:
    - "SignalFeedRow.tsx — 44px tier-border row, hover-expand 44→88px, TYPE_A arrival animation"
    - "SignalFeed.tsx — 12-row cap, empty state per UI-SPEC §8"
    - "TapeRow.tsx — 18px row with time/price/side/size/marker columns, pulse-in"
    - "TapeScroll.tsx — auto-scroll + userScrolled pill + empty state"
    - "ReplayControls.tsx — 52px strip per UI-SPEC §4.8"
    - "SessionSelector.tsx — dark shadcn Select variant"
    - "ReturnToLivePill.tsx — breathing live pill / outlined replay button"
    - "ErrorBanner.tsx — exact UI-SPEC §8 copy and styling"
    - "animations.ts — signalRowArrival variant exports added"
  affects:
    - "Task 3 (operator visual acceptance checkpoint — not yet executed)"
tech_stack:
  added: []
  patterns:
    - "motion/react useReducedMotion hook — used in all animated components; zero-duration fallback"
    - "motion.div clip-path reveal (320ms cubic-bezier(0.16,1,0.3,1)) for TYPE_A row arrival"
    - "motion.div backgroundColor keyframe for 800ms lime bg flash"
    - "motion.div filter keyframe for 1200ms lime glow decay"
    - "motion.div height animate for 44→88px hover-expand"
    - "motion.div backgroundColor for 300ms T&S row pulse-in"
    - "onMouseEnter/onMouseLeave inline style for TransportBtn hover (avoids Tailwind class conflicts)"
    - "color-mix(in srgb, var(--accent) 40%, transparent) for ErrorBanner border opacity"
key_files:
  created: []
  modified:
    - "dashboard/components/signals/SignalFeedRow.tsx"
    - "dashboard/components/signals/SignalFeed.tsx"
    - "dashboard/components/tape/TapeRow.tsx"
    - "dashboard/components/tape/TapeScroll.tsx"
    - "dashboard/components/replay/ReplayControls.tsx"
    - "dashboard/components/replay/SessionSelector.tsx"
    - "dashboard/components/replay/ReturnToLivePill.tsx"
    - "dashboard/components/common/ErrorBanner.tsx"
    - "dashboard/lib/animations.ts"
decisions:
  - "SignalFeedRow: hover-expand uses motion.div animate={{ height }} (44→88) rather than CSS max-height transition — gives precise control over which sub-content appears and when"
  - "TapeScroll: manual div + onScroll instead of shadcn ScrollArea — ScrollArea's virtualized inner div doesn't expose scrollTop reliably for the userScrolled detection pattern"
  - "ReplayControls: TransportBtn uses inline onMouseEnter/onMouseLeave for hover background — avoids Tailwind class specificity fights with shadcn Button overrides"
  - "ErrorBanner: color-mix() for border-bottom opacity rather than rgba() — keeps the token name in the CSS and works with any CSS custom property value"
  - "ReturnToLivePill: split into two render paths (isLive → motion.div, else → button) rather than one element with conditional animation — clearer intent, no animation state leak between modes"
metrics:
  duration: "12 minutes"
  completed: "2026-04-14T12:34:15Z"
  tasks_completed: 2
  tasks_total: 3
  files_created: 0
  files_modified: 9
---

# Phase 11.2 Plan 04: Signal Feed + Tape + Replay Restyle + ErrorBanner Summary

**One-liner:** SignalFeedRow rewritten with 4px tier-color border, 44→88px hover-expand revealing engine detail, TYPE_A arrival clip-path/flash/glow animation; TapeScroll rewritten with 18px rows, marker column (★/⊟/ⓘ), auto-scroll + new-row pill; ReplayControls restyled to 52px strip with 36×36 transport buttons, dark shadcn selects, and breathing LIVE pill; ErrorBanner copy updated to exact UI-SPEC §8 strings.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | SignalFeedRow rewrite + SignalFeed empty state + animations.ts extension | 8c89ee8 | SignalFeedRow.tsx, SignalFeed.tsx, animations.ts |
| 2 | TapeRow/TapeScroll + ReplayControls/SessionSelector/ReturnToLivePill/ErrorBanner | c0e36f6 | 6 components |

## Tasks Deferred (operator gate)

| # | Task | Reason |
|---|------|--------|
| 3 | Operator visual acceptance (UI-SPEC §10) | checkpoint:human-verify — requires human to run demo_broadcast.py and validate 5 AC criteria |

## Verification Output

```
> dashboard@0.1.0 typecheck
> tsc --noEmit
(clean — no errors)

> dashboard@0.1.0 build
> next build
▲ Next.js 16.2.3 (Turbopack)
✓ Compiled successfully in 1486ms
✓ Generating static pages (4/4) in 190ms

 ✓ store/ringBuffer.test.ts (5 tests)
 ✓ store/tradingStore.test.ts (7 tests)
 ✓ store/replayStore.test.ts (9 tests)
 ✓ lib/lw-charts/FootprintSeries.test.ts (6 tests)
 ✓ hooks/useWebSocket.test.ts (5 tests)
 Test Files  5 passed (5) | Tests  32 passed (32)

git diff --stat dashboard/store dashboard/hooks dashboard/types deep6 tests/api
(empty — zero lines changed on locked files)
```

## Deviations from Plan

None — plan executed exactly as written. All components rewritten per their UI-SPEC sections without modifying any locked files.

## Known Stubs

None introduced by this plan. Existing stubs from prior plans (kronosConfidence, ZoneList POC-only) remain documented in 11.2-02-SUMMARY.md.

## Threat Surface Scan

T-11.2-09 mitigations applied in SignalFeedRow.tsx:
- Feed capped at 12 visible rows (SignalFeed.tsx `.slice(0, 12)`)
- TYPE_A animations auto-cleanup via motion.js built-in lifecycle (no persistent timers beyond the 8s dot pulse, which uses clearTimeout on unmount)
- `useReducedMotion()` short-circuits all animations to instant/disabled

T-11.2-10 mitigations applied in TapeRow.tsx:
- Pulse uses only `backgroundColor` transition (GPU-accelerated composite property)
- 300ms transition duration; no filter or transform on the pulse path
- TapeScroll buffer cap enforced by existing TAPE_CAPACITY=50 in tradingStore

T-11.2-11: ErrorBanner exposes only generic copy — "LINK DOWN. RETRYING…", "STALE — last tick Ns ago" (coarse staleness count only), "SESSION NOT FOUND. SELECT FROM HISTORY." — no IPs, stack traces, or internal identifiers.

No new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED

Files verified present:
- `dashboard/components/signals/SignalFeedRow.tsx` — FOUND
- `dashboard/components/signals/SignalFeed.tsx` — FOUND
- `dashboard/components/tape/TapeRow.tsx` — FOUND
- `dashboard/components/tape/TapeScroll.tsx` — FOUND
- `dashboard/components/replay/ReplayControls.tsx` — FOUND
- `dashboard/components/replay/SessionSelector.tsx` — FOUND
- `dashboard/components/replay/ReturnToLivePill.tsx` — FOUND
- `dashboard/components/common/ErrorBanner.tsx` — FOUND
- `dashboard/lib/animations.ts` — FOUND

Commits verified:
- `8c89ee8` — feat(11.2-04): rewrite SignalFeedRow + SignalFeed empty state
- `c0e36f6` — feat(11.2-04): rewrite TapeRow/TapeScroll + restyle replay components
