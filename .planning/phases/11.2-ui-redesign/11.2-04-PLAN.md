---
phase: 11.2
plan: 04
type: execute
wave: 4
depends_on: [11.2-01, 11.2-02, 11.2-03]
files_modified:
  - dashboard/components/signals/SignalFeedRow.tsx
  - dashboard/components/signals/SignalFeed.tsx
  - dashboard/components/tape/TapeScroll.tsx
  - dashboard/components/tape/TapeRow.tsx
  - dashboard/components/replay/ReplayControls.tsx
  - dashboard/components/replay/SessionSelector.tsx
  - dashboard/components/replay/ReturnToLivePill.tsx
  - dashboard/components/common/ErrorBanner.tsx
autonomous: false
requirements: [APP-01, APP-03, APP-04, APP-06, APP-08]
must_haves:
  truths:
    - "Signal feed rows have 4px tier-color left border and expand to 88px on hover showing engine_agreement/GEX/kronos_bias"
    - "TYPE_A signal arrival triggers 320ms clip-path reveal + 800ms lime flash + 1200ms glow"
    - "T&S rows are 18px tall with monospace columns and marker column (★/⊟/ⓘ/blank)"
    - "Replay strip matches UI-SPEC §4.8 — shadcn dark selectors, lucide icons, LIVE pill with breathing pulse"
    - "Error banners use exact UI-SPEC §8 copy (LINK DOWN. RETRYING…)"
    - "Operator has visually accepted all 5 UI-SPEC §10 acceptance criteria"
  artifacts:
    - path: "dashboard/components/signals/SignalFeedRow.tsx"
      provides: "Rewritten 44px row with hover-expand per §4.3"
    - path: "dashboard/components/tape/TapeScroll.tsx"
      provides: "Rewritten 18px-row tape per §4.4 with auto-scroll pill"
    - path: "dashboard/components/replay/ReplayControls.tsx"
      provides: "Restyled 52px replay strip per §4.8"
    - path: "dashboard/components/common/ErrorBanner.tsx"
      provides: "New copy per §8"
  key_links:
    - from: "dashboard/components/signals/SignalFeedRow.tsx"
      to: "dashboard/store/tradingStore.ts"
      via: "read-only signal selector"
      pattern: "useTradingStore"
    - from: "dashboard/components/replay/ReplayControls.tsx"
      to: "dashboard/hooks/useReplayController.ts"
      via: "existing hook wiring unchanged"
      pattern: "useReplayController"
---

<objective>
Complete the Terminal Noir visual rebuild: rewrite SignalFeedRow (tier border, hover-expand, TYPE_A flash), TapeScroll (18px rows, marker column, pulse-in), restyle replay controls + session selector + return-to-live pill, update ErrorBanner copy. End with an operator visual acceptance checkpoint against UI-SPEC §10.

Purpose: Closes the visual rebuild — the right column and footer match the Terminal Noir language established by the hero column and footprint.
Output: Fully rebuilt visual layer, validated by operator running demo_broadcast and signing off on 5 acceptance criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.2-ui-redesign/UI-SPEC-v2.md
@.planning/phases/11.2-ui-redesign/11.2-01-SUMMARY.md
@.planning/phases/11.2-ui-redesign/11.2-02-SUMMARY.md
@.planning/phases/11.2-ui-redesign/11.2-03-SUMMARY.md
@dashboard/components/signals/SignalFeed.tsx
@dashboard/components/signals/SignalFeedRow.tsx
@dashboard/components/tape/TapeScroll.tsx
@dashboard/components/tape/TapeRow.tsx
@dashboard/components/replay/ReplayControls.tsx
@dashboard/components/replay/SessionSelector.tsx
@dashboard/components/replay/ReturnToLivePill.tsx
@dashboard/components/common/ErrorBanner.tsx
@dashboard/lib/animations.ts
@dashboard/hooks/useReplayController.ts

# LOCKED — DO NOT MODIFY:
# - dashboard/store/tradingStore.ts
# - dashboard/store/replayStore.ts
# - dashboard/hooks/useWebSocket.ts
# - dashboard/hooks/useReplayController.ts
# - dashboard/types/deep6.ts
# - deep6/**
# - tests/api/**

<interfaces>
<!-- Tier colors: see Plan 11.2-02 interfaces block -->
<!-- Signal row age format: +1.2s (<60s), +45s (<60s exact), +3m (≥60s) -->
<!-- T&S marker glyphs: ★ sweep, ⊟ iceberg, ⓘ kronos-flagged, blank otherwise -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SignalFeedRow + SignalFeed arrival animation</name>
  <files>dashboard/components/signals/SignalFeedRow.tsx, dashboard/components/signals/SignalFeed.tsx</files>
  <action>
    **SignalFeedRow.tsx** per UI-SPEC §4.3 — "use client".

    Row structure (default 44px tall, expands to 88px on hover):
    - Container: `position: relative`, `border-left: 4px solid {tierColor}`, `padding: 8px 12px 8px 16px`, `border-bottom: 1px solid --rule`, `background: --surface-1` on hover
    - Top line (14px tall, flex row):
      - Status dot (8×8 circle in tier color). For 8s after arrival (`Date.now() - signal.timestamp < 8000`), apply Motion `animate` pulse (scale 1↔1.2, opacity 0.6↔1.0, 1.2s loop). After 8s, steady.
      - Tier badge `[TYPE_A]` — text in brackets, `text-xs` 600, color = tier color, letter-spaced 0.08em
      - Narrative (`text-sm`, `--text`, truncate with `text-overflow: ellipsis`, `white-space: nowrap`, flex-1)
      - Age (`text-xs`, `--text-dim`, `.tnum`): format `+1.2s` / `+45s` / `+3m` relative to `signal.timestamp` — compute in a `useEffect` + setInterval(1000) so it ticks
    - Bottom line (14px tall):
      - Total score (`text-xs` 600 `.tnum` in tier color)
      - Middle dot `·` in `--text-mute`
      - Agreement string UPPERCASE in `--text-dim` (truncate with ellipsis)
      - Trailing `→` chevron in `--text-mute`

    Hover expand (88px):
    - Use Motion `<motion.div animate={{ height: hover ? 88 : 44 }}>` with reduced-motion fallback (instant)
    - Additional content revealed on hover:
      - engine_agreement %, GEX regime, kronos_bias, category_count
      - Each as a labeled pair: label `text-xs` `--text-dim` `.label-tracked`, value tabular
    - Use `useState` for hover; apply on `onMouseEnter`/`onMouseLeave`

    **TYPE_A arrival animation** (trigger: tier === "TYPE_A" AND within 1200ms of arrival):
    1. Row slides in with clip-path reveal: `clipPath: ["inset(0 100% 0 0)", "inset(0 0 0 0)"]` over 320ms with `cubic-bezier(0.16, 1, 0.3, 1)`
    2. Background flash: `backgroundColor: ["rgba(163,255,0,0.2)", "rgba(163,255,0,0)"]` over 800ms
    3. Lime glow filter on row for 1200ms: `filter: drop-shadow(0 0 8px rgba(163,255,0,0.5))` fading to none
    - All animations use `motion.div`. Respect `prefers-reduced-motion` (all instant).
    - Reuse `typeAFlashKeyframes` from `lib/animations.ts` where appropriate; extend that file with `signalRowArrival` variant if needed.

    Read signal data from existing store selector used by the current SignalFeedRow — do not change the data contract.

    **SignalFeed.tsx** — container. 12 rows visible (newest at top, oldest fades out), infinite scroll. If the existing Phase 11 feed already does this, keep the scroll/sort logic and just pass rows to the new SignalFeedRow. If there's an empty state path, replace its copy with UI-SPEC §8:
    ```
    [ NO SIGNALS ]
    tail -f /dev/orderflow
    ```
    First line `text-sm` `--text-mute`, second line `text-xs` `--text-mute` italic.

    Do NOT modify `tradingStore`.
  </action>
  <verify>
    <automated>cd dashboard && npm run typecheck && npm run build</automated>
  </verify>
  <done>
    - SignalFeedRow: tier border, hover-expand 44→88, TYPE_A arrival animation
    - Age ticks via interval; tabular nums
    - Empty state copy matches §8
    - Store unchanged
    - Build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite TapeScroll + TapeRow + ReplayControls + SessionSelector + ReturnToLivePill + ErrorBanner</name>
  <files>dashboard/components/tape/TapeScroll.tsx, dashboard/components/tape/TapeRow.tsx, dashboard/components/replay/ReplayControls.tsx, dashboard/components/replay/SessionSelector.tsx, dashboard/components/replay/ReturnToLivePill.tsx, dashboard/components/common/ErrorBanner.tsx</files>
  <action>
    **TapeRow.tsx** — 18px row per UI-SPEC §4.4. Props: time, price, side, size, marker ("sweep"|"iceberg"|"kronos"|null).

    Layout (flex row, gap-2, items-center, height 18px, padding 0 8px):
    - Time (`text-xs` `--text-dim` `.tnum`): `HH:MM:SS.mmm`
    - Price (`text-xs` `--text` `.tnum`)
    - Side badge (`text-xs` 500, width 28px center-aligned): color `--ask` or `--bid`
    - Size (`text-xs` `.tnum`): weight 400 if <50, weight 600 `--text` if ≥50
    - Marker column (width 14px, right-aligned): `★` (sweep) / `⊟` (iceberg) / `ⓘ` (kronos) / empty
    - New-row pulse: on mount, `motion.div` `animate={{ backgroundColor: [sideColorAt25Pct, "transparent"] }} transition={{ duration: 0.3 }}`. Reduced-motion: skip.

    **TapeScroll.tsx**:
    - Fixed container, flex-col, auto-scroll to top on new row UNLESS user has scrolled up
    - Track scroll state via `useRef` + `onScroll`: if `scrollTop > threshold`, set `userScrolled=true`
    - When `userScrolled` and new rows arrive, increment a `newCount`; render a "↓ NEW (47)" pill at bottom-right. Pill: `--surface-2` bg, `--text` text, `--rule-bright` border, `text-xs`, clicking scrolls to top and clears count
    - Empty state copy: `// no prints yet` single line `text-xs` `--text-mute` monospace-comment centered
    - Keep existing data source (do not change which store/selector it reads)

    **ReplayControls.tsx** per UI-SPEC §4.8 — restyle only, wiring to `useReplayController` unchanged:
    - Container: height 52px, bg `--surface-1`, border-top `--rule`, flex items-center, gap-3, padding 0 16px
    - Session selector (left): use shadcn `<Select>` wrapped via SessionSelector.tsx (see below)
    - Transport buttons: 36×36, no border, lucide icons (`SkipBack`, `Play`, `Pause`, `SkipForward`), icon stroke-width 1.25 per UI-SPEC §7, color `--text` (active) or `--text-mute` (disabled), `--surface-2` bg on hover. Wire existing handlers (play/pause/prev/next) from the hook.
    - Bar input: `<input type="number">`, 56×36, `--surface-2` bg, `--rule` border, focus border `--lime`, `text-sm` `.tnum` center-aligned. Wire existing onChange that seeks to bar index.
    - Bar position display: `text-sm` `.tnum` `--text-dim` — `142/512` format
    - Speed selector: shadcn `<Select>` matching SessionSelector style — options from existing controller (`0.5x`, `1x`, `2x`, `5x`, whatever the hook already supports)
    - Spacer `flex-1`
    - LIVE pill (right): use the restyled ReturnToLivePill component

    **SessionSelector.tsx**: wrap shadcn `<Select>` with dark variant props:
    - Trigger: `--surface-2` bg, `--rule-bright` border, `text-sm` `--text`, padding 8px 12px, height 36px
    - Content dropdown: `--surface-1` bg, `--rule-bright` border, items `text-sm` `--text-dim`, hover `--surface-2` bg and `--text` color, active `--lime` text
    - Do NOT change the data source or `onChange` contract consumed by parent

    **ReturnToLivePill.tsx**:
    - Live mode: filled pill, `--ask` bg, white text, `text-xs` 600, padding 4px 10px, border-radius 9999px. Breathing pulse: `motion.div animate={{ opacity: [0.7, 1, 0.7] }} transition={{ duration: 1.5, repeat: Infinity, ease: "easeInOut" }}`. Reduced-motion: static opacity 1.
    - Replay mode: outlined button, `--text-dim` border + text, no fill. Click returns to live (existing handler unchanged).
    - Text: `● LIVE` in both states (circle glyph + label)

    **ErrorBanner.tsx** per UI-SPEC §8:
    - Copy by reason: `LINK DOWN. RETRYING…` (reason: disconnected, color `--bid`), `STALE — last tick {N}s ago` (reason: stale, color `--amber`), `SESSION NOT FOUND. SELECT FROM HISTORY.` (reason: replay_404, color `--amber`)
    - Container: bg `--surface-1`, no fill-block, border-bottom 1px in the reason color at 40% opacity, padding 6px 16px, `text-sm`, `.label-tracked`
    - Remove any v1 generic styles
    - Keep current trigger logic (which store fields surface these states) — just replace visual + copy

    Do NOT modify `replayStore`, `useReplayController`, or any hook. Do NOT add new store fields.
  </action>
  <verify>
    <automated>cd dashboard && npm run typecheck && npm run build && (cd dashboard && npm test --silent 2>&1 | tail -20)</automated>
  </verify>
  <done>
    - All six components rewritten per UI-SPEC sections 4.4, 4.8, 8
    - TapeRow pulse-in + auto-scroll-with-pill functional
    - ReplayControls styled per §4.8 with lucide icons and shadcn dark selects
    - ReturnToLivePill breathes in live mode, outlined in replay
    - ErrorBanner copy matches §8 exactly
    - No locked files touched
    - Build + tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator visual acceptance (UI-SPEC §10)</name>
  <files>.planning/phases/11.2-ui-redesign/acceptance-screenshot.png</files>
  <action>Operator runs demo broadcast and dashboard dev server, then validates all 5 UI-SPEC §10 acceptance criteria as described in <how-to-verify>. This is a human-only verification gate — no automation substitute exists for "feels expensive" and "cannot be mistaken for any other product." Save the acceptance screenshot to the path in <files>.</action>
  <what-built>
    Full Terminal Noir visual rebuild across 4 plans:
    - Plan 11.2-01: tokens, atmosphere, layout shell, HeaderStrip
    - Plan 11.2-02: Confluence Pulse hero, Kronos bar, Zone list
    - Plan 11.2-03: Footprint renderer rewrite (volume bars + bloom + markers)
    - Plan 11.2-04: Signal feed + tape + replay restyle + error banner
  </what-built>
  <how-to-verify>
    1. Ensure backend is running or start demo broadcast:
       `cd /Users/teaceo/DEEP6 && python scripts/demo_broadcast.py` (or your equivalent live/demo data path)

    2. Start dashboard dev server:
       `cd /Users/teaceo/DEEP6/dashboard && npm run dev`

    3. Open http://localhost:3000 at a standard desktop width (≥1920×1080 recommended, also verify 1440×900)

    4. Visual acceptance checklist — confirm ALL 5 per UI-SPEC §10:

       [ ] **AC-1: Within 1s, locate the Confluence Pulse and read the current score.**
           The 320×320 meter must be the visual center. Number must be legible immediately.

       [ ] **AC-2: Within 3s, identify any TYPE_A signal fired in the last 10s.**
           Check the pulse arcs (TYPE_A firings show lime-saturated ring segments + flash decay)
           and the top row of the signal feed (lime border + glow residual).

       [ ] **AC-3: Within 5s, identify GEX regime, Kronos bias direction, and connection status.**
           Header strip must surface all three without scanning. Kronos bar confirms direction.

       [ ] **AC-4: Feels "expensive" — not glossy, but "built by people who know what they're doing."**
           Terminal density, saturated neons on true black, atmosphere layers working subliminally.

       [ ] **AC-5: Cannot be mistaken for any other dashboard product.**
           Screenshot (unbranded) should not elicit "what is that?" from a trader.

    5. Additional checks:
       [ ] Screenshot the dashboard at 1920×1080 and save to `.planning/phases/11.2-ui-redesign/acceptance-screenshot.png`
       [ ] Verify `prefers-reduced-motion: reduce` in DevTools → CRTSweep disappears, pulse flash disappears, digit-roll instant
       [ ] Verify JetBrains Mono is the only font in Network tab (no Inter, no Geist)
       [ ] Verify pure black #000000 body background (not #0a0a0f) via computed styles
       [ ] Trigger a disconnect (stop backend briefly) → `LINK DOWN. RETRYING…` banner appears in `--bid`

    6. If any AC fails: document which one, return to that plan's scope, iterate, re-verify.

    7. Post a summary of findings + the screenshot path in your reply.
  </how-to-verify>
  <verify><automated>Human-only gate — no automated check. Operator confirms 5 ACs pass and attaches screenshot.</automated></verify>
  <done>Operator typed "accepted"; acceptance-screenshot.png exists at the documented path; any "rework" feedback has been resolved and re-verified.</done>
  <resume-signal>Type "accepted" with notes, or "rework {AC-#}: {reason}" to request targeted fixes</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS signal/tape events → feed/tape components | Untrusted live inputs rendered with animation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-09 | D (DoS) | SignalFeedRow arrival animations | mitigate | Cap feed at 12 visible rows; animations auto-cleanup after 1200ms; reduced-motion short-circuits |
| T-11.2-10 | D (DoS) | TapeScroll new-row pulse | mitigate | Pulse is 300ms CSS transition per row; use `transform`/`opacity` only (GPU-accelerated); scrollback cap enforced by existing TapeScroll buffer from Phase 11 |
| T-11.2-11 | I (Info Disclosure) | ErrorBanner | accept | Error strings are generic (no IPs, no stack traces); `last tick Ns ago` exposes only a coarse count |
</threat_model>

<verification>
- `npm run typecheck && npm run build && npm test` pass
- Operator checkpoint (Task 3) accepted
- `git diff --stat dashboard/store dashboard/hooks dashboard/types deep6 tests/api` → zero lines changed across all 4 Phase 11.2 plans
</verification>

<success_criteria>
- All UI-SPEC §4 components rewritten
- UI-SPEC §8 state copy verbatim
- UI-SPEC §10 acceptance criteria operator-signed
- Zero changes to locked files across the entire phase
- Dashboard is visually distinctive and instrument-like
</success_criteria>

<output>
After completion, create `.planning/phases/11.2-ui-redesign/11.2-04-SUMMARY.md` including:
- Acceptance screenshot path
- Operator's acceptance note
- List of any deferred polish items (if any)
</output>
