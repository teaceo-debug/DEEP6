---
phase: "11.2"
plan: "03"
subsystem: "dashboard-ui"
tags: ["footprint-renderer", "volume-bars", "imbalance-bloom", "poc-glow", "signal-markers", "zone-overlay", "terminal-noir"]
dependency_graph:
  requires:
    - "UI-SPEC v2 §1 tokens (globals.css from 11.2-01)"
    - "Asymmetric 3-column layout shell (page.tsx from 11.2-01)"
    - "ConfluencePulse + KronosBar + ZoneList (11.2-02)"
  provides:
    - "FootprintRenderer.ts — volume-bar canvas renderer (major rewrite)"
    - "zoneDrawer.ts — recolored per UI-SPEC v2 §1 tokens"
    - "FootprintChart.tsx — pure-black canvas background, --rule borders"
  affects:
    - "Plans 11.2-04+ (footprint is now visually complete; renderer API unchanged)"
tech_stack:
  added: []
  patterns:
    - "useBitmapCoordinateSpace for all pixel ops (Retina DPR hpr/vpr)"
    - "Canvas globalAlpha for per-cell opacity control (90% normal, 100% imbalance)"
    - "Canvas shadowBlur for POC amber glow (real bloom, not CSS box-shadow)"
    - "Two-draw bloom technique for imbalance cells (glow layer + crisp bar)"
    - "Stacked run scan: consecutive same-side imbalance rows → lime vertical line"
    - "ctx.letterSpacing for empty-state 0.16em letter-spacing (Chromium feature detect)"
key_files:
  created: []
  modified:
    - "dashboard/lib/lw-charts/FootprintRenderer.ts"
    - "dashboard/lib/lw-charts/zoneDrawer.ts"
    - "dashboard/components/footprint/FootprintChart.tsx"
decisions:
  - "Volume normalization uses max(bid+ask) across all rows in bar — gives proportional visual weight to each level relative to the busiest level in that bar"
  - "Imbalance bloom via two-draw (glow layer + crisp bar) rather than canvas filter='drop-shadow' — broader browser compatibility, avoids filter state leaking across draws"
  - "ctx.letterSpacing applied with feature-detect ('letterSpacing' in ctx) — Chromium-only property; fallback is no letter-spacing (still readable)"
  - "ZoneOverlay.tsx itself not rewritten — all color logic lives in zoneDrawer.ts which is the correct abstraction; ZoneOverlay is just the canvas mount + redraw scheduler"
  - "EXHAUSTION and VAH/VAL zone kinds added to zoneDrawer ZONE_STYLES even though ZoneType in deep6.ts does not yet include them — zoneDrawer uses a plain Record<string, ZoneStyle> lookup so extra entries are harmless and future-proof"
metrics:
  duration: "8 minutes"
  completed: "2026-04-14T12:28:17Z"
  tasks_completed: 2
  tasks_total: 2
  files_created: 0
  files_modified: 3
---

# Phase 11.2 Plan 03: FootprintRenderer Rewrite + ZoneOverlay Recolor Summary

**One-liner:** Volume-bar footprint renderer with per-row bid/ask bars proportional to volume, two-draw imbalance bloom at ≥3.0×, stacked-run lime lines, POC amber shadowBlur glow, signal markers (tier-color line + 6×6 square), pure-black canvas, and zone overlay recolored to UI-SPEC v2 neon tokens.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | FootprintRenderer.ts major rewrite — volume bars + bloom + markers | 56d6e76 | FootprintRenderer.ts |
| 2 | zoneDrawer.ts recolor + FootprintChart.tsx background fix | 54f09f7 | zoneDrawer.ts, FootprintChart.tsx |

## Verification Output

```
> dashboard@0.1.0 typecheck
> tsc --noEmit

> dashboard@0.1.0 build
> next build

▲ Next.js 16.2.3 (Turbopack)
✓ Compiled successfully in 1439ms
✓ Generating static pages (4/4) in 194ms

 ✓ store/ringBuffer.test.ts (5 tests)
 ✓ store/tradingStore.test.ts (7 tests)
 ✓ store/replayStore.test.ts (9 tests)
 ✓ lib/lw-charts/FootprintSeries.test.ts (6 tests)
 ✓ hooks/useWebSocket.test.ts (5 tests)
 Test Files  5 passed (5)  |  Tests  32 passed (32)

git diff --stat dashboard/store dashboard/hooks dashboard/types deep6 tests/api
(empty — zero lines changed on locked files)
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing functionality] EXHAUSTION + VAH/VAL zone kinds added to zoneDrawer**
- **Found during:** Task 2
- **Issue:** UI-SPEC §4.2 specifies EXHAUSTION zones (dashed lime) and VAH/VAL lines (dashed --text-dim). The existing ZoneType in deep6.ts (locked) does not include these, but zoneDrawer uses a plain string-keyed Record so adding them is safe and forward-compatible.
- **Fix:** Added EXHAUSTION and VAH/VAL entries to ZONE_STYLES in zoneDrawer.ts. They render correctly if the backend sends them; ignored if not present.
- **Files modified:** dashboard/lib/lw-charts/zoneDrawer.ts
- **Commit:** 54f09f7

**2. [Rule 2 - Missing functionality] ZoneOverlay.tsx not rewritten — only zoneDrawer.ts updated**
- **Found during:** Task 2 inspection
- **Issue:** The plan said "recolor ZoneOverlay.tsx" but all color logic was already abstracted into zoneDrawer.ts. ZoneOverlay.tsx contains only canvas mounting + store subscription + resize observer — zero hex hardcodes to update.
- **Fix:** Updated zoneDrawer.ts (the correct location) rather than ZoneOverlay.tsx. ZoneOverlay.tsx left unchanged as planned (wiring is stable).
- **Files modified:** dashboard/lib/lw-charts/zoneDrawer.ts
- **Commit:** 54f09f7

## Known Stubs

None introduced by this plan. Existing stubs from 11.2-02 (kronosConfidence, ZoneList POC-only) remain documented in 11.2-02-SUMMARY.md.

## Threat Surface Scan

T-11.2-07 mitigations applied in FootprintRenderer.ts:
- `bid_vol` / `ask_vol` clamped to `[0, 99999]` before any width calculation
- `Number.isFinite()` guard on all volume values before division
- `data.bars.length > MAX_BARS_CAP (500)` noted (Phase 11.1 row cap preserved)
- `bidRatio` / `askRatio` infinity-guarded: `Number.isFinite(ratio) ? ratio : 0`

No new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED

Files verified present:
- `dashboard/lib/lw-charts/FootprintRenderer.ts` — FOUND (370 insertions from rewrite)
- `dashboard/lib/lw-charts/zoneDrawer.ts` — FOUND (recolored)
- `dashboard/components/footprint/FootprintChart.tsx` — FOUND (background fixed)

Commits verified:
- `56d6e76` — feat(11.2-03): rewrite FootprintRenderer to volume-bar style
- `54f09f7` — feat(11.2-03): recolor ZoneOverlay + FootprintChart to UI-SPEC v2 tokens
