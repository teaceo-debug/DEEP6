---
phase: "11.2"
plan: "01"
subsystem: "dashboard-ui"
tags: ["terminal-noir", "design-tokens", "atmosphere", "layout", "typography", "motion"]
dependency_graph:
  requires: []
  provides:
    - "UI-SPEC v2 §1 color tokens (globals.css)"
    - "Tailwind v4 @theme mappings for all tokens"
    - "JetBrains Mono variable font (layout.tsx)"
    - "Atmosphere layers: Scanlines + Grain + CRTSweep (app root)"
    - "Asymmetric 3-column layout shell (page.tsx)"
    - "Rewritten HeaderStrip (UI-SPEC §4.7)"
  affects:
    - "All Phase 11.2 plans (consume tokens defined here)"
    - "dashboard/app/globals.css (all downstream CSS)"
tech_stack:
  added:
    - "motion ^11.18.2 (MIT) — CRTSweep animation + future Motion usage"
  patterns:
    - "Tailwind v4 CSS-first config via @theme inline block"
    - "filter: drop-shadow() bloom (not box-shadow) for glow utilities"
    - "body::before vignette via radial-gradient"
    - "SVG feTurbulence fractalNoise grain tile at mix-blend-mode: overlay"
key_files:
  created:
    - "dashboard/components/atmosphere/Scanlines.tsx"
    - "dashboard/components/atmosphere/Grain.tsx"
    - "dashboard/components/atmosphere/CRTSweep.tsx"
    - "dashboard/public/grain.svg"
  modified:
    - "dashboard/app/globals.css"
    - "dashboard/app/layout.tsx"
    - "dashboard/app/page.tsx"
    - "dashboard/components/layout/HeaderStrip.tsx"
    - "dashboard/package.json"
decisions:
  - "Tailwind v4 uses CSS-first config (@theme inline) — no tailwind.config.ts exists; color extensions added directly in globals.css"
  - "motion resolved to ^11.18.2 (npm resolved higher patch than plan specified ^11.11.17 — same minor, compatible)"
  - "CRTSweep checks prefers-reduced-motion via window.matchMedia at render time (not useEffect) to avoid flash"
  - "ScoreWidget removed from page.tsx imports; data-slot placeholder added for Plan 11.2-02"
metrics:
  duration: "3 minutes"
  completed: "2026-04-14T12:15:19Z"
  tasks_completed: 3
  tasks_total: 3
  files_created: 4
  files_modified: 5
---

# Phase 11.2 Plan 01: Terminal Noir Foundation Summary

**One-liner:** Pure-black canvas with UI-SPEC v2 §1 tokens, JetBrains Mono variable font, SVG grain + scanline + CRT sweep atmosphere layers, asymmetric 3-column layout shell (flex-1 | 320px hero | 300px right), and rewritten HeaderStrip with price flash, E10, GEX, ET clock, and connection dot.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Rewrite globals.css + layout.tsx (tokens, font, atmosphere mount) | da0ef13 | globals.css, layout.tsx, package.json, package-lock.json |
| 2 | Create atmosphere components (Scanlines, Grain, CRTSweep) | da0ef13 | Scanlines.tsx, Grain.tsx, CRTSweep.tsx, grain.svg |
| 3 | Rewrite HeaderStrip + asymmetric page.tsx shell | da48e26 | HeaderStrip.tsx, page.tsx |

Tasks 1 and 2 were committed together since layout.tsx imports the atmosphere components created in Task 2 — they form one coherent compilation unit.

## Verification Output

```
> dashboard@0.1.0 typecheck
> tsc --noEmit

> dashboard@0.1.0 build
> next build

▲ Next.js 16.2.3 (Turbopack)
✓ Compiled successfully in 1268ms
✓ Generating static pages (4/4) in 189ms
```

`git diff --stat dashboard/store dashboard/hooks dashboard/types deep6 tests/api` → zero lines changed.

## Deviations from Plan

### Auto-fixed Issues

None — plan executed exactly as written with one configuration clarification:

**1. [Rule 3 - Clarification] tailwind.config.ts does not exist (Tailwind v4 CSS-first)**
- **Found during:** Task 1
- **Issue:** Plan referenced `dashboard/tailwind.config.ts` for color extensions. Tailwind v4 uses CSS-first configuration — there is no `tailwind.config.ts` in this project.
- **Fix:** Color token extensions added to `globals.css` via `@theme inline { --color-* }` block, which is the correct Tailwind v4 mechanism. Result is identical: `bg-void`, `text-lime`, `border-rule` etc. all work as utility classes.
- **Files modified:** `dashboard/app/globals.css` only

## Known Stubs

| Stub | File | Line | Reason |
|------|------|------|--------|
| `data-slot="confluence-pulse"` empty div | `dashboard/app/page.tsx` | hero aside | Placeholder for ConfluencePulse component — wired in Plan 11.2-02 |
| `data-slot="kronos-bar"` empty div | `dashboard/app/page.tsx` | hero aside | Placeholder for KronosBar component — wired in Plan 11.2-02 |
| `data-slot="zone-list"` empty div | `dashboard/app/page.tsx` | hero aside | Placeholder for ZoneList component — wired in Plan 11.2-02 |

These stubs are intentional. The plan's goal (foundation shell) is fully achieved; the hero column slots are scaffolding for Plan 11.2-02.

## Threat Surface Scan

| Flag | File | Description |
|------|------|-------------|
| No new surface | — | All changes are pure visual/CSS layer. No new network endpoints, auth paths, file access, or schema changes introduced. |

T-11.2-01 mitigated: motion ^11.18.2 installed, MIT license confirmed, `npm audit` returned 0 high/0 critical.

## Self-Check: PASSED

Files verified:
- `dashboard/app/globals.css` — FOUND, contains `--void: #000000`
- `dashboard/app/layout.tsx` — FOUND, JetBrains_Mono only, no Inter
- `dashboard/components/atmosphere/Scanlines.tsx` — FOUND
- `dashboard/components/atmosphere/Grain.tsx` — FOUND
- `dashboard/components/atmosphere/CRTSweep.tsx` — FOUND
- `dashboard/public/grain.svg` — FOUND
- `dashboard/components/layout/HeaderStrip.tsx` — FOUND, rewritten
- `dashboard/app/page.tsx` — FOUND, asymmetric layout

Commits verified:
- `da0ef13` — feat(11.2-01): Terminal Noir tokens + atmosphere layers
- `da48e26` — feat(11.2-01): asymmetric layout shell + rewritten HeaderStrip
