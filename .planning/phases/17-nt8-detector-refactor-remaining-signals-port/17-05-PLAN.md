---
phase: 17-nt8-detector-refactor-remaining-signals-port
plan: 05
type: execute
wave: 5
depends_on: [17-02, 17-03, 17-04]
files_modified:
  - ninjatrader/Custom/AddOns/DEEP6/Detectors/Delta/DeltaDetector.cs
  - ninjatrader/Custom/AddOns/DEEP6/Detectors/Trap/TrapDetector.cs
  - ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/TrespassDetector.cs
  - ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/CounterSpoofDetector.cs
  - ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/IcebergDetector.cs
  - ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/MicroProbDetector.cs
  - ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/VPContextDetector.cs
  - ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/SignalConfigScaffold.cs
  - ninjatrader/Custom/AddOns/DEEP6/Registry/SessionContext.cs
  - ninjatrader/Custom/AddOns/DEEP6/Registry/DetectorRegistry.cs
  - ninjatrader/Custom/Indicators/DEEP6/CaptureHarness.cs
  - ninjatrader/tests/fixtures/delta/delt-08-slingshot.json
  - ninjatrader/tests/fixtures/delta/delt-10-cvd-divergence.json
  - ninjatrader/tests/fixtures/trap/trap-05-cvd-trap.json
  - ninjatrader/tests/fixtures/engines/eng-02-trespass.json
  - ninjatrader/tests/fixtures/engines/eng-03-counter-spoof.json
  - ninjatrader/tests/fixtures/engines/eng-04-iceberg.json
  - ninjatrader/tests/fixtures/engines/eng-04-iceberg-with-absorption-zone.json
  - ninjatrader/tests/fixtures/engines/eng-05-micro-prob.json
  - ninjatrader/tests/fixtures/engines/eng-06-vp-context.json
  - ninjatrader/tests/Detectors/DeltaHardTests.cs
  - ninjatrader/tests/Detectors/TrapHardTests.cs
  - ninjatrader/tests/Detectors/EngineDetectorsTests.cs
  - ninjatrader/tests/Parity/PolyfitVsNumpyParityTests.cs
  - ninjatrader/tests/Parity/WassersteinVsScipyParityTests.cs
  - ninjatrader/tests/Parity/SessionReplayParityTests.cs
  - ninjatrader/tests/Parity/fixtures/polyfit-cases.json
  - ninjatrader/tests/Parity/fixtures/wasserstein-cases.json
  - ninjatrader/tests/Parity/CaptureReplayLoader.cs
  - ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs
  - .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-05-PARITY-REPORT.md
autonomous: true
requirements:
  - DELT-08
  - DELT-10
  - TRAP-05
  - ENG-02
  - ENG-03
  - ENG-04
  - ENG-05
  - ENG-06
  - ENG-07

must_haves:
  truths:
    - "DELT-08 slingshot, DELT-10 CVD polyfit divergence, and TRAP-05 CVD trap ported using hand-rolled LeastSquares with bit-for-bit parity vs numpy.polyfit on committed cases"
    - "ENG-02 TrespassDetector implements OnMarketDepth updating double[40] arrays in-place and a weighted-sum logistic approximation"
    - "ENG-03 CounterSpoofDetector uses hand-rolled Wasserstein1 matching scipy.stats.wasserstein_distance on committed cases (zero-sum guard matches Python)"
    - "ENG-04 IcebergDetector uses Stopwatch-based monotonic clock for <250ms refill detection"
    - "ENG-04 IcebergDetector exposes MarkAbsorptionZone(price, radiusTicks); DetectorRegistry notifies it after AbsorptionDetector signals (per RESEARCH.md §ENG-04 cross-detector wiring)"
    - "ENG-05 MicroProbDetector runs LAST in registration order (depends on ENG-02/ENG-04 outputs via SessionContext)"
    - "ENG-06 VPContextDetector ports POC signals only (LVN lifecycle + GEX integration explicitly deferred to Phase 18)"
    - "ENG-07 SignalConfigScaffold provides threshold-override hooks matching signal_config.py defaults"
    - "NDJSON capture harness writes OnMarketData + OnMarketDepth events to ninjatrader/captures/YYYY-MM-DD-session.ndjson"
    - "Session-replay parity test loads an NDJSON capture, runs both legacy and registry paths, and reports signal-count delta within +/-2 per signal type"
    - "UseNewRegistry default flipped to TRUE only after synthetic-fixture parity PASS + session-replay parity PASS on at least 5 recorded sessions"
    - "All 44 signals fire under the registry path; dotnet test exits 0 with all waves' tests green"
  artifacts:
    - path: "ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/TrespassDetector.cs"
      provides: "ENG-02 multi-level weighted DOM imbalance + logistic approximation"
      contains: "class TrespassDetector : ISignalDetector"
    - path: "ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/CounterSpoofDetector.cs"
      provides: "ENG-03 Wasserstein-1 distribution monitor + large-order cancel detection"
      contains: "class CounterSpoofDetector : ISignalDetector"
    - path: "ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/IcebergDetector.cs"
      provides: "ENG-04 native (trade > DOM) + synthetic (refill <250ms via Stopwatch) iceberg detection"
      contains: "class IcebergDetector : ISignalDetector"
    - path: "ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/MicroProbDetector.cs"
      provides: "ENG-05 Naive Bayes micro-probability (consumes ENG-02/ENG-04 outputs)"
      contains: "class MicroProbDetector : ISignalDetector"
    - path: "ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/VPContextDetector.cs"
      provides: "ENG-06 POC signal detection from SessionContext.PocHistory (LVN + GEX explicitly deferred)"
      contains: "class VPContextDetector : ISignalDetector"
    - path: "ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/SignalConfigScaffold.cs"
      provides: "ENG-07 threshold-override scaffold mirroring signal_config.py defaults"
      contains: "class SignalConfigScaffold"
    - path: "ninjatrader/Custom/Indicators/DEEP6/CaptureHarness.cs"
      provides: "NDJSON capture writer for OnMarketData + OnMarketDepth events"
      contains: "class CaptureHarness"
    - path: "ninjatrader/tests/Parity/CaptureReplayLoader.cs"
      provides: "Loads NDJSON captures and replays through detector pipeline for parity"
      contains: "class CaptureReplayLoader"
    - path: ".planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-05-PARITY-REPORT.md"
      provides: "Final Phase 17 parity sign-off across synthetic fixtures + 5-session replay"
      contains: "PHASE 17 PARITY: PASS"
  key_links:
    - from: "TrespassDetector"
      to: "SessionContext.BidDomLevels/AskDomLevels (double[40])"
      via: "OnMarketDepth updates arrays in-place; OnBar snapshots"
      pattern: "BidDomLevels|AskDomLevels"
    - from: "MicroProbDetector"
      to: "TrespassDetector + IcebergDetector outputs"
      via: "SessionContext.LastTrespassResult + LastIcebergSignals fields populated by upstream detectors"
      pattern: "LastTrespassResult|LastIcebergSignals"
    - from: "DEEP6Strategy.UseNewRegistry (default after Wave 5)"
      to: "DetectorRegistry"
      via: "UseNewRegistry = true flipped in final task after parity PASS"
      pattern: "UseNewRegistry.*=.*true"
---

<objective>
Close out Phase 17: port the 6 HARD-tier signals (DELT-08, DELT-10, TRAP-05, ENG-02, ENG-03, ENG-04, ENG-05) plus ENG-06 POC-only scope and ENG-07 config scaffold. Build the NDJSON capture harness (lives in the NT8 indicator) and the C#-side replay loader (lives in tests). Add two dedicated numeric-parity test suites (polyfit-vs-numpy, Wasserstein-vs-scipy) and a session-replay parity suite consuming at least 5 committed NDJSON captures. Commit the final parity report. Flip DEEP6Strategy.UseNewRegistry default to TRUE only AFTER parity PASS.

Purpose: Complete the port, prove numeric and session-level parity, and promote the registry from opt-in to default. This is the gate between Phase 17 and Phase 18 (scoring port).
Output: 6 HARD signals + 2 ENG scaffold signals, capture harness, replay harness, numeric parity suites, 5-session replay parity report, feature-flag flip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-CONTEXT.md
@.planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-RESEARCH.md
@.planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-02-PARITY-REPORT.md
@deep6/engines/delta.py
@deep6/engines/trap.py
@deep6/engines/trespass.py
@deep6/engines/counter_spoof.py
@deep6/engines/iceberg.py
@deep6/engines/micro_prob.py
@deep6/engines/vp_context_engine.py
@deep6/engines/signal_config.py
@ninjatrader/Custom/AddOns/DEEP6/Math/LeastSquares.cs
@ninjatrader/Custom/AddOns/DEEP6/Math/Wasserstein.cs
@ninjatrader/Custom/AddOns/DEEP6/Registry/SessionContext.cs
@ninjatrader/captures/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port DELT-08 slingshot, DELT-10 CVD polyfit divergence, TRAP-05 CVD trap + numeric polyfit parity suite</name>
  <files>
    ninjatrader/Custom/AddOns/DEEP6/Detectors/Delta/DeltaDetector.cs,
    ninjatrader/Custom/AddOns/DEEP6/Detectors/Trap/TrapDetector.cs,
    ninjatrader/Custom/AddOns/DEEP6/Registry/SessionContext.cs,
    ninjatrader/tests/fixtures/delta/delt-08-slingshot.json,
    ninjatrader/tests/fixtures/delta/delt-10-cvd-divergence.json,
    ninjatrader/tests/fixtures/trap/trap-05-cvd-trap.json,
    ninjatrader/tests/Detectors/DeltaHardTests.cs,
    ninjatrader/tests/Detectors/TrapHardTests.cs,
    ninjatrader/tests/Parity/PolyfitVsNumpyParityTests.cs,
    ninjatrader/tests/Parity/fixtures/polyfit-cases.json
  </files>
  <read_first>
    - deep6/engines/delta.py (lines 263-279 — DELT-08 slingshot; polyfit block for DELT-10)
    - deep6/engines/trap.py (lines 298-349 — TRAP-05 CVD trap)
    - .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-RESEARCH.md (Hard Signal Deep-Dives: DELT-08, DELT-10, TRAP-05; Pitfall 4 polyfit edge cases)
    - ninjatrader/Custom/AddOns/DEEP6/Math/LeastSquares.cs
    - ninjatrader/Custom/AddOns/DEEP6/Detectors/Delta/DeltaDetector.cs
    - ninjatrader/Custom/AddOns/DEEP6/Detectors/Trap/TrapDetector.cs
    - ninjatrader/Custom/AddOns/DEEP6/Registry/SessionContext.cs
  </read_first>
  <action>
    DeltaDetector.cs additions:

      DELT-08 SLINGSHOT (delta.py lines 263-279): rolling 4-bar deltaHistory; count bars where |delta| < totalVol * quiet_ratio (default 0.05) = "compressed" gate; current bar must have |delta| > totalVol * explosive_ratio (default 0.30) = "explosive" gate. If compressed_count >= 3 AND current bar is explosive → fire. Use session.DeltaHistory and a parallel Queue<long> TotalVolHistory added to SessionContext. SignalId="DELT-08", FlagBit=Mask(DELT_08) (bit 28).

      DELT-10 CVD POLYFIT DIVERGENCE (delta.py polyfit block): window 5-20 bars (use 10 as default per signal_config). Call LeastSquares.Fit1(session.CvdHistory slice last 10) and Fit1(session.PriceHistory slice last 10). Fire when:
        priceSlope > 0 AND cvdSlope < -Math.Abs(priceSlope) * divergence_factor (default 0.5) → bearish divergence, Direction=-1
        priceSlope < 0 AND cvdSlope > Math.Abs(priceSlope) * divergence_factor → bullish divergence, Direction=+1
      SignalId="DELT-10", FlagBit=Mask(DELT_10) (bit 30). Strength = Math.Min(Math.Abs(cvdSlope / priceSlope), 1.0).

    TrapDetector.cs addition:

      TRAP-05 CVD TRAP (trap.py lines 298-349): window cvd_trap_lookback (default 10). Compute cvdSlope via LeastSquares.Fit1(session.CvdHistory slice). Fire when Math.Abs(cvdSlope) > min_slope (default 50) AND Math.Sign(bar.BarDelta) != Math.Sign(cvdSlope) (current bar opposes prior CVD trend). SignalId="TRAP-05", FlagBit=Mask(TRAP_05) (bit 41). Direction = Math.Sign(bar.BarDelta).

    SessionContext.cs: ensure Queue<double> PriceHistory, Queue<long> CvdHistory, Queue<long> DeltaHistory, Queue<long> TotalVolHistory all exist with MaxHistory >= 20. ResetSession() clears them.

    Fixtures:
      delt-08-slingshot.json: deltaHistory=[10,15,-8,12] (all |delta| < totalVol*0.05 with totalVolHistory matching) + current bar barDelta=+900 with totalVol=1000 (explosive). Expected DELT-08 direction=+1.
      delt-10-cvd-divergence.json: priceHistory 10 ascending values 20000..20009, cvdHistory 10 descending values 500..-400. Expected DELT-10 direction=-1 (bearish divergence).
      trap-05-cvd-trap.json: cvdHistory 10 ascending values 0..1000 (positive slope 100), current barDelta=-500. Expected TRAP-05 direction=-1.

    Tests:
      DeltaHardTests.cs: [TestCase] per DELT-08 and DELT-10 fixture; add [Test] Delt08_RequiresFourBarDeltaHistory() feeding only 3 bars and asserting no fire.
      TrapHardTests.cs: [TestCase] on TRAP-05 fixture + [Test] Trap05_FiresOnlyWhenCurrentDeltaOpposesPriorCvdSlope().

    PolyfitVsNumpyParityTests.cs + polyfit-cases.json:
      Generate polyfit-cases.json offline via Python (committed as static fixture, not regenerated in CI). Each case:
        { "y": [float, float, ...], "expectedSlope": <numpy.polyfit(x, y, 1)[0]>, "expectedIntercept": <numpy.polyfit(x, y, 1)[1]> }
      Commit at least 10 cases covering:
        - monotonically increasing
        - monotonically decreasing
        - flat
        - sinusoidal
        - noisy linear
        - single outlier
        - n=5 (min window)
        - n=20 (max window)
        - near-degenerate (all x equal — should return slope=0 without crashing per Pitfall 4)
        - floating-point edge: y values on the order of 1e-8
      Test iterates cases, calls LeastSquares.Fit1(y), asserts slope within 1e-9 and intercept within 1e-9 of expected.
  </action>
  <verify>
    <automated>grep -E '"DELT-08"|"DELT-10"' ninjatrader/Custom/AddOns/DEEP6/Detectors/Delta/DeltaDetector.cs && grep -E '"TRAP-05"' ninjatrader/Custom/AddOns/DEEP6/Detectors/Trap/TrapDetector.cs && grep -E "LeastSquares\\.Fit1" ninjatrader/Custom/AddOns/DEEP6/Detectors/Delta/DeltaDetector.cs && grep -E "LeastSquares\\.Fit1" ninjatrader/Custom/AddOns/DEEP6/Detectors/Trap/TrapDetector.cs && python3 -c "import json;assert len(json.load(open('ninjatrader/tests/Parity/fixtures/polyfit-cases.json'))) >= 10" && dotnet test ninjatrader/tests/ninjatrader.tests.csproj --filter "FullyQualifiedName~DeltaHard|FullyQualifiedName~TrapHard|FullyQualifiedName~PolyfitVsNumpy" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - DeltaDetector.cs contains literals "DELT-08", "DELT-10" and at least one `LeastSquares.Fit1` call
    - TrapDetector.cs contains literal "TRAP-05" and at least one `LeastSquares.Fit1` call
    - `ninjatrader/tests/Parity/fixtures/polyfit-cases.json` exists with at least 10 cases
    - `dotnet test --filter "DeltaHard|TrapHard|PolyfitVsNumpy"` exits 0
    - Test output reports at least 12 passing tests (3 hard-signal fixtures + 10 polyfit parity cases)
    - DELT-08, DELT-10, TRAP-05 SignalResults emit a Detail string mentioning "slope" or "polyfit" for diagnostic traceability
  </acceptance_criteria>
  <done>DELT-08, DELT-10, TRAP-05 ported using hand-rolled LeastSquares; numeric parity proven against numpy on 10+ cases.</done>
</task>

<task type="auto">
  <name>Task 2: Port ENG-02 TrespassDetector + ENG-03 CounterSpoofDetector + ENG-04 IcebergDetector with Wasserstein parity + DOM integration</name>
  <files>
    ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/TrespassDetector.cs,
    ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/CounterSpoofDetector.cs,
    ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/IcebergDetector.cs,
    ninjatrader/Custom/AddOns/DEEP6/Registry/SessionContext.cs,
    ninjatrader/Custom/AddOns/DEEP6/Registry/DetectorRegistry.cs,
    ninjatrader/tests/fixtures/engines/eng-02-trespass.json,
    ninjatrader/tests/fixtures/engines/eng-03-counter-spoof.json,
    ninjatrader/tests/fixtures/engines/eng-04-iceberg.json,
    ninjatrader/tests/fixtures/engines/eng-04-iceberg-with-absorption-zone.json,
    ninjatrader/tests/Detectors/EngineDetectorsTests.cs,
    ninjatrader/tests/Parity/WassersteinVsScipyParityTests.cs,
    ninjatrader/tests/Parity/fixtures/wasserstein-cases.json
  </files>
  <read_first>
    - deep6/engines/trespass.py (lines 1-141, esp. 129-131 logistic approximation)
    - deep6/engines/counter_spoof.py (lines 128-143 zero-guard, W1 code)
    - deep6/engines/iceberg.py (lines 1-341 native+synthetic detection, 250ms refill)
    - .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-RESEARCH.md (Hard Signal Deep-Dives: ENG-02, ENG-03, ENG-04; Pitfall 5 W1 all-zero guard)
    - ninjatrader/Custom/AddOns/DEEP6/Math/Wasserstein.cs
    - ninjatrader/Custom/AddOns/DEEP6/Registry/SessionContext.cs (BidDomLevels/AskDomLevels double[40])
    - ninjatrader/Custom/AddOns/DEEP6/Registry/ISignalDetector.cs
    - ninjatrader/Custom/AddOns/DEEP6/Detectors/Absorption/AbsorptionDetector.cs (ENG-04 wiring depends on AbsorptionDetector's SignalResult output shape — IcebergDetector.MarkAbsorptionZone must accept the price/radius derived from an ABS signal)
  </read_first>
  <action>
    Target namespace: NinjaTrader.NinjaScript.AddOns.DEEP6.Detectors.Engines. Pure BCL only.

    Extend ISignalDetector with an optional second interface for DOM-consuming detectors — add a sibling interface in the same file:
        public interface IDepthConsumingDetector : ISignalDetector {
            void OnDepth(SessionContext session, int side, int levelIdx, double price, long size, long? priorSize);
        }
    DetectorRegistry.cs: add method `public void DispatchDepth(SessionContext session, int side, int levelIdx, double price, long size, long? priorSize)` that iterates registered detectors and calls OnDepth on those implementing IDepthConsumingDetector.

    TrespassDetector.cs: class TrespassDetector : IDepthConsumingDetector.
      Config: weights array length 10 (top-of-book weighted: w[0]=1.0, w[1]=0.9, ... w[9]=0.1) per trespass.py, plus trespass_threshold (default 0.65).
      OnDepth: update session.BidDomLevels[levelIdx] or session.AskDomLevels[levelIdx] in place (BCL-only, zero allocation).
      OnBar: compute weighted bid sum = sum(session.BidDomLevels[i] * w[i] for i in 0..9), weighted ask sum similarly, ratio = askSum / Math.Max(bidSum, 1.0), probability = Math.Clamp((ratio - 1.0) * 0.5 + 0.5, 0.0, 1.0) per research §ENG-02 formula. Fire ENG-02 when probability > trespass_threshold (bullish) or probability < (1 - trespass_threshold) (bearish). SignalId="ENG-02", FlagBit=Mask(ENG_02) (bit 52). Direction=+1/-1 based on side.
      Save LastTrespassResult to session (for MicroProbDetector): add field `public double LastTrespassProbability; public int LastTrespassDirection;` to SessionContext.

    CounterSpoofDetector.cs: class CounterSpoofDetector : IDepthConsumingDetector.
      Maintain rolling previous DOM snapshot: `private double[] _prevBid = new double[40]; private double[] _prevAsk = new double[40];` (instance-level, private, not in SessionContext since it's detector-internal).
      OnBar: compute w1Bid = Wasserstein.Distance(session.BidDomLevels, _prevBid); w1Ask = Wasserstein.Distance(session.AskDomLevels, _prevAsk); fire ENG-03 if either exceeds spoof_threshold (default 0.25). Copy current to _prev after. SignalId="ENG-03", FlagBit=Mask(ENG_03) (bit 53). Direction derived from which side had the large cancel (bid-side spike → bearish, ask-side → bullish).

    IcebergDetector.cs: class IcebergDetector : IDepthConsumingDetector.
      Use `System.Diagnostics.Stopwatch` for monotonic clock per RESEARCH.md §ENG-04 "Better approach". Instance field `private long _startTick = Stopwatch.GetTimestamp();` and helper `private double NowMs() => (Stopwatch.GetTimestamp() - _startTick) * 1000.0 / Stopwatch.Frequency;`.
      Track per-price depletion/refill timestamps in `Dictionary<double, (double depletedAtMs, long priorSize)>`.
      OnDepth: when a level's size drops to 0 or below a threshold, record depletion timestamp. When that level refills within 250ms to near-prior size, mark as synthetic iceberg.
      Native iceberg: exposed via SessionContext.LastTradeSize and comparison to visible DOM at that price during OnBar (simplified: if bar has a level with volume > 2x visible DOM size, flag native).
      Populate SessionContext.LastIcebergSignals list for MicroProbDetector.
      SignalId="ENG-04", FlagBit=Mask(ENG_04) (bit 54).

      **ENG-04 cross-detector wiring (per RESEARCH.md §ENG-04):** Add public method to IcebergDetector:
          public void MarkAbsorptionZone(double price, int radiusTicks)
      which records the absorption zone in an internal Dictionary<double, int> _absorptionZones so IcebergDetector can treat refills inside that price +/- (radiusTicks * session.TickSize) window as higher-confidence synthetic icebergs (boost strength or emit an ENG-04 variant with Detail string "in-abs-zone").

      **DetectorRegistry.cs wiring:** In DetectorRegistry.EvaluateBar, after each detector's OnBar() returns SignalResult[], if the detector is AbsorptionDetector AND any emitted SignalResult has SignalId starting with "ABS-", call `_icebergDetector?.MarkAbsorptionZone(signal.Price, 2)` with a 2-tick radius. Registry must hold a typed reference `private IcebergDetector _icebergDetector;` set during Register(). Add a post-detector-notification hook (simple: inspect concrete type of detector during EvaluateBar).

      **SignalResult extension:** Add `public double Price;` field to SignalResult if not already present (AbsorptionDetector must populate it with the triggering bar price — typically bar.PocPrice or the absorption level).

    SessionContext additions: `public double LastTrespassProbability; public int LastTrespassDirection; public List<string> LastIcebergSignals = new();` (cleared at start of each OnBar cycle by SessionContext.BeginBar()).

    Fixtures:
      eng-02-trespass.json: includes "depthEvents" array populating BidDomLevels=[100,90,80,...] and AskDomLevels=[500,400,300,...] (heavy ask-side), expected ENG-02 direction=+1 (bullish pressure).
      eng-03-counter-spoof.json: "priorDepth" + "currentDepth" arrays where ask side has a large cancel (e.g., prior ask[0]=1000, current ask[0]=50). Expected ENG-03 with Direction=-1 (bullish — seller withdrew).
      eng-04-iceberg.json: "depthEvents" with timestamps showing level depletion at t=0 and refill at t=150ms. Expected ENG-04 fires.
      eng-04-iceberg-with-absorption-zone.json: prior absorption event at price 20003.00 (populate session state or simulate an AbsorptionDetector signal feed-in via fixture `"priorAbsorptionZones": [{"price": 20003.00, "radiusTicks": 2}]` — extend fixture schema). "depthEvents" show refill at 20003.25 (inside 2-tick radius) at 150ms — expected ENG-04 fires with Detail string containing "in-abs-zone".

    Extend fixture schema with optional `"depthEvents": [{"tsMs":..., "side":"bid"|"ask", "idx":0..39, "price":..., "size":...}, ...]` replayed through OnDepth before OnBar.

    EngineDetectorsTests.cs: [TestCase] per fixture.

    WassersteinVsScipyParityTests.cs + wasserstein-cases.json:
      Offline generation via Python (committed as static fixtures). Each case:
        { "u": [float], "v": [float], "expected": <scipy.stats.wasserstein_distance(positions=range(n), u_weights=u, v_weights=v)> }
      At least 10 cases: identical distributions (W1=0), shifted, two-peak vs one-peak, all-zero u (guard → 0.0), all-zero v (guard → 0.0), n=5, n=40, normalized weights, non-normalized weights, integer weights, float weights.
      Test iterates, asserts |Wasserstein.Distance(u, v) - expected| < 1e-9.
  </action>
  <verify>
    <automated>grep -l "class TrespassDetector" ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/TrespassDetector.cs && grep -l "class CounterSpoofDetector" ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/CounterSpoofDetector.cs && grep -l "class IcebergDetector" ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/IcebergDetector.cs && grep -E '"ENG-02"' ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/TrespassDetector.cs && grep -E 'Wasserstein\.Distance' ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/CounterSpoofDetector.cs && grep -E 'Stopwatch' ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/IcebergDetector.cs && python3 -c "import json;assert len(json.load(open('ninjatrader/tests/Parity/fixtures/wasserstein-cases.json'))) >= 10" && dotnet test ninjatrader/tests/ninjatrader.tests.csproj --filter "FullyQualifiedName~EngineDetectors|FullyQualifiedName~WassersteinVsScipy" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - All three detector files exist with correct class declarations
    - TrespassDetector.cs contains literal "ENG-02" and implements IDepthConsumingDetector
    - CounterSpoofDetector.cs contains literal "ENG-03" and calls `Wasserstein.Distance`
    - IcebergDetector.cs contains literal "ENG-04" and uses `Stopwatch`
    - DetectorRegistry.cs contains a `DispatchDepth` method
    - IcebergDetector.cs contains `public void MarkAbsorptionZone(double price, int radiusTicks)`
    - DetectorRegistry.cs contains `_icebergDetector?.MarkAbsorptionZone` call in EvaluateBar after AbsorptionDetector's OnBar
    - Fixture `ninjatrader/tests/fixtures/engines/eng-04-iceberg-with-absorption-zone.json` exists covering the absorption-zone case
    - SessionContext.cs contains `LastTrespassProbability` and `LastIcebergSignals`
    - `wasserstein-cases.json` has >= 10 cases and the test suite passes
    - `dotnet test --filter "EngineDetectors|WassersteinVsScipy"` exits 0
    - All fixture files parse as valid JSON
  </acceptance_criteria>
  <done>ENG-02, ENG-03, ENG-04 ported with DOM integration, Stopwatch monotonic clock, and numeric parity vs scipy.</done>
</task>

<task type="auto">
  <name>Task 3: Port ENG-05 MicroProb + ENG-06 VP-Context (POC only) + ENG-07 config scaffold; register all engine detectors with correct ordering</name>
  <files>
    ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/MicroProbDetector.cs,
    ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/VPContextDetector.cs,
    ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/SignalConfigScaffold.cs,
    ninjatrader/tests/fixtures/engines/eng-05-micro-prob.json,
    ninjatrader/tests/fixtures/engines/eng-06-vp-context.json,
    ninjatrader/tests/Detectors/EngineDetectorsTests.cs,
    ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs
  </files>
  <read_first>
    - deep6/engines/micro_prob.py (lines 74-131 — Naive Bayes, hardcoded likelihoods)
    - deep6/engines/vp_context_engine.py (lines 1-305 — identify POC-only scope per RESEARCH.md §ENG-06 — LVN lifecycle + GEX explicitly DEFERRED to Phase 18)
    - deep6/engines/signal_config.py (lines 1-460 — threshold defaults)
    - .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-RESEARCH.md (Hard Signal Deep-Dives: ENG-05, ENG-06; Pitfall 7 ENG-06 scope creep)
    - ninjatrader/Custom/AddOns/DEEP6/Registry/SessionContext.cs (LastTrespass* + LastIcebergSignals from Task 2)
    - ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs
  </read_first>
  <action>
    Target namespace: NinjaTrader.NinjaScript.AddOns.DEEP6.Detectors.Engines. Pure BCL only.

    MicroProbDetector.cs: class MicroProbDetector : ISignalDetector. Name = "MicroProb".
      Config constants per micro_prob.py lines 74-131: bullLikelihood=0.65, bearLikelihood=0.65, prior=0.5.
      OnBar: read session.LastTrespassProbability, session.LastTrespassDirection, session.LastIcebergSignals.Count. Apply Naive Bayes:
        posterior = prior
        if LastTrespassDirection != 0: posterior *= (LastTrespassDirection > 0 ? bullLikelihood : bearLikelihood) / (1 - bullLikelihood)
        if LastIcebergSignals.Count > 0: posterior *= 1.2 (iceberg boost)
        Normalize via sigmoid or clamp to [0,1].
      Fire ENG-05 when posterior > 0.70 (bull) or < 0.30 (bear). SignalId="ENG-05", FlagBit=Mask(ENG_05) (bit 55).
      CRITICAL: MicroProbDetector MUST register AFTER TrespassDetector and IcebergDetector (sequential dispatch in DetectorRegistry ensures it reads their results written to SessionContext within the same bar).

    VPContextDetector.cs: class VPContextDetector : ISignalDetector. Name = "VPContext".
      Scope per RESEARCH.md §ENG-06 Phase 17 scope: POC signal detection from session.PocHistory ONLY. LVN lifecycle FSM: EXPLICITLY DO NOT PORT (Phase 18). GEX integration: EXPLICITLY DO NOT TOUCH (stays in DEEP6Footprint.cs GEX client lines ~769-924).
      Emit ENG-06 when POC migration pattern fires (reuse VOLP-04 detection as contextual enrichment but emit under ENG-06 id with Detail string describing context). Or fire ENG-06 as a "POC context" signal whenever bar.PocPrice is at session VAH/VAL boundary.
      SignalId="ENG-06", FlagBit=Mask(ENG_06) (bit 56).
      Guard comment at top of file: `// Phase 17 scope: POC context only. LVN lifecycle and GEX integration deferred to Phase 18.`

    SignalConfigScaffold.cs: class SignalConfigScaffold. Public constants/properties mirroring signal_config.py defaults for every signal family (thresholds, ratios). Consumers: other detectors reference SignalConfigScaffold.Defaults.XXX via an injection pattern (or simply reference static values directly; the scaffold exists for Wave 18 scorer to override). Emit ENG-07 as a "config-loaded" no-op signal on first bar only (Direction=0, Strength=0.0), with Detail string listing the config version hash. SignalId="ENG-07", FlagBit=Mask(ENG_07) (bit 57).

    Fixtures:
      eng-05-micro-prob.json: pre-set session.LastTrespassProbability=0.85, LastTrespassDirection=+1, LastIcebergSignals=["native-ask-20005"] (extend fixture schema to set these on session). Expected ENG-05 direction=+1.
      eng-06-vp-context.json: pocHistory [20001.00, 20002.00, 20003.00] + current bar with poc at VAH=20003.00. Expected ENG-06 fires.

    EngineDetectorsTests.cs extension: add [TestCase] for ENG-05 and ENG-06 fixtures. Add [Test] MicroProbDetector_IsRegisteredLast() — construct a DetectorRegistry with canonical registration order and assert the last registered is MicroProbDetector.

    DEEP6Strategy.cs update — final registration block inside UseNewRegistry==true State.Configure branch, ORDER IS CRITICAL:
        _registry.Register(new AbsorptionDetector());
        _registry.Register(new ExhaustionDetector());
        _registry.Register(new ImbalanceDetector());
        _registry.Register(new DeltaDetector());
        _registry.Register(new AuctionDetector());
        _registry.Register(new VolPatternDetector());
        _registry.Register(new TrapDetector());
        _registry.Register(new TrespassDetector());     // ENG-02 — depth consumer, writes LastTrespassResult
        _registry.Register(new CounterSpoofDetector()); // ENG-03 — depth consumer
        _registry.Register(new IcebergDetector());      // ENG-04 — depth consumer, writes LastIcebergSignals
        _registry.Register(new VPContextDetector());    // ENG-06
        _registry.Register(new SignalConfigScaffold()); // ENG-07 (if it implements ISignalDetector)
        _registry.Register(new MicroProbDetector());    // ENG-05 — MUST BE LAST (reads LastTrespass + LastIcebergSignals)
      Keep UseNewRegistry = false default for now — the flip to true happens in Task 5 after parity passes.
  </action>
  <verify>
    <automated>grep -l "class MicroProbDetector" ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/MicroProbDetector.cs && grep -l "class VPContextDetector" ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/VPContextDetector.cs && grep -l "class SignalConfigScaffold" ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/SignalConfigScaffold.cs && grep -E "Phase 17 scope.*POC.*only|LVN.*deferred|GEX.*deferred|defer" ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/VPContextDetector.cs && grep -E "LastTrespass|LastIceberg" ninjatrader/Custom/AddOns/DEEP6/Detectors/Engines/MicroProbDetector.cs && awk '/Register\(new (Trespass|CounterSpoof|Iceberg|MicroProb)Detector/{print NR,$0}' ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs && dotnet test ninjatrader/tests/ninjatrader.tests.csproj --filter "FullyQualifiedName~EngineDetectors" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - MicroProbDetector.cs contains `class MicroProbDetector` and references `LastTrespass` or `LastIceberg`
    - VPContextDetector.cs contains a comment mentioning either "deferred" or "Phase 18" regarding LVN/GEX scope
    - SignalConfigScaffold.cs contains `class SignalConfigScaffold`
    - DEEP6Strategy.cs contains `Register(new MicroProbDetector` and it appears on a line number GREATER than the lines containing `Register(new TrespassDetector` AND `Register(new IcebergDetector` (enforced by the awk check + manual review in SUMMARY.md)
    - DEEP6Strategy.cs `UseNewRegistry` default is still `false` (flip happens in Task 5)
    - `dotnet test --filter "EngineDetectors"` exits 0
    - Test output shows MicroProbDetector_IsRegisteredLast passing
  </acceptance_criteria>
  <done>ENG-05, ENG-06 (POC-only), ENG-07 ported; registration order enforces MicroProbDetector runs last. Flag still default false pending parity.</done>
</task>

<task type="auto">
  <name>Task 4: Capture harness, 5-session parity sign-off, flag flip, and legacy Obsolete marking</name>
  <files>
    ninjatrader/Custom/Indicators/DEEP6/CaptureHarness.cs,
    ninjatrader/captures/README.md,
    ninjatrader/tests/Parity/CaptureReplayLoader.cs,
    ninjatrader/tests/Parity/SessionReplayParityTests.cs,
    .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-05-PARITY-REPORT.md,
    ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs,
    ninjatrader/Custom/Indicators/DEEP6/DEEP6Footprint.cs
  </files>
  <read_first>
    - .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-CONTEXT.md (D-06 feature flag; D-09 parity pass bar ±2 signals/type/session tolerance; D-10 capture harness)
    - .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-RESEARCH.md (§Pattern 6 NDJSON Capture Harness)
    - .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-02-PARITY-REPORT.md (Wave 2 format to extend)
    - ninjatrader/captures/README.md (Wave 2 schema doc)
    - ninjatrader/Custom/Indicators/DEEP6/DEEP6Footprint.cs (OnMarketData + OnMarketDepth hook points; legacy ABS/EXH methods to [Obsolete]; GEX code at ~769-924 must NOT be touched)
    - ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs (UseNewRegistry default currently false)
    - ninjatrader/Custom/AddOns/DEEP6/Registry/DetectorRegistry.cs
  </read_first>
  <action>
    This merged task delivers: (a) NDJSON capture harness, (b) replay loader, (c) 5-session parity suite run, (d) final parity report, (e) flip UseNewRegistry default to true, (f) mark legacy ABS/EXH methods [Obsolete]. Execute in this order so the report reflects test results before the flip is committed.

    **(a) CaptureHarness.cs** (NT8-facing — this one file CAN reference NT8 types; lives under Indicators folder, NOT AddOns, to keep AddOns test-compatible).
      Namespace: NinjaTrader.NinjaScript.Indicators.DEEP6.
      Public class `CaptureHarness` with constructor taking a session date and opening `ninjatrader/captures/YYYY-MM-DD-session.ndjson` via `new StreamWriter(path, append:true)` with auto-flush.
      Methods:
        WriteDepth(DateTime tsUtc, int side, int idx, double price, long size) — writes one NDJSON line {"type":"depth","ts_ms":..., "side":0|1, "idx":..., "price":..., "size":...}
        WriteTrade(DateTime tsUtc, double price, long size, int aggressor) — writes {"type":"trade","ts_ms":..., "price":..., "size":..., "aggressor":0|1|2}
        Close() — flush + dispose
      Use manual string.Format JSON construction (keep output format explicit).
      Hook: document in captures/README.md how to wire into DEEP6Footprint.cs OnMarketData/OnMarketDepth guarded by a new `EnableCapture` bool property — DO NOT wire live this phase; Phase 19 operators toggle during live capture.

    **captures/README.md** extension: document wire-up instructions; verify schema matches Wave 2's schema.

    **(b) CaptureReplayLoader.cs** (net8.0 test side):
      Namespace: NinjaTrader.Tests.Parity.
      public class CaptureReplayLoader {
        public void Replay(string ndjsonPath, Action<string, long, double, long, int?, int?, int?> onEvent);
      }
      Parse NDJSON line-by-line via System.Text.Json.JsonDocument. Emit callbacks preserving event order by ts_ms.
      Helper `public List<SignalResult> RunThroughRegistry(string ndjsonPath, DetectorRegistry registry, SessionContext session)` — replays capture, calls registry.DispatchDepth on depth events, accumulates a FootprintBar from trade events, calls registry.EvaluateBar at synthetic bar-close boundaries (every 60s or explicit "bar_close" marker if present).

    **(c) SessionReplayParityTests.cs**:
      [Test] public void FiveRecordedSessions_RegistryPathMatchesLegacy_WithinTolerance()
      - For committed NDJSON captures under `ninjatrader/tests/fixtures/sessions/*.ndjson` (require at least 5).
      - Per capture: run through both the legacy static ABS/EXH path (via LegacyDetectorsBridge from Wave 2) AND the full registry path; aggregate signal counts by SignalId; assert for each of the 44 SignalIds that |legacyCount - registryCount| <= 2 (per CONTEXT.md D-09).
      - Emit per-session delta table into 17-05-PARITY-REPORT.md.

    **Minimum 5 sessions:** If 5 live captures are not yet available, commit 5 synthetic NDJSON captures generated from Python reference engine test fixtures covering trending up, trending down, balanced, news-driven, low-volume regimes. Place at `ninjatrader/tests/fixtures/sessions/session-01-trending-up.ndjson` through `session-05-low-vol.ndjson`. Note in the report that these are synthetic; live captures replace them as they arrive.

    **(d) 17-05-PARITY-REPORT.md** (fill with actual values after full `dotnet test`):
      ```
      # Phase 17 — Final Parity Report
      Date: YYYY-MM-DD

      ## Synthetic Fixture Parity
      PARITY: PASS | FAIL
      Fixtures tested: 44 signals × at least 1 fixture each = 44+
      Polyfit cases: 10 passing within 1e-9 of numpy.polyfit
      Wasserstein cases: 10 passing within 1e-9 of scipy.stats.wasserstein_distance

      ## Session Replay Parity
      PARITY: PASS | FAIL
      Sessions: 5 (list each with filename + regime label)
      Per-signal delta table: SignalId | LegacyCount | RegistryCount | |Δ| | Within tolerance (≤2)? Yes/No
      Max |Δ| across all 44 signals × 5 sessions: <n>
      Tolerance: ±2 per signal type per session (CONTEXT.md D-09)

      ## Python Reference Bugs Found + Fixed
      <list each with: bug description, file, line, fix committed in plan SUMMARY>

      ## Overall: PHASE 17 PARITY: PASS | FAIL
      Gate: UseNewRegistry default may flip to TRUE | must remain FALSE until delta resolved
      ```

    **(e) DEEP6Strategy.cs change** — ONLY commit this flip if the parity report shows `Overall: PHASE 17 PARITY: PASS` with all synthetic + all 5 session replays passing:
      Change `public bool UseNewRegistry { get; set; } = false;` to `public bool UseNewRegistry { get; set; } = true;`.
      If ANY failure: leave default false, STOP, document the blocker in the report with FAIL status, do NOT proceed. Phase 17 cannot close without PASS.

    **(f) DEEP6Footprint.cs change**:
      Mark the static legacy ABS/EXH methods with `[Obsolete("Use AbsorptionDetector/ExhaustionDetector via DetectorRegistry (Phase 17). Will be removed in Phase 18.")]`. Do NOT delete them — Phase 18 removes after scoring port stabilizes.
      Do NOT touch GEX client code (lines ~769-924).
      Do NOT touch risk-gate-adjacent code.

    Final verification: full `dotnet test` run — all waves green. Count passing tests and record in report (expect >70 passing tests across all 5 waves combined).
  </action>
  <verify>
    <automated>grep -l "class CaptureHarness" ninjatrader/Custom/Indicators/DEEP6/CaptureHarness.cs && grep -l "class CaptureReplayLoader" ninjatrader/tests/Parity/CaptureReplayLoader.cs && grep -l "class SessionReplayParityTests" ninjatrader/tests/Parity/SessionReplayParityTests.cs && ls ninjatrader/tests/fixtures/sessions/*.ndjson 2>/dev/null | wc -l | awk '{if ($1 < 5) exit 1}' && grep -E "PHASE 17 PARITY: PASS" .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-05-PARITY-REPORT.md && grep -E "UseNewRegistry.*=.*true" ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs && grep -E "\[Obsolete" ninjatrader/Custom/Indicators/DEEP6/DEEP6Footprint.cs && dotnet test ninjatrader/tests/ninjatrader.tests.csproj --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `ninjatrader/Custom/Indicators/DEEP6/CaptureHarness.cs` exists with `class CaptureHarness` and WriteDepth + WriteTrade methods
    - `ninjatrader/tests/Parity/CaptureReplayLoader.cs` exists with `class CaptureReplayLoader`
    - `ninjatrader/tests/Parity/SessionReplayParityTests.cs` exists
    - At least 5 NDJSON files under `ninjatrader/tests/fixtures/sessions/` (live or synthetic)
    - `captures/README.md` documents EnableCapture property wire-up pattern for Phase 19 operators
    - `17-05-PARITY-REPORT.md` exists and contains the exact string `PHASE 17 PARITY: PASS`
    - Report enumerates all 5 session fixture filenames
    - Report contains the per-signal delta table with a maximum |Δ| value reported
    - DEEP6Strategy.cs `UseNewRegistry` default is now `true` (grep matches `UseNewRegistry.*=.*true`)
    - DEEP6Footprint.cs contains at least one `[Obsolete` attribute marking legacy ABS/EXH methods
    - DEEP6Footprint.cs still contains GEX code (`MassiveGexClient` identifier) — grep matches (GEX client NOT touched)
    - DEEP6Strategy.cs still contains `AccountWhitelist` or `IsAccountAllowed` (risk gate untouched)
    - Full `dotnet test` exits 0 — all waves green (>70 passing tests)
  </acceptance_criteria>
  <done>Phase 17 closed: NDJSON capture harness built, 5-session parity proven within ±2 tolerance, UseNewRegistry default flipped to TRUE, legacy ABS/EXH methods marked [Obsolete], risk-gate + GEX code untouched. Phase 18 (scoring port) unblocked.</done>
</task>

</tasks>

<verification>
- All 44 signals have at least one committed fixture and one passing NUnit test
- LeastSquares matches numpy.polyfit within 1e-9 on 10+ cases
- Wasserstein matches scipy.stats.wasserstein_distance within 1e-9 on 10+ cases (including zero-sum guard)
- 5 session NDJSON captures replay through both legacy and registry paths with |Δ| ≤ 2 per signal per session
- MicroProbDetector registered LAST (depth-consuming detectors + ENG-06 must run first)
- UseNewRegistry default=true in DEEP6Strategy
- Legacy ABS/EXH code in DEEP6Footprint.cs marked [Obsolete] but retained (deletion is Phase 18 scope)
- GEX client code untouched
- Risk-gate code (account whitelist, news blackout, RTH window, daily loss cap) untouched across all 5 waves
- Full `dotnet test` exits 0
- 17-05-PARITY-REPORT.md signed off with PHASE 17 PARITY: PASS
</verification>

<success_criteria>
- 44 of 44 signals migrated (10) or ported (34) under the new DetectorRegistry
- All 5 ROADMAP Phase 17 success criteria met:
  1. DEEP6Footprint.cs split into modular files under AddOns/DEEP6/Detectors/{Family}/; no single file > 2000 LOC
  2. ISignalDetector + DetectorRegistry replaces hardcoded ABS+EXH routing
  3. All 34 ported signals fire bar-for-bar matching Python within ±2 tolerance on 5-session replay
  4. Per-family unit test fixtures committed under ninjatrader/tests/
  5. No regression in the 10 Phase 16 signals or GEX overlay
- Phase 18 (scoring port) cleared to start
</success_criteria>

<output>
After completion, create `.planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-05-SUMMARY.md` documenting:
- Signals ported this wave (9 IDs + config scaffold)
- Capture harness + replay loader architecture
- Numeric parity test counts (polyfit + wasserstein cases)
- Session replay parity table (per-signal |Δ| across 5 sessions)
- Python reference bugs found + fixed across Phase 17 total
- UseNewRegistry flip confirmed + Obsolete annotations added
- Risk-gate + GEX code untouched confirmation (diff stats)
- Next phase hand-off: Phase 18 scoring port unblocked, expected entry criteria
</output>
