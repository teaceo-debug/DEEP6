---
phase: 17-nt8-detector-refactor-remaining-signals-port
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - ninjatrader/tests/fixtures/absorption/abs-02-passive.json
  - ninjatrader/tests/fixtures/absorption/abs-03-stopping.json
  - ninjatrader/tests/fixtures/absorption/abs-04-effort-vs-result.json
  - ninjatrader/tests/fixtures/absorption/abs-07-va-extreme.json
  - ninjatrader/tests/fixtures/exhaustion/exh-02-exhaustion-print.json
  - ninjatrader/tests/fixtures/exhaustion/exh-03-thin-print.json
  - ninjatrader/tests/fixtures/exhaustion/exh-04-fat-print.json
  - ninjatrader/tests/fixtures/exhaustion/exh-05-fading-momentum.json
  - ninjatrader/tests/fixtures/exhaustion/exh-06-bid-ask-fade.json
  - ninjatrader/tests/Detectors/AbsorptionParityTests.cs
  - ninjatrader/tests/Detectors/ExhaustionParityTests.cs
  - ninjatrader/tests/Parity/LegacyVsRegistryParityTests.cs
  - ninjatrader/captures/README.md
  - ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs
  - .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-02-PARITY-REPORT.md
autonomous: true
requirements: []  # Gate wave — parity-only; no new signal IDs. ENG-07 (SignalConfigScaffold) is implemented in 17-05.
must_haves:
  truths:
    - "All 10 legacy signals (ABS-01..04, ABS-07, EXH-01..06) have committed fixture files with Python-derived expected values"
    - "Registry-path AbsorptionDetector + ExhaustionDetector produce bit-for-bit identical SignalResult output to the legacy DEEP6Footprint.cs code path on every fixture"
    - "DEEP6Strategy wired so UseNewRegistry=true routes ABS/EXH evaluation through DetectorRegistry; UseNewRegistry=false retains legacy path as default"
    - "A parity test suite compares legacy static-call output vs registry output on 10+ synthetic bars and asserts equality (SignalId, Direction, Strength within 1e-4, FlagBit)"
    - "A parity report is committed documenting zero divergences — Wave 2 MUST BLOCK Wave 3+ if any divergence exists"
  artifacts:
    - path: "ninjatrader/tests/fixtures/absorption/"
      provides: "4 absorption fixtures (abs-01..04) + abs-07 VA extreme bonus"
    - path: "ninjatrader/tests/fixtures/exhaustion/"
      provides: "6 exhaustion fixtures (exh-01..06)"
    - path: "ninjatrader/tests/Parity/LegacyVsRegistryParityTests.cs"
      provides: "Parity suite calling legacy static methods vs registry-path on identical synthetic bars"
      contains: "class LegacyVsRegistryParityTests"
    - path: ".planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-02-PARITY-REPORT.md"
      provides: "Signed-off parity gate — required before Wave 3 begins"
      contains: "PARITY: PASS"
  key_links:
    - from: "ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs"
      to: "ninjatrader/Custom/AddOns/DEEP6/Registry/DetectorRegistry.cs"
      via: "if (UseNewRegistry) _registry.EvaluateBar(...) branch"
      pattern: "if \\(UseNewRegistry\\).*EvaluateBar"
    - from: "ninjatrader/tests/Parity/LegacyVsRegistryParityTests.cs"
      to: "both legacy static ABS/EXH methods and AbsorptionDetector/ExhaustionDetector"
      via: "Assert.That(legacy.Equals(registry))"
      pattern: "Assert\\.That"
---

<objective>
Gate wave. Prove that the migrated AbsorptionDetector + ExhaustionDetector emit bit-for-bit identical output to the legacy DEEP6Footprint.cs static code path. Wire DEEP6Strategy to the registry behind the UseNewRegistry flag. Author the 10 fixtures covering every legacy sub-type with expected values derived from PORT-SPEC.md algorithms (and cross-checked against Python reference unit tests). Commit a parity report. Wave 3+ MUST NOT start until this report shows PARITY: PASS with zero divergences.

Purpose: Absorption + Exhaustion are the highest-alpha signals in the system. Any migration regression poisons every subsequent wave. This gate catches it before we port 34 new signals on top.
Output: 9 new fixtures, legacy-vs-registry parity test suite green, UseNewRegistry branch wired, parity report committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-CONTEXT.md
@.planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-RESEARCH.md
@.planning/phases/16-ninjatrader-8-footprint-indicator-standalone-parallel-delive/PORT-SPEC.md
@.planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-01-SUMMARY.md
@ninjatrader/Custom/AddOns/DEEP6/Detectors/Absorption/AbsorptionDetector.cs
@ninjatrader/Custom/AddOns/DEEP6/Detectors/Exhaustion/ExhaustionDetector.cs
@ninjatrader/Custom/Indicators/DEEP6/DEEP6Footprint.cs
@ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs
@deep6/engines/absorption.py
@deep6/engines/exhaustion.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author remaining 9 legacy fixtures (ABS-02/03/04/07, EXH-02/03/04/05/06) with PORT-SPEC-derived expected values</name>
  <files>
    ninjatrader/tests/fixtures/absorption/abs-02-passive.json,
    ninjatrader/tests/fixtures/absorption/abs-03-stopping.json,
    ninjatrader/tests/fixtures/absorption/abs-04-effort-vs-result.json,
    ninjatrader/tests/fixtures/absorption/abs-07-va-extreme.json,
    ninjatrader/tests/fixtures/exhaustion/exh-02-exhaustion-print.json,
    ninjatrader/tests/fixtures/exhaustion/exh-03-thin-print.json,
    ninjatrader/tests/fixtures/exhaustion/exh-04-fat-print.json,
    ninjatrader/tests/fixtures/exhaustion/exh-05-fading-momentum.json,
    ninjatrader/tests/fixtures/exhaustion/exh-06-bid-ask-fade.json,
    ninjatrader/tests/Detectors/AbsorptionParityTests.cs,
    ninjatrader/tests/Detectors/ExhaustionParityTests.cs
  </files>
  <read_first>
    - ninjatrader/tests/fixtures/absorption/abs-01-classic.json (fixture schema pattern from Wave 1)
    - .planning/phases/16-ninjatrader-8-footprint-indicator-standalone-parallel-delive/PORT-SPEC.md (§2.Detect, §3.Detect — authoritative thresholds)
    - deep6/engines/absorption.py (parity reference for strengths)
    - deep6/engines/exhaustion.py (parity reference for cooldown + sub-types)
    - ninjatrader/Custom/AddOns/DEEP6/Detectors/Absorption/AbsorptionDetector.cs
    - ninjatrader/Custom/AddOns/DEEP6/Detectors/Exhaustion/ExhaustionDetector.cs
    - deep6/engines/tests/test_absorption.py (if exists — fixture values cross-check)
    - deep6/engines/tests/test_exhaustion.py (if exists)
  </read_first>
  <action>
    For each fixture file, construct a synthetic FootprintBar that triggers exactly the target sub-type per PORT-SPEC.md thresholds:

    - **abs-02-passive.json** — extreme zone (top 20% of bar range) holds ≥60% of totalVol, bar closes below that zone (per PORT-SPEC §2 PASSIVE block). Expected direction=-1, SignalId="ABS-02", strength = upperZoneVol/totalVol capped at 1.0.
    - **abs-03-stopping.json** — bar totalVol > volEma20 * 2.0 (StopVolMult) with POC in wick (above bodyTop OR below bodyBot). Expected SignalId="ABS-03".
    - **abs-04-effort-vs-result.json** — totalVol > volEma20 * 1.5 AND barRange < atr * 0.30. Expected SignalId="ABS-04", Direction derived from sign of barDelta.
    - **abs-07-va-extreme.json** — ABS-01 classic case with bar.High within `VaExtremeTicks * tickSize = 2*0.25 = 0.5` ticks of session.vah. Expected: ABS-01 SignalResult present with Detail suffix " @VAH" AND AtVaExtreme-equivalent diagnostic emitted as SignalId="ABS-07".
    - **exh-02-exhaustion-print.json** — single extreme-level volume > (totalVol * ExhaustWickMin/3 / 100) with no follow-through; delta-gate passing.
    - **exh-03-thin-print.json** — ≥3 body-levels with vol < maxLevelVol * ThinPct (0.05).
    - **exh-04-fat-print.json** — first level where vol > avgLevelVol * FatMult (2.0). Direction=0 per PORT-SPEC.
    - **exh-05-fading-momentum.json** — `|barDelta| > totalVol * 0.15`; Direction opposite to bar direction.
    - **exh-06-bid-ask-fade.json** — fixture with a "priorBar" field (extend schema to include it) so `currHighAsk < priorHighAsk * FadeThreshold (0.60)` triggers.

    **Expected values derivation protocol:**
    1. Hand-compute per PORT-SPEC.md formulas; write result in `expected` array.
    2. If Python reference test exists for that sub-type, cross-check: compute the same bar in a Python ipython scratch (not committed) calling `deep6/engines/{absorption,exhaustion}.py.detect(...)` and confirm Python's strength matches to 4 decimals. If it DOES NOT match and PORT-SPEC is correct, a latent Python bug is suspected — follow CONTEXT.md §Python reference bug policy: fix Python first, update Python tests, re-derive expected.

    **Fixture schema extension for EXH-06** — add optional `"priorBar"` field with same shape as `"bar"`; update ExhaustionDetector tests to pass PriorBar through SessionContext.PriorBar.

    **AbsorptionParityTests.cs** — add `[TestCase("fixtures/absorption/abs-02-passive.json")] [TestCase(...)]` rows for each ABS fixture; assert emitted SignalResult[i].SignalId, Direction, and Strength (within 1e-4) match `expected[i]`.

    **ExhaustionParityTests.cs** — same pattern for EXH-01..06. Also test cooldown: after firing EXH-02 on bar N, feed an identical bar at N+1 (still within CooldownBars=5) and assert EXH-02 does NOT fire again (empty for that sub-type).
  </action>
  <verify>
    <automated>for f in ninjatrader/tests/fixtures/absorption/abs-02-passive.json ninjatrader/tests/fixtures/absorption/abs-03-stopping.json ninjatrader/tests/fixtures/absorption/abs-04-effort-vs-result.json ninjatrader/tests/fixtures/absorption/abs-07-va-extreme.json ninjatrader/tests/fixtures/exhaustion/exh-02-exhaustion-print.json ninjatrader/tests/fixtures/exhaustion/exh-03-thin-print.json ninjatrader/tests/fixtures/exhaustion/exh-04-fat-print.json ninjatrader/tests/fixtures/exhaustion/exh-05-fading-momentum.json ninjatrader/tests/fixtures/exhaustion/exh-06-bid-ask-fade.json; do python3 -c "import json;json.load(open('$f'))" || exit 1; done && dotnet test ninjatrader/tests/ninjatrader.tests.csproj --filter "FullyQualifiedName~AbsorptionParity|FullyQualifiedName~ExhaustionParity" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - All 9 fixture files exist and are valid JSON
    - Each fixture contains non-empty `"expected"` array with at least one SignalResult object
    - abs-07-va-extreme.json fixture's expected array contains either Detail string ending in " @VAH" or " @VAL", OR a SignalId "ABS-07"
    - exh-06-bid-ask-fade.json contains a top-level `"priorBar"` key
    - `dotnet test` filtered on `AbsorptionParity|ExhaustionParity` exits 0
    - Test output reports ≥10 passing tests (ABS-01..04 + ABS-07 + EXH-01..06)
  </acceptance_criteria>
  <done>10 legacy signals each have a fixture that exercises their defining condition; parity tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DEEP6Strategy UseNewRegistry=true branch through DetectorRegistry + run legacy-vs-registry parity suite</name>
  <files>
    ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs,
    ninjatrader/tests/Parity/LegacyVsRegistryParityTests.cs,
    ninjatrader/captures/README.md,
    .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-02-PARITY-REPORT.md
  </files>
  <read_first>
    - ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs (current EvaluateEntry + risk-gate code — risk gates MUST NOT be modified)
    - ninjatrader/Custom/Indicators/DEEP6/DEEP6Footprint.cs (legacy ABS/EXH static call sites)
    - ninjatrader/Custom/AddOns/DEEP6/Registry/DetectorRegistry.cs
    - ninjatrader/Custom/AddOns/DEEP6/Detectors/Absorption/AbsorptionDetector.cs
    - ninjatrader/Custom/AddOns/DEEP6/Detectors/Exhaustion/ExhaustionDetector.cs
    - .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-CONTEXT.md (D-04 Wave 2 parity gate, D-06 feature flag, D-10 capture harness)
    - .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-RESEARCH.md (§Feature Flag Integration in DEEP6Strategy)
  </read_first>
  <action>
    Python source reference: n/a (C#-only wiring).
    Target class: `NinjaTrader.NinjaScript.Strategies.DEEP6Strategy` (extend existing, do not move).

    **DEEP6Strategy.cs changes** — ADD ONLY, no modification of:
      - `AccountWhitelist` / `IsAccountAllowed`
      - `NewsBlackout` / `IsNewsBlackoutActive`
      - `RTHWindow` / `IsWithinRthWindow`
      - `DailyLossCap` / `MaxDailyLoss` check
      - any order-submission code

    Changes:
    1. On State.Configure: when `UseNewRegistry == true`, instantiate `_registry = new DetectorRegistry(); _registry.Register(new AbsorptionDetector()); _registry.Register(new ExhaustionDetector()); _session = new SessionContext();`. Else `_registry = null`.
    2. In the existing EvaluateEntry (or OnBarUpdate IsFirstTickOfBar) call site that currently calls the static ABS/EXH methods: wrap in `if (UseNewRegistry && _registry != null) { var results = _registry.EvaluateBar(_currentBar, _session); /* funnel results into same downstream consumer (score/narrative) as legacy path */ } else { /* existing static calls */ }`. The legacy branch is unchanged.
    3. Session reset hook: when existing code detects RTH open, also call `_session?.ResetSession(); _registry?.ResetAll();`.
    4. Risk gates execute BEFORE the branch — same as legacy. Verify by reading file and confirming risk-gate early-returns appear above the detector call site.

    **LegacyVsRegistryParityTests.cs** (the critical gate):
    - Build a test helper `ParityHarness` that takes a `FootprintBar` + `SessionContext` and returns two `SignalResult[]`: one from calling the legacy static ABS + EXH methods (reachable by including the `.cs` files extracted from DEEP6Footprint.cs — since NT8 types are used in the indicator, this may require excluding the NT8 outer indicator class from the test `.csproj` compile glob and referencing only the pure-logic ABS/EXH helpers; if legacy code is tangled with NT8 types, add a `Compile Include` for a new bridge file `ninjatrader/Custom/AddOns/DEEP6/Detectors/Legacy/LegacyDetectorsBridge.cs` that exposes the legacy algorithms as pure BCL statics lifted verbatim from DEEP6Footprint.cs — DO NOT MODIFY the live DEEP6Footprint.cs).
    - For each of the 10 fixtures, assert `Legacy(bar).OrderBy(r => r.SignalId).SequenceEqual(Registry(bar).OrderBy(r => r.SignalId))` with field-wise comparison: SignalId exact, Direction exact, Strength within 1e-4, FlagBit exact.
    - Add 10 additional randomized synthetic bars (seeded `new Random(17)`) that combine ABS + EXH triggers in the same bar; assert parity holds across multi-signal outputs.
    - Include `[Test] public void ExhaustionCooldown_Parity()` feeding the same bar twice consecutively through both paths; assert cooldown suppression behavior matches.

    **captures/README.md** — document the capture harness format (NDJSON per CONTEXT.md D-10 + §specifics): path pattern `ninjatrader/captures/YYYY-MM-DD-session.ndjson`, schema lines `{"type":"depth","ts_ms":...,"side":"bid"|"ask","idx":0..39,"price":...,"size":...}` and `{"type":"trade","ts_ms":...,"price":...,"size":...,"aggressor":0|1|2}`. The harness itself is implemented in Wave 5 — Wave 2 only documents the format so captures can begin off-phase.

    **17-02-PARITY-REPORT.md** — template:
    ```markdown
    # Phase 17 — Wave 2 Parity Report
    Date: YYYY-MM-DD
    PARITY: PASS | FAIL
    Fixtures tested: 10 (ABS-01..04, ABS-07, EXH-01..06)
    Synthetic bars tested: 20 (10 fixtures + 10 seeded-random)
    Divergences: 0
    `dotnet test` output: <paste summary>
    Python-reference bugs found: <list or "none">
    Gate: Wave 3 may proceed.
    ```
    Fill in real values after `dotnet test` completes. If any divergence is found, STATUS=FAIL and Wave 3 MUST NOT start — investigate and fix the detector or the legacy bridge until divergences resolve.
  </action>
  <verify>
    <automated>dotnet test ninjatrader/tests/ninjatrader.tests.csproj --filter "FullyQualifiedName~LegacyVsRegistryParity" --nologo && grep -q "PARITY: PASS" .planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-02-PARITY-REPORT.md && grep -q "if (UseNewRegistry" ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs && grep -q "AccountWhitelist\|IsAccountAllowed" ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs</automated>
  </verify>
  <acceptance_criteria>
    - `ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs` contains `if (UseNewRegistry`
    - DEEP6Strategy.cs still contains identifier `AccountWhitelist` or `IsAccountAllowed` (risk gate retained)
    - DEEP6Strategy.cs still contains `NewsBlackout`, `RTHWindow` (or equivalent), and `DailyLossCap` or `MaxDailyLoss` (all risk gates retained)
    - `ninjatrader/tests/Parity/LegacyVsRegistryParityTests.cs` exists and contains `class LegacyVsRegistryParityTests`
    - `dotnet test --filter "FullyQualifiedName~LegacyVsRegistryParity"` exits 0
    - `17-02-PARITY-REPORT.md` exists and contains the exact string `PARITY: PASS`
    - Parity report states `Divergences: 0`
    - `ninjatrader/captures/README.md` exists and contains `YYYY-MM-DD-session.ndjson`
  </acceptance_criteria>
  <done>UseNewRegistry branch wired, legacy-vs-registry parity green on 20 bars, parity report committed with PASS, risk gates untouched, capture format documented for Wave 5.</done>
</task>

</tasks>

<verification>
- All 10 legacy sub-type fixtures commit and parse as JSON
- Legacy static path and registry path produce bit-for-bit matching SignalResult[] on every fixture (SignalId, Direction, FlagBit exact; Strength within 1e-4)
- DEEP6Strategy UseNewRegistry=false (default) keeps legacy code path active; UseNewRegistry=true routes through DetectorRegistry with zero behavioral delta vs legacy
- Risk-gate code (account whitelist, news blackout, RTH window, daily loss cap) unchanged — grep-verified
- GEX code (DEEP6Footprint.cs lines ~769-924) unchanged — grep-verified
- 17-02-PARITY-REPORT.md committed with `PARITY: PASS`
- **Wave 3+ is BLOCKED until parity report shows PASS with zero divergences** (per CONTEXT.md D-04)
</verification>

<success_criteria>
- Full legacy-vs-registry parity confirmed across 20 synthetic bars covering all 10 sub-types + cooldown behavior
- Wave 3 may proceed (gate unblocked)
- Capture harness schema documented; NT8 operators can begin recording live sessions off-phase for later Wave 5 replay
</success_criteria>

<output>
After completion, create `.planning/phases/17-nt8-detector-refactor-remaining-signals-port/17-02-SUMMARY.md` documenting:
- Fixtures added (paths) + each fixture's trigger condition in one sentence
- LegacyVsRegistryParityTests count + pass/fail
- DEEP6Strategy diff stats (lines added, risk-gate lines modified = 0 required)
- Parity report path + final PARITY status
- Python-reference bugs discovered (if any) — follow CONTEXT.md §Python reference bug policy for each
- Explicit gate statement: "Wave 3 cleared to proceed" or "Wave 3 BLOCKED — divergences: <n>"
</output>
