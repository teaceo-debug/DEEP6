---
phase: quick-260423-d6s
plan: 01
type: debug
wave: 1
depends_on: []
files_expected_to_modify:
  - ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs
  - ninjatrader/simulator/NinjaTrader.Stubs/NinjaScript/NinjaScriptBase.cs
requirements:
  - Reproduce the strategy build failure in a deterministic local harness
  - Fix the root cause blocking DEEP6Strategy from compiling in the simulator build
  - Verify the simulator project builds after the fix
status: in_progress
owner: Hermes
updated: 2026-04-23
---

# DEEP6 Strategy Build Debug Plan

Goal:
Get the strategy-side DEEP6 build path working again by restoring a deterministic compile loop outside NT8 UI automation.

Root-cause target:
`powershell.exe -NoProfile -Command "dotnet build ninjatrader/simulator/NinjaScriptSim.csproj -c Debug"`
currently fails on `ninjatrader/Custom/Strategies/DEEP6/DEEP6Strategy.cs` with CS0118 at the `CopyTo(...)` override.

Execution steps:
1. Keep the simulator build as the primary repro harness.
2. Patch the strategy/stub boundary only as much as needed so the strategy compiles under `NINJASCRIPT_SIM`.
3. Re-run the simulator build and continue until it is green or a new concrete blocker appears.
4. Report the exact build command, root cause, fix, and next blockers if any remain.
