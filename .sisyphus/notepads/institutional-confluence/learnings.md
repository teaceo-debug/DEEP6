2026-05-26: Replaced confluence_server quantsynth dark-pool/regime/composite dependencies with local nq_atlas-derived computation. Use nq_atlas.server.atlas_state dynamically instead of importing a copied singleton so later set_state() injections stay visible to the confluence server.
2026-05-26: equilibrium_module fetch_chain must use Polygon's /v3/snapshot/options/{underlying} with apiKey query params, parse contract metadata from results[].details, and preserve apiKey during next_url pagination. Previous-close index/ETF spot fallback works via /v2/aggs/ticker/I:NDX/prev (or plain ETF ticker like QQQ).
2026-05-26: confluence_server weight assertion is still hardcoded at import time (W_DP/W_GEX/W_REGIME/W_MTF), so import-failure coverage needs a temp source reimport with invalid weights; env monkeypatch alone does not affect the assertion.
2026-05-26: compute_equilibrium can be unit-tested cleanly by monkeypatching fetch_chain and disabling QQQ daily fallback; a minimal contract list with 1D + 5D expiries is enough to produce a non-stale payload and validate sfv/regime structure.
2026-05-27: /equilibrium/nq must treat ndx as optional all the way through the server path; the endpoint signature, cache key, and input normalization all need None-safe handling or the request still 500s before Polygon fallback can run.
2026-05-27: build_alerts should short-circuit to [] when bands lack SFV/sigma-derived edges; this keeps stale-data responses safe and preserves 200 OK even when external API data is unavailable.
