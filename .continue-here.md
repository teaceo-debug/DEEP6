# DEEP6 — Continue Here (Pause Handoff)

**Paused:** 2026-04-17
**Branch:** main · **Latest commit:** `20cf506`
**Active phase:** 18-nt8-scoring-backtest-validation (visual + backtest workstream)

---

## What was happening

User pissed at current Mission Control panel design. Two parallel workstreams paused:

### 1. Aesthetic redesign — awaiting user pick
User wants minimalist symbol-based UI (no footprint, just signal symbols). 5 designs proposed:

- **A — AIRPORT PICTOGRAM** (3 geometric shapes, amber)
- **B — TYPOGRAPHY-ONLY** (one giant word + price)
- **C — FIGHTER JET HUD** (phosphor green vector lines)
- **D — STOPLIGHT** (3 huge circles, 1 lights up)
- **E — JAPANESE ZEN** (single vertical stroke + price)

Summary doc pushed for phone viewing: `.planning/aesthetics-proposals/PICK-ONE.md`
Full design specs in: `.planning/aesthetics-proposals/`
- `A-bloomberg-reinvented.md`, `B-bookmap-pro.md`, `C-linear-modern.md`, `D-cyberpunk-neon.md`, `E-f1-telemetry-hud.md` (originals — superseded)
- `F-mission-control-preview.html` (current shipped — being replaced)

**Next session: ask user "which letter (A/B/C/D/E)?" then rip out current Mission Control + 3-tier hierarchy from `DEEP6Footprint.cs` and ship the chosen aesthetic.**

### 2. Backtest spec — locked, ready to execute

Full spec at `.planning/backtest-tier3-followsignal-spec.md`.

**Hypothesis:** TIER 3 (gray) signals predict price direction when traded mechanically in the direction of the underlying absorption/exhaustion.

**Run all 3 R:R variants:**
- A: 8pt stop / 15pt target (1.875:1)
- B: 10pt stop / 20pt target (2:1)
- C: 5pt stop / 17.5pt target (3.5:1)

**Data:** `data/backtests/nq_3mo_1m.dbn.zst` (3 months NQ 1-min)
**Reference scripts:** `scripts/backtest_3mo_full.py`, `backtest_r1_finetune.py`, `backtest_r3_stress.py`

**Deliverables per variant:**
- `data/backtests/tier3_followsignal_{A|B|C}_trades.csv`
- `data/backtests/tier3_followsignal_{A|B|C}_equity.png`
- Combined report at `.planning/backtest-tier3-followsignal-results.md` (push to GitHub for phone)

---

## What's already shipped to GitHub

| Commit | What |
|---|---|
| `70f73a0` | Mission Control panel + 3-tier signal hierarchy + pulsing arrow + stale-signal expiry |
| `c29b706` | F1 PITWALL preview + Mission Control preview HTML |
| `a7cfa9d` | `PICK-ONE.md` mobile summary |
| `20cf506` | TIER 3 follow-signal backtest spec |

Files installed on Windows via `git pull` + symlink to `%USERPROFILE%\Documents\NinjaTrader 8\bin\Custom\Indicators\DEEP6\`.

---

## Resume protocol (next session)

1. Read this file
2. Read `.planning/aesthetics-proposals/PICK-ONE.md`
3. Read `.planning/backtest-tier3-followsignal-spec.md`
4. **Ask user: "Which aesthetic letter (A-E) do you want shipped first, AND should I run the backtest in parallel?"**
5. Both can run independently — backtest is Python, aesthetic is C#

**Build sim path:** `cd /Users/teaceo/DEEP6/ninjatrader/simulator && dotnet build NinjaScriptSim.csproj` for any C# changes.

**Aesthetic redesign workflow:**
- Read `DEEP6Footprint.cs` lines 754-2280 (existing Mission Control + tier code)
- Strip RenderMissionControl, RenderTier1Overlay, all `_pwHud*Font`, `_pwSurface*Dx` etc.
- Implement chosen aesthetic per its proposal section 8 (SharpDX drop-in code)
- Keep stub-compatible (NO `PathGeometry`, NO `Brush.Opacity`, NO `FillGeometry`)
- Validate: `dotnet build` + `dotnet run -- run all`
- Push to GitHub
- User pulls on Windows, F5 in NT8

**Backtest workflow:**
- Build `scripts/backtest_tier3_followsignal.py`
- Reuse fixture replay loop from `scripts/backtest_3mo_full.py`
- Override scorer to expose TYPE_C signals (currently filtered)
- Run 3 variants, output CSVs + PNGs + summary
- Push results report to GitHub for phone viewing

---

## Known issues / context for next session

- User is on phone, can't easily view local files — push everything to GitHub
- Stub limitations: no `PathGeometry`, no `FillGeometry`, no `Brush.Opacity` mutation. Use `DrawLine` + `FillRectangle` + `DrawTextLayout` only.
- `_lastScorerResult` is `volatile` (HIGH-priority race fix already shipped)
- `ChartPanel.W < 200` early-return guard exists (small-pane safety)
- `_armedSignalBarIndex` + `ArmedSignalValidBars=5` already wired (stale-signal expiry)
- Existing helpers reusable: `MakeFrozenBrush`, `DisposeSolidBrush`, `DrawHaloText`, `DrawCornerBrackets`, `IsArmedSignalValid`

---

**Status:** Both workstreams blocked on user input. Resume by asking the question above.
