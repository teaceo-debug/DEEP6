# For local development (real backend on :8000):
# NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws/live
# NEXT_PUBLIC_DEMO_MODE=false

# For Vercel deployment (no backend — in-browser demo):
NEXT_PUBLIC_DEMO_MODE=true
